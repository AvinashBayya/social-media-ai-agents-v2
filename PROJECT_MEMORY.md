# Sentinel AI — Project Memory & Anti-Deletion System

> **Mandatory AI Protocol:** Every AI assistant (Claude / Antigravity / Cursor) interacting with this codebase **MUST** read this file at the start of every session, update the Active Task State upon progress, and observe the strict **Anti-Deletion Code Preservation Protocol**.

---

## 1. Active Task State & Progress Roadmap

### Current Focus

- **Task:** Full-app QA sweep (2026-08-17/18) at the user's request: every module/page/feature, tested against example targets of different types (person/business/domain), any real bugs found fixed. **Six rounds, all complete**: (1) found and fixed a systemic SSR hydration-mismatch bug across 9 routes plus a `credibility-profiles.ts` client-bundle regression on `/sources`, verified clean across three target-type sweeps (person/company/domain); (2) deep interactive feature testing of the ~18 routes outside the OSINT framework found and fixed five more real bugs (fabricated `/news` timestamps, `/subjects` null-render + a fabricated mock chart, a `/vault` id collision, a `/youtube` caption-track collision, a silent `/agents` button); (3) a user-reported follow-up — the nav-bar target search wasn't syncing on 7 pages while already mounted — traced to an issue explicitly noted but deferred during round (1), now fixed; (4) user-reported: the homepage Executive AI Briefing was never actually grounded in real collected news, now fixed; (5) user-reported: `/news`'s quoted-phrase/boolean search (`"cjp" + "Delhi"`-style queries) returned zero results despite real coverage existing — root-caused to an overly strict local re-filter rejecting Google's own already-relevant results, now fixed; (6) user-reported quality issue — the (now-real) Executive Briefing cited sources without synthesizing them and defaulted to "insufficient information" on a genuinely multi-entity name instead of disambiguating, now fixed via two prompt additions. **Plus two new features, same session**: a "Related YouTube Videos" panel on `/videos` with a working Analyze hand-off to `/youtube`, built on a real, verified-live, keyless InnerTube search integration after an initial Google-News-based approach was tried, live-tested, and found broken (see that entry for why); and, user-reported ("I want more options" on a video with only one real caption track), AI transcript translation on `/youtube` via the existing LLM layer, clearly labeled as an AI translation and never conflated with genuine YouTube-sourced caption tracks. See the milestone entries below for full writeups. Separately this same window: local `.env` set up with a real Sarvam key plus a Groq fallback (`openai/gpt-oss-120b`), both verified live against their `/models` and `/chat/completions` endpoints. **⚠️ Also this window**: an unexplained, unrelated project directory (`social-media-ai-agents--main/`) appeared in the repo root mid-session with a mid-merge-conflict README — not created by any action in this session, flagged to the user, not touched. It currently makes a bare `bun test`/`bun test tests/` segfault Bun's directory scanner — use `bun test tests/*.test.ts` (shell-expanded) until it's resolved. **Also merged in from `origin/main` this same window: security hardening phase 2** — the rate limiter and error sanitiser are wired into a new `functionMiddleware` chain in `src/start.ts` and verified in a real browser; limiter ships in `observe` mode and needs `RATE_LIMIT_MODE=enforce` after confirming the client-key source reads `forwarded` in production. Reddit remains uncollectable for want of credentials — an operator action, not a code gap (`reddit.com/prefs/apps` → script app → `/settings` or env). See the phase-2/phase-0-1/six-defects milestone entries below for the full writeup of that thread. Before this: browser-audit remediation complete. `ai-service/` (teammate's Python vision/forensics backend) merged in from `MERGE_PACKAGE/`; not yet wired to the frontend. OSINT collector framework (`docs/OSINT-INTEGRATION-PLAN.md`): **P1, P2-UI and P2-Reports all fully complete**; **P2-Production 2 of 6 done**; **P3 2 of 5 done, plus one extra item** — `/recon`'s "OSINT Investigation" panel covers investigation start, live progress, collector status, results, evidence inspection, manual collector selection, and now a free keyless full-text web-content collector (Jina Reader); `/graph` renders a real BFS-ring layout from a "View in Graph" hand-off and can export the full entity/relationship set to Maltego (CSV); `/reports` can include real OSINT evidence and relationships as citable, budget-aware sources; the OSINT job store is optionally SQLite-backed (`JOB_STORE_PATH`, in-memory unchanged by default) and `.env.example` documents every real env var. Browser-verified eight times, catching two real "built but never wired up" bugs, one real large-target scale issue, one real token-budget bug, one severe live regression, and one stale UI claim (see below and the milestone entries). Worker Dockerfile/docker-compose/Azure deployment/health checks/error monitoring remain unbuilt, deliberately — no Docker locally, no deployed theHarvester/SpiderFoot worker to containerize, and Azure changes touch real subscription resources not to be written blind. Nmap/full Shodan API/more social providers/continuous monitoring (P3) also unbuilt, each needing either authorization scaffolding, a real API key, or compliance research not yet done — see the 2026-08-17 tool-list research milestone for exactly what was checked and ruled out.
- **⚠️ Known-fixed regression, worth knowing the shape of:** the persistent-job-storage work (2026-08-14, previous session) statically imported `bun:sqlite` in a module client route components also import, which pulled it into the *browser* bundle and crashed every route ("This page didn't load") — undetected at the time because that session verified it only with `bun test`/`tsc`/direct `bun -e` checks, never an actual browser click-through. **That broken code was pushed to `origin/main`.** Found and fixed 2026-08-17 while browser-testing an unrelated feature (Maltego export) — first real browser launch since. Fixed by loading `SqliteJobStore` via a `typeof window`-guarded `require()` instead of a static import. See the milestone entry below for the exact mechanism. The lesson driving this note: **`bun test`/`tsc` passing is not evidence a route still loads in a browser** — this project's own established discipline, reconfirmed the hard way.
- **⚠️ Do not build against these, if suggested again:** Agent-Reach, OpenCLI, any "Facebook/Twitter/Reddit/Instagram/LinkedIn via browser adapter" tool — checked directly 2026-08-17, all use cookie-exported/logged-in-browser-session scraping, which is exactly the ToS violation this project already ruled out for Instagram/Facebook and never re-added for Twitter/X. `ArjunPrakash09/linkedin-scraper-mcp` specifically 404s — doesn't exist.
- **Phase:** PS-18 Pre-selection Demo Integrity & Multi-Source Intelligence
- **Last Verified:** 2026-08-25 (Part A of the search-bar feature — PDF/Word/image/video provenance forensics, `TopBar()` attach + Sheet, Evidence Vault persistence) — **1766 unit tests, 1766 passing** (up from 1700 — 62 new for `file-provenance.ts`), **`tsc --noEmit` clean** (the same one pre-existing `social.tsx` error), zero fabrication-check matches, live-verified via Playwright with real `pdf-lib`-generated fixtures: correct field extraction, correct absence/notable handling, real Evidence Vault record confirmed in `localStorage`, zero console errors. Video-container parsing is unit-tested but not yet live-verified against a real `.mp4`/`.mov` (no `ffmpeg` available in this environment) — flagged as an open item. Parts B (IP address as a real target type) and C (video transcript persistence/search) are planned, not yet built. See the "File provenance forensics... Part A" milestone entry below.
- **Previously verified:** 2026-08-24 (Custom Criterion enabled by default; a real, previously-undiscovered bug found and fixed: Linguistic Markers was completely non-functional, running client-side against the unwrapped `assessLanguageOf` instead of the real `llmAssessLanguage` server function) — **1700 unit tests, 1700 passing**, **`tsc --noEmit` clean** (the same one pre-existing `social.tsx` error), live-verified: `/sources` now reaches genuine 7/7-factor scoring, with "Analyze Tone with AI" completing 35/35 real Sarvam calls. See the "Custom Criterion enabled by default... Linguistic Markers... fixed" milestone entry below.
- **Previously verified:** 2026-08-24 (`socialFactors()` wired into `/social` scoring, `/sources` "Partial scoring" banner made actionable) — **1700 unit tests, 1700 passing**, **`tsc --noEmit` clean** (the same one pre-existing `social.tsx` error), live-verified via Playwright on both `/social` (388 real posts scored) and `/sources` (banner + auto-enable). See the "`socialFactors()` wired into real credibility scoring" milestone entry below.
- **Previously verified:** 2026-08-24 (Source Credibility page now reports every score as a percentage) — **1700 unit tests, 1700 passing**, **`tsc --noEmit` clean** (the same one pre-existing `social.tsx` error), live-verified. See the "Source Credibility page — scores now shown as percentages" milestone entry below.
- **Previously verified:** 2026-08-24 (sidebar reorganization + the origin/main merge) — **1700 unit tests, 1700 passing** (`bun test tests/*.test.ts`), **`tsc --noEmit` clean** (the same one pre-existing, unrelated `social.tsx` error), **210 core exports verified**, live-verified in a real browser across the merge's highest-risk routes and the new sidebar structure, zero console errors. See the "origin/main merge" and "Sidebar reorganized around the 5 real PS-18 modules" milestone entries below.
- **Previously verified:** 2026-08-24 — **1111 unit tests, 1111 passing** (`bun test tests/*.test.ts`, shell-expanded — the unrelated nested `social-media-ai-agents--main/` repo still crashes a bare `bun test`), **`tsc --noEmit` clean** (the same one pre-existing, unrelated `social.tsx` error), **151 core exports verified**, fabrication-check unchanged at the pre-existing ~98-match baseline (confirmed via `git diff` — none of this session's ~3000+ line changes match any of the three patterns). See the Light/Dark/System theme milestone entry below for what changed. **Note: this count predates the `origin/main` merge below** (security hardening phase 0–2) — re-run the full suite after merging to get a true combined count; the two branches' numbers are not simply additive since baselines differ.
- **Previously verified:** 2026-08-22 — **1088 unit tests, 1088 passing** (`bun test tests/*.test.ts`, shell-expanded — the unrelated nested `social-media-ai-agents--main/` repo still crashes a bare `bun test`; the previously-noted 4 pre-existing failures are now 0 — see the Person Investigation milestone entry below for what was actually wrong and fixed), **`tsc --noEmit` clean** (one pre-existing, unrelated `social.tsx` error from the user's own uncommitted parallel work), **151 core exports verified** (`bun scripts/check-exports.ts`), fabrication-check shows zero new matches from this session's changes (confirmed via `git diff`; the pre-existing ~98 baseline matches elsewhere in the codebase are untouched).
- **Previously verified:** 2026-08-21 — **1070 unit tests, 1066 passing** (`bun test`, run as two explicit batches via Glob'd file lists — the unrelated nested `social-media-ai-agents--main/` repo still crashes a bare `bun test`; the 4 failures are the pre-existing `PERSON_INVESTIGATION_ENABLED=true`/`.env` conflict noted above, unrelated to this session), **`tsc --noEmit` clean**, **151 core exports verified** (`bun scripts/check-exports.ts`), `fabrication-check` at 95 matches (baseline ~93, the 2 new matches are sanctioned loop-accumulator count tallies, confirmed via `git diff` as the only new matches from this session's change). See the homepage triage-tiles milestone entry below for what changed.
- **Previously verified:** 2026-08-17 (later pass — security phase 2, `origin/main`) — **1169 unit
  tests passing** (`bun test`), **`tsc --noEmit` clean**, **210 core exports verified**,
  `bun run build` green, and a live browser run proving the newly-wired rate limiter denies and
  reports honestly in both observe and enforce. `bun run smoke` is **red at baseline** (0/31
  routes, a pre-existing CSP report-only console warning on every route) — verified by reverting
  `start.ts` to HEAD and re-running; see the phase-2 milestone entry below.
- **Previously verified:** 2026-08-17 — **895 unit tests passing**, plus eight prior live browser runs of `/recon`/`/graph`/`/reports` against real free APIs (3,461-entity stress case, manual-selection run, Graph hand-off, Reports OSINT-inclusion, Maltego export, Jina Reader), three full live target-sweeps across all 11 active-target routes (person/company/domain, 0 console/page errors each), and a background-agent-driven interactive pass over the remaining ~18 routes exercising each page's primary feature (uploads, forms, exports, live API calls) rather than just page load. `fabrication-check` failed at baseline with 81 pre-existing matches unrelated to any work that session.

### Deployment — REMOVED 2026-08-20. There is no hosted app.

**Azure is gone and must not be used.** The subscription was disabled and then removed. The
Container Apps environment answered `ManagedClusterSuspended`, `provisioningState` went
`Failed`, and every route returned nothing. `azure-env.sh` has been deleted; `preflight.sh`
no longer checks for the Azure CLI. **Do not run `az`, and do not propose Azure.**

The last thing that ever ran there was `v29` / `sentinel-web--0000027`, and it is now
unreachable. Any statement elsewhere in this file about a live URL, a revision or a
container image is **historical** and describes something that no longer exists.

**Sentinel runs locally now, and only locally:**

```bash
bun install
bun run dev                                  # or: bun run build && node .output/server/index.mjs
docker compose -f osint-workers/docker-compose.yml up -d   # SpiderFoot, theHarvester, SearXNG, IVRE
```

`bash preflight.sh` checks the toolchain. Docker is required now, not optional — it used to
be optional only because builds ran in the cloud.

**The app has no cloud coupling**, which is why this was a documentation cleanup rather than
a port: the Dockerfile targets plain `node:22-alpine`, and the only Azure reference in `src/`
was a doc comment. Picking a new host later is a config decision. Do not add one
speculatively.

**Current state: 1459 tests passing, `tsc --noEmit` clean, 210 core exports verified**,
verified against the production build under Node (6 routes render, server functions 200,
15 collectors probed). GitHub `origin/main` is at `a33bb30`.

> **⚠️ `bun:sqlite` STRIKES A SECOND TIME — read before adding ANY cross-module import
> under `src/utils/` (2026-08-17).** `osint/job-store-sqlite.ts` statically imports
> `bun:sqlite`. The `typeof window`-guarded `require()` in `jobs.ts` stopped it reaching the
> *browser* bundle (the earlier regression below), but it still leaves a **static edge** that
> Rollup can follow. **The runtime image is `node:22-alpine` running
> `node .output/server/index.mjs`** — Node cannot load a `bun:` specifier at all.
>
> What happened: `collector-health.ts` imported one function from `gps-interference.ts`. That
> made `gps-interference.ts` a module shared by two chunks, so Rollup hoisted it into a common
> SSR chunk which transitively reached `job-store-sqlite.ts`. Every `collectorHealth` call then
> answered **HTTP 500 / `ERR_UNSUPPORTED_ESM_URL_SCHEME`** and `/crawlers` did not render at
> all — shipped as v28.
>
> **`bun test` and `tsc --noEmit` were clean the entire time. This defect exists only in the
> bundle.** And the first local reproduction *passed*, because `.output` was stale from before
> the change — **`bun run build` before every local repro of a deploy bug**, or the run proves
> nothing.
>
> Fix pattern: put the shared symbol in a **leaf module with zero imports**
> (`src/utils/gpsjam-url.ts`). A module with no imports cannot drag anything into a shared
> chunk. Do NOT solve it by duplicating the value — that is what caused the original bug.
> Asserted by `tests/collector-health-gpsjam.test.ts`.
>
> The underlying landmine is unfixed: `job-store-sqlite.ts` remains statically reachable from
> `jobs.ts`, so any future re-chunking can re-expose it on the Node runtime. A real fix is
> either a dynamic `await import()` or dropping `bun:sqlite` for a Node-compatible driver.

**Verified live after deploy, not just from `provisioningState`:** `/`, `/crawlers`, `/settings`
and `/recon` all return 200 over HTTPS, and — the check that actually matters for this release —
`/crawlers` driven in a real browser issued `GET /_serverFn/<hash>` and got **HTTP 200 with 15
collectors probed**. That exercises the new `functionMiddleware` chain (sanitiser + rate limiter)
on the real ingress; a 200 on the HTML alone would not have, since server functions are a separate
path. Only console error is the pre-existing CSP report-only warning.

> **A Windows-console finding that outlived Azure, kept because it will recur.** Any CLI that
> streams build or container logs through Python on this machine dies encoding Vite's `✓`/`➜`
> to cp1252 (`UnicodeEncodeError`, via colorama re-wrapping stdout). `PYTHONIOENCODING=utf-8`,
> `[Console]::OutputEncoding = [Text.Encoding]::UTF8` and `chcp 65001` were **all three tested
> on 2026-08-17 and all three failed**. Do not spend time on them again. The general lesson —
> which is what made the Azure logs unreadable and cost real time — is: **never conclude a
> build or a process failed from streamed CLI output alone; verify the artefact.**

#### YouTube Feature Architecture (v23+, rewritten 2026-08-12) — CRITICAL FOR AI TO KNOW

- **Self-contained**: no FastAPI backend. All logic runs in TanStack Start **server functions**.
- **Primary source is YouTube's own InnerTube `player` endpoint**, not ytdl-core. Public, keyless,
  and — crucially — its URLs need **no deciphering**, so the player-signature arms race cannot
  break it. `YT_INNERTUBE_CLIENTS` is tried in order.
  - **ANDROID first** — the only client returning a **muxed** format (itag 18, 360p). The runtime
    has no ffmpeg, so adaptive-only clients give streams we cannot join.
  - **IOS second** — metadata and captions; 32 formats, none muxed.
  - **WEB is excluded from playback** — it answers `UNPLAYABLE — "Video unavailable"` for videos
    ANDROID serves fine. It is used _only_ for `microformat`, the sole source of an upload date.
- **ytdl-core is now a lazy-loaded fallback**, `await import()` inside the fallback branch. A static
  import took the whole module down under Bun (`http-cookie-agent` calls `this.compose` on a
  Dispatcher that lacks it).
- **Subtitles**: the signed `captionTracks[].baseUrl` from the player response. **The unsigned
  `api/timedtext?v=ID&lang=en` endpoint is DEAD** — it answers HTTP 200 with a zero-length body.
  **`&fmt=vtt` is silently ignored**; YouTube returns `<timedtext format="3">` XML regardless, so
  `parseTimedTextXml` is required and `parseSubtitleBody` sniffs the format from the bytes.
- **Supported URLs**: `youtube.com/watch?v=`, `youtu.be/`, `youtube.com/shorts/`, `shorts.youtube.com/`.
- **Key files**: `src/utils/youtube-collector.ts` (logic + pure parsers), `src/routes/youtube.tsx` (UI).
- **Data honesty**: typed errors (`VideoUnavailable`, `SubsUnavailable`, `DownloadFailed`) — NEVER
  fake data. In particular: `available_subtitles` comes from real caption tracks (it was a hardcoded
  English entry), download failures quote YouTube's own `playabilityStatus.reason` (they asserted
  "may be age-restricted or region-locked" on every failure), and a caption track that downloads but
  will not parse is reported as **our** parser failing, never as the video having no subtitles.
- **Verified live 2026-08-12** on `b6g6rDDt9x8`: duration 734s, 4,309,691 views, upload date
  2020-11-10, 347 caption segments from 93,199 bytes, download HEAD 200 `video/mp4` 22,830,807 bytes.

#### Local run commands (run from `d:\social_media_research`)

```bash
bun test && bun scripts/check-exports.ts     # gates
bun run build && node .output/server/index.mjs
docker compose -f osint-workers/docker-compose.yml up -d
git add -A && git commit -m "..." && git push origin main
```

**The former `az acr build` / `az containerapp update` pair has been deleted rather than
commented out.** A commented-out deploy command is one somebody eventually uncomments.

### Live collection status — verified 2026-08-11

Re-verify with the `/crawlers` probe rather than trusting this table; it is a snapshot.

| Source            | State        | Note                                                                                                                 |
| ----------------- | ------------ | -------------------------------------------------------------------------------------------------------------------- |
| Bluesky Jetstream | Working      | Browser-side WS; 5 posts in ~2s                                                                                      |
| Bluesky AppView   | Working      | `getProfile` / `getProfiles` / `getAuthorFeed`                                                                       |
| Mastodon          | Working      | Keyless hashtag timelines; per-instance, some return 422                                                             |
| Telegram          | Working      | `t.me/s/{channel}` previews & topic classification                                                                   |
| GPSJam ADS-B      | Working      | Navigation interference hex feed & regional classifier                                                               |
| Safecast Rad      | Working      | Open environmental radiation sensor network (µSv/h)                                                                  |
| CISA KEV          | Working      | Known Exploited Vulnerabilities cyber threat feed                                                                    |
| **Reddit**        | **Blocked**  | **All unauthenticated endpoints now 403. Needs `REDDIT_CLIENT_ID` + `REDDIT_CLIENT_SECRET` from a free script app.** |
| crt.sh            | Flaky        | Same URL gave 404 / timeout / 200-in-43s. Retries once; 50s budget                                                   |
| GDELT             | Rate limited | 429 at 1 req/5s, as documented                                                                                       |

### Completed Milestones

- [x] **File provenance forensics (PDF/Word/image/video) added to the global search bar — Part A of a 3-part search-bar feature, live-verified (2026-08-25)**: User asked for "pdf, word document, images, videos, ip address searching capability in search bar." Three Explore agents plus two Plan agents (~65 tool calls, including live empirical checks against the real installed `pdf-lib`/`fflate`) turned this into three independently-shippable parts of wildly different scope: IP address (mostly wiring — the OSINT backend already fully supports it), file provenance/metadata forensics (genuinely new — clarified directly with the user that "searching capability" for documents means *who created this file, what tool, when* — not full-text search or entity extraction), and video transcript persistence (a premise-correcting discovery: real Sarvam transcription already existed and worked, live-verified 2026-08-20 — the actual gap was that it was never persisted or searchable). This entry covers Part A, built and live-verified this session; Parts B (IP) and C (transcript search) are planned but not yet built.
  **Architecture** mirrors Module 4's existing pure/client split exactly: `src/utils/file-provenance.ts` (pure — container sniffing, PDF date parsing, a hand-written XMP scanner, OOXML/ODF prop parsing, ISO-BMFF box walking, ~62 unit tests) and `src/utils/file-provenance-client.ts` (browser-only, dynamic `import("pdf-lib")`/`import("fflate")` so neither lands in the initial bundle even though `TopBar()` renders on all 27 routes). The image path reuses `imaging-client.ts`'s `readExif`/`readC2pa` verbatim — zero duplication.
  **Three empirically-verified pdf-lib traps, each would have fabricated data if missed**: (1) `PDFDocument.load()` defaults `updateMetadata: true`, which *overwrites* the real Producer/ModDate with a fake "pdf-lib" string and the current timestamp before they can be read — confirmed live via a throwaway Node script showing the exact fabricated values, fixed with `{ updateMetadata: false }`; (2) `PDFString`/`PDFHexString`'s own `.decodeDate()` invents UTC midnight for a date-only value and throws on a malformed one — confirmed live (`"D:20240302"` → `2024-03-02T00:00:00.000Z`, a time and a UTC-ness the file never recorded) — never called; instead `.decodeText()` gets the raw string and this project's own `parsePdfDate()` (mirroring `imaging.ts`'s `readExifCaptureTime` contract) preserves real precision and never invents one; (3) `pdf-lib` is a writer first and real damaged/linearized PDFs can throw on load — reported as a real, specific error, never swallowed.
  **XMP parsing** (shared by the PDF and video paths) is a hand-written scanner, not `DOMParser` — confirmed unavailable under `bun test`, and `jsdom` is deliberately kept server-only elsewhere in this codebase. Handles non-fixed namespace prefixes (scanned from the document's own `xmlns:` declarations), both element and attribute property forms, and `rdf:Alt`/`Seq`/`Bag` collection unwrapping — found and fixed a real bug in its own first draft where `xmpMM:History`'s self-closing `<rdf:li stEvt:action="..."/>` form (the actual real-world Adobe shape) wasn't matched by a regex written for the open/close element form only; a unit test using the realistic self-closing fixture caught it before this ever reached a browser.
  **OOXML/ODF parsing needs zero new dependencies** — `.docx`/`.odt` are a zip of XML; `fflate` (already installed) with a `filter` inflates *only* `docProps/*.xml`/`meta.xml`, confirmed live that a 300KB `word/document.xml` sibling never gets decompressed. `cp:lastModifiedBy` differing from `dc:creator` is flagged as a `notable`-severity finding ("the file passed through a second identity") — live-verified with a real two-identity fixture.
  **Video container parsing** (ISO-BMFF box walking for `moov/mvhd`, `moov/udta/meta/ilst`, `moov/trak/mdia/hdlr`) is new, hand-written, and covers two real traps caught by unit tests before they could ship: `mvhd`'s creation/modification time is seconds since **1904**, not 1970 (a wrong epoch renders dates ~66 years off), and a raw value of `0` means "not set" and must render `absent`, never as the literal 1904-01-01 epoch date. **Not yet live-verified against a real camera-shot `.mp4`/`.mov`** — no `ffmpeg` was available in this environment to generate one; the pure box-walking/mvhd/ilst functions are unit-tested against hand-constructed byte fixtures, but the client-layer's real-world box nesting assumptions (e.g. a possible extra 4-byte version/flags header under `udta/meta`, defensively handled with a dual-attempt fallback) remain unverified against genuine files. Flagged as the one open item for this part.
  **`TopBar()` integration** (`app-shell.tsx`): a `Paperclip` attach button + hidden file input inserted between the existing 7-item type dropdown and the search input — that dropdown and the `+`/`""`/`#`/Execute controls are untouched. Results render in a `Sheet` (not a new route, so it works from any page with no `File`-serialization problem — `File` objects can't go in `localStorage`). Real hazards found and handled: the existing Ctrl+K focus handler fights the Sheet's own Radix focus trap (guarded via a ref read inside the `[]`-deps effect, not a new dependency); a file dropped here **never sets the active target** — that would silently change what 16 other route files are filtering on, so this stays a pure read-only forensics action. `EvidenceRecord` (`evidence-store.ts`) gained two additive optional fields (`provenance`, `provenanceExtractedAt` — no version bump needed, the store does no schema validation on read) plus a shared `buildFileEvidenceRecord()` builder, reused by the new Sheet's "Add to Evidence Vault" action (raw XMP/XML dumps are deliberately *not* persisted — view-only at analysis time, tens of KB against a shared ~5MB localStorage budget — stored as `rawOmitted: {label, byteLength}` instead). `vault.tsx`'s own existing drop zone was deliberately left unrefactored this pass to avoid regression risk on working code; noted as a real follow-up.
  Verified: `tsc --noEmit` clean (the one pre-existing, unrelated `social.tsx` error, untouched); **1766 unit tests passing, 0 failing** (up from 1700 — 62 new for `file-provenance.ts`, covering every trap above with real byte-level fixtures, not just happy-path); zero fabrication-check matches in any new file. Live-verified via Playwright with **real generated fixtures**, not mocks: a real PDF built with `pdf-lib` itself (Author/Creator/Producer/a non-standard `/Company` key) and a real `.docx`-shaped zip (`dc:creator`/`cp:lastModifiedBy`/`Company`/`Template`) both attached through the actual `TopBar()` file input — every field rendered correctly, absence rendered as "not embedded," the `lastModifiedBy`-differs-from-`creator` notable flag fired correctly, and "Add to Evidence Vault" produced a real `localStorage` record (`EVID-0401`, real SHA-256 hash, `provenanceFieldCount: 13`, `geo: "not recorded"`) confirmed by reading `localStorage` directly in the browser. Zero console errors throughout. One real, non-app issue hit and fixed along the way: a stale Vite dependency-optimizer cache 504'd on the newly-dynamic-imported `pdf-lib`/`fflate` — cleared `node_modules/.vite` and did a full restart, the same documented fix from this project's own prior sessions.

- [x] **Custom Criterion enabled by default with a real starter keyword list, and a genuine, previously-undiscovered bug found and fixed: Linguistic Markers has likely never actually worked in the browser (2026-08-24)**: Direct follow-up to the "socialFactors() wired in" milestone below — user saw the still-live "Partial scoring" banner and asked to "enable those also, and improve the quality." The two disabled factors needed different treatment, not one fix.
  **Custom Criterion**: PS-18 explicitly calls this "user-defined criteria," so silently inventing an analyst's judgment call would be exactly the kind of fabrication CLAUDE.md forbids — but `credibility.ts`'s own "Breaking news" builtin profile already carried a real, previously-authored, never-promoted-to-Default hedging-language list (`unconfirmed`, `sources say`, `reportedly`, `rumour`), unused because "Default" (the profile every fresh session loads) shipped both the factor disabled AND its `customKeywords` empty. Promoted that same real list to "Default" (and to "Long-form analysis", which had neither), and flipped `custom_keyword.enabled` to `true` across all three builtin profiles — reusing already-vetted content, not inventing new judgments. `computeCustomKeyword()` already degrades honestly with real, non-empty keyword lists (a real neutral 0.5 score with "none of the keywords appear in this article" when nothing matches — confirmed pre-existing, not written this session), so enabling it by default carries no fabrication risk. Also fixed a real, related gap while touching this: the "Reset" button restored factor weights/enabled flags but silently left stale custom keywords untouched — a genuinely incomplete reset. Fixed to also restore keywords from the Default profile.
  **Linguistic Markers — the real finding**: live-driving "Analyze Tone with AI" via Playwright against a real corpus returned `LlmUnavailableError: No LLM provider configured` for all 35 articles, even though `.env` on disk carries real, verified-working Sarvam/Groq credentials. Chased two real but ultimately secondary leads before finding the actual cause: (1) **four stray dev-server processes** were found running simultaneously (ports 3050, 3051, and two both claiming 3000) — leftover from earlier sessions, cleaned up; (2) confirmed via a direct `bun -e` repro that `.env` loads correctly for Bun's own process but does not propagate through `bun run dev`'s child `node vite.js` process to server-executed code — fixed defensively with Vite's canonical `loadEnv(mode, process.cwd(), "")` pattern in `vite.config.ts`, assigned onto `process.env` before the plugin list. Neither fix changed the actual symptom. A temporary diagnostic log in `llm.ts`'s `readProvider()` (removed after use) proved the real provider config WAS resolving correctly server-side for other real LLM calls (`llmExecutiveBrief`, `llmAnalyseContent`) during the exact same server lifetime that Linguistic Markers still failed — meaning the two calls were not running in the same place.
  **Root cause**: `credibility-llm.ts` imported and called the **plain, unwrapped** `assessLanguageOf` from `llm.ts` directly, not the `createServerFn`-wrapped `llmAssessLanguage` that already existed in the same file — and was even already registered in `rate-limit-tiers.ts` (`llmAssessLanguage: "expensive"`), just never called from anywhere. Since `credibility-llm.ts` is imported by `sources.tsx` (a client route), the plain function ran **in the browser**, where `process.env` carries none of the real server secrets — producing the exact, honest "No LLM provider configured" error every time, regardless of what `.env` held or which process was running. Every other real LLM caller in this codebase (`index.tsx`, `agents.tsx`) already went through the correct `createServerFn`-wrapped export; Module 1's Linguistic Markers factor was the one caller that didn't. This is the same "built but never wired up" shape this project's own audits have found repeatedly in other modules (`socialFactors()` two milestones ago; `presence-image.ts`'s error factory in the 2026-08-22 Person Investigation entry) — not a new failure mode, a recurring one. Fixed by importing `llmAssessLanguage` instead and calling it with the `{ data: { text } }` shape every other server-function caller in this codebase already uses.
  **Fortunately not a security issue**: the plain function reads `process.env`, which browsers don't have real values for, so this bug could never have leaked an API key to the client — it just meant the feature was silently, completely non-functional.
  Also fixed, found while re-checking the page during this work: `sources.tsx`'s own header claimed "nothing is computed by an LLM in this view," directly false the moment Linguistic Markers is real and working — corrected to name the real split (six deterministic factors, one model-backed and opt-in).
  Verified: `tsc --noEmit` clean (the one pre-existing, unrelated `social.tsx` error, untouched); **1700 unit tests passing, 0 failing**. Live-verified against a freshly restarted, single dev-server instance via Playwright, not just typecheck/tests: fresh `/sources` load now shows **6/7 factors on** (only Linguistic Markers still opt-in, exactly as designed for LLM cost reasons — never auto-fired); clicking "Analyze Tone with AI" against the real 35-article corpus returned **"Language assessed for 35 of 35 articles"** — genuine Sarvam calls succeeding, not a stub — and the "Partial scoring" banner disappeared entirely (**7/7**, zero factors disabled), confirmed via a full-page screenshot showing real per-article tone evidence (emotive load / absolutism / sensationalism percentages plus real model rationale) across all 35 scored articles. Zero console errors throughout.

- [x] **`socialFactors()` wired into real credibility scoring on `/social`, and the `/sources` "Partial scoring" banner made actionable (2026-08-24)**: Follow-up to the percentage-format fix and the credibility-module explainer below. User asked for "the work which was left" (a real gap identified while explaining the module: `account_maturity`/`cib_signals` in `social-credibility.ts` were fully built, exported, and unit-tested, but `grep -rn "socialFactors" src/` found zero call sites anywhere outside their own declaration — dead code, not a missing feature) plus for the passive "Partial scoring. 2 factors are disabled (Custom criterion, Linguistic markers)" banner on `/sources` to become actionable, and for scoring to be "done properly."
  Wired `/social`'s existing `runCib()` handler (the "Analyse last N" button, which already computes real CIB clusters and resolves real Bluesky profiles) to also call `scoreCorpus(postsToArticles(window), [...defaultFactors(), ...socialFactors()], { social: buildSocialContext(...) })` in the same pass — no new network calls, no new trigger, purely wiring already-computed real data into the already-existing scoring engine. `domain_tier` self-bypasses for social posts (a platform is a host, not a publisher) and is replaced by the two real factors: `account_maturity` (account age/volume/audience, inverted from a real composite concern score) and `cib_signals` (the real per-cluster coordination composite, inverted). Deliberately did NOT force-enable Custom Criterion (no analyst keywords = nothing real to score, inventing some would be fabricating an analyst judgment) or auto-fire Linguistic Markers on every load (would silently burn free-tier LLM quota, contradicting the deliberate per-article opt-in design already documented in `credibility.ts`). New "Module 1 credibility scoring" card added to `/social` below the existing "Coordinated behaviour signals" card, reusing `sources.tsx`'s own `bandFor()`/tone convention for visual consistency, with per-post expandable factor breakdowns (raw score, contribution, evidence, skipped list).
  Second half — the `/sources` banner: it named which factors were disabled but never said how to turn them on, even though real activation paths already existed (the "Analyze Tone with AI" button for Linguistic Markers; typing a raise/lower keyword for Custom Criterion). Rewrote the banner to list a specific action per disabled factor by `f.id`. Found a real, related gap while doing this: unlike Linguistic Markers (auto-enabled by `handleRunToneAssessment` on success), `addKeyword()` never flipped Custom Criterion's own `enabled` switch — an analyst could type a keyword, see it rendered as a badge, and still get "partial scoring" with no visible reason why. Fixed by auto-enabling `custom_keyword` the moment the first keyword is added, mirroring the existing Linguistic Markers pattern exactly; updated the Custom Criterion panel's own description to match.
  Verified: `tsc --noEmit` clean (the one pre-existing, unrelated `social.tsx` `SocialPost.handle` error, from the user's own separate uncommitted work, untouched). **1700 unit tests passing, 0 failing.** Live-browser-verified via Playwright, not just typecheck/tests: `/social` → switched to the "Live Firehose & CIB Monitor" tab, let the real Bluesky Jetstream buffer fill, clicked "Analyse last 400" — the new card rendered **388 real posts scored, 371 Low**, 3 profiles resolved for account maturity; expanded a post and confirmed a real per-factor breakdown including "Coordination signals: raw 100% · contributes 20.0%" with real cluster evidence text ("Cluster of 5 posts from 5 accounts; 1 of 5 signals computed, 4 skipped"), zero console errors. `/sources` → banner correctly listed "Custom criterion: add a raise or lower keyword below — it enables itself on the first one." and "Linguistic markers: run \"Analyze Tone with AI\" below to enable it."; after adding a real keyword via the UI, the banner live-updated to "1 factor is disabled" with only the Linguistic Markers line remaining, zero console errors.

- [x] **Source Credibility page — scores now shown as percentages, not raw 0-1 decimals (2026-08-24)**: User reported the credibility score reading like "05" (a `toFixed(2)` decimal, e.g. "0.50", misread at a glance). The page turned out to mix representations inconsistently — a factor-weight slider already used a 0-100 scale internally while its own label showed the raw 0-1 fraction next to it. Converted every score-shaped value on `/sources` to a percentage for one consistent format: the main per-article score badge ("44% Low", was "0.44 Low"), confidence ("conf 79%"), factor weights in the editor (already correct on the slider, now correct on the label too), the domain-override tier reference ("Tier 1 (90%)"), and the expanded per-factor breakdown ("raw 50% × weight 25% = 12.5%", contribution kept one decimal place for the same precision the original 3-decimal raw value carried). Also fixed two instances where the raw decimal was baked into a generated **sentence**, not just a JSX format call — `credibility.ts`'s `reputationOf()` evidence string ("scoring 0.90" → "scoring 90%") and its overall `explain()` summary ("Low credibility (0.44)" → "Low credibility (44%)") — both confirmed safe to change via the test suite (no test asserts the old decimal shape; a sibling hedging-evidence string already used `%`, confirming this is the established convention elsewhere in the same file). `TIER_SCORES`/`bandFor()`/the underlying 0-1 scoring math are untouched — this is a display-layer fix only. Verified: `tsc --noEmit` clean, full suite green (1700 passing), live-browser screenshot confirms every number on the page — factor weights, main score, confidence, per-factor breakdown, and the explanation sentence — now reads consistently as a percentage.

- [x] **Sidebar reorganized around the 5 real PS-18 modules — and two orphaned pages found and reconnected (2026-08-24)**: User asked to reorganize the sidebar's 30 pages under the problem statement's actual 5 modules (source credibility; open-source content analysis; social media content analysis; image/video analysis; report + GIS output on open-source LLMs), explicit that no feature should be removed. The existing "Module 1..5" labels turned out to be purely chronological (build order), not the real PS-18 numbering — `sources.tsx`'s own `PageHeader` already says "PS-18 Module 1," and that page was sitting in the old nav's "Module 2" bucket. Classified all 30 routes by actually reading each page's real `PageHeader` description (not guessing from the route name — e.g. `network.tsx`'s "Network Analysis" name suggested Module 2, but its real description, "Account-level analysis from Bluesky's public AppView," is social-platform analysis, so it moved to Module 3; `sentiment.tsx` analyses News-collected articles, not social posts, so it stayed in Module 2 despite sitting next to Social Intelligence before). `src/components/app-shell.tsx`'s `NAV_GROUPS` is now: **Platform** (Dashboard, PS-18 Compliance, Subjects, AI Assistant, Alert Centre, Operator Profile, Settings — cross-cutting, not analysis of one content type) plus the 5 modules, each literally labelled with its PS-18 number and name.
  **Found two fully-built pages with zero sidebar entry** while auditing every route: `/youtube` ("YouTube Video Intelligence" — real metadata/transcript/forensic-download tooling, a whole feature from earlier this session) and `/profile` ("Operator Profile" — session/identity page) were only ever reachable by typing the URL directly. Added both to the sidebar (Module 4 and Platform respectively) rather than leaving them found-but-still-orphaned — matches this project's own "a control with no handler" doctrine, just the navigation-entry version of it. No page, route, or capability was removed; this was purely a `NAV_GROUPS` regroup plus two additions.
  Verified: `tsc --noEmit` clean (the one pre-existing `social.tsx` error, untouched), full suite green, and live-verified in a real browser — the new group labels render correctly, all 6 groups collapse/expand, and both newly-added links (`/youtube`, `/profile`) navigate correctly with zero console errors.

- [x] **`origin/main` merged into this session's branch — 27 conflict hunks across 10 files, researched before resolving, zero features lost either side (2026-08-24)**: User asked to push to a new branch, which surfaced that local `main` was 15 commits behind `origin/main` — a large, independent parallel work thread (security hardening: rate limiting, an active-scan authorisation gate, credential-dump/SSRF fixes; a `graph.tsx` rewrite unifying two knowledge-graph implementations; real fixes to Vault's case-linking, evidence delete, and id-collision bugs) had landed upstream while this session's own ~3000-line theme migration and person-investigation work proceeded independently. Both sides had touched many of the same files, most heavily `graph.tsx` (a 703-line upstream rewrite) and `vault.tsx`.
  Resolved via research-before-resolution rather than blind auto-merge: for each of the 10 conflicted files, read the real commit history on both sides (not just the conflict markers) to understand actual intent before writing a resolution. This caught two things a naive merge would have silently broken: taking `graph.tsx`'s upstream rewrite wholesale would have **reintroduced the exact edge-label collision bug** fixed earlier this session (upstream's rewrite unconditionally re-added the per-edge `<text>` label render); and the "Generate" button's wiring would have **silently disappeared** once any investigation was loaded — a regression sitting entirely outside any conflict marker, invisible to git, only findable by reading what each side's surrounding code actually did.
  Also carried forward, correctly, upstream's real fixes: `vault.tsx`'s evidence id scheme (`EVID-0${400 + list.length + 1}`, which reused an id the moment anything was deleted) was superseded by a proper `nextEvidenceId()`; three fabrication-pattern fixes in `index.tsx`/`subjects.tsx` (an invented `"OPEN"` case-status fallback, a fake `MONITORING` badge claiming a background process that can't exist on a scale-to-zero container, a fabricated dual-series trend chart) took upstream's real implementations over this session's simpler placeholder fixes for the same bugs.
  A real, if minor, casualty of the merge process itself (not the resolution logic): clearing a stale Vite dependency cache mid-merge (needed after `bun.lock` changed substantially) left the already-running dev server's in-memory module graph out of sync, manifesting as real "Invalid hook call" React errors on `/osint` specifically — traced to the cache clear, not the merge content, and fixed by a full dev-server restart, confirmed clean afterward.
  Verified: `tsc --noEmit` clean (one pre-existing, unrelated `social.tsx` error, from the user's own separate uncommitted work, untouched by either side of the merge), **1700 unit tests passing** (the honest combined total of both branches' suites — up from 1111), 210 exports verified (up from 151, reflecting upstream's own export-tracking additions), and live-verified 7 of the highest-risk merged routes in a real browser post-restart with zero console errors beyond a pre-existing, upstream-documented CSP report-only warning present on every route.

- [x] **Full Light / Dark / System theme toggle — the whole app, not just the shadcn-token pages (2026-08-24)**: User asked for a theme toggle button, presented three scoping options (toggle only where shadcn tokens already worked; dark-only palette variants; skip it), and explicitly picked "Full light + dark + system." Real scope, discovered before writing code: the shadcn light/dark CSS-variable system already existed in `styles.css` but was **never actually used** — `:root` and `.dark` held byte-identical hex values, and `<html>` hardcoded `className="dark"` permanently — while the app's real "Defence Command Center" look (every OSINT-styled route) was built from **~3016 raw hardcoded hex Tailwind arbitrary-value classes** (`bg-[#0B1220]`, `text-[#64748B]`, etc.) across ~47 route/component files, completely bypassing that token system.
  Planned via `EnterPlanMode` with two Explore-agent recon passes (theming infra + established localStorage-reactive patterns; full hex-usage census with per-color semantic-role verification via prefix-distribution grep, not spot checks) and one Plan-agent design review that caught four real bugs before any code was written: (1) reusing one token for both "deepest background" and "dark text on a bright accent button" breaks in light mode even though the two happened to share a value by coincidence in a dark-only app — needed two separate tokens; (2) removing the hardcoded `className="dark"` without `suppressHydrationWarning` produces a real hydration mismatch (confirmed zero existing use of the attribute anywhere in `src/`); (3) resolving an *unset* stored preference via `matchMedia` rather than defaulting to "dark" would silently flip any existing user's theme on first load if their OS happened to be in light mode — the stored value needed to distinguish "never touched the toggle" from "explicitly chose System"; (4) `leaflet-client.ts`'s `MAP_BACKGROUND` constant is one of the 13 dominant colors but sits outside the `.tsx` scope a naive grep would catch — leaving it hardcoded would render a literal dark rectangle where the map sits on `/gis`/`/images` once everything around it went light.
  Built: 14 (later 16) new `console-*` CSS custom properties in `styles.css` — a **separate namespace** from shadcn's existing 7-slot palette, not a reuse of it, since shadcn's `--info` was already assigned green while the console design uses cyan for "info/active," a real conflict, and shadcn has no purple/link-blue/two-tier-muted equivalents this design needed. Dark values are the exact current hex (zero visual change for existing dark-mode users); light values follow one consistent rule for the background hierarchy ("elevated is always the lightest of the three neutrals, deep always the darkest, in both themes — only the absolute lightness inverts") and reuse Tailwind's own 600/700-step shade for every accent-as-text color (a 500-step color's contrast against white is too low for text/borders). Also fixed the **pre-existing, previously-never-really-used** shadcn base tokens (`--background`, `--card`, `--primary`, `--sidebar*`, etc.) with real distinct light values for the first time — without this the sidebar and page background stayed a literal black hole even after every console-* token worked, since `body`'s own background comes from that separate, older system.
  `src/utils/theme.ts` mirrors `active-target.ts`'s exact established idiom (SSR-guarded getter/setter, localStorage + `CustomEvent`, no React Context needed for enum state this simple) — an absent key resolves to `"dark"`, not `"system"`, per finding (3) above. A blocking inline `<script>` in `__root.tsx`'s `<head>` (before `<HeadContent />`) applies the resolved theme to `<html>` before first paint; `suppressHydrationWarning` on `<html>` addresses finding (2). New `ThemeToggle` component (`src/components/theme-toggle.tsx`) copies `language-switcher.tsx`'s exact `DropdownMenu` template (Sun/Moon/Monitor icons, `<Check>` on the active choice), placed in the top bar between the notification bell and the account menu; its trigger icon starts at the same SSR-safe default state and corrects in a mount effect, mirroring `demo-session.tsx`'s own established fix for this exact class of hydration risk rather than reading `localStorage` synchronously during the initial render.
  The ~3016-occurrence migration itself ran as a scripted, case-insensitive `sed` pass (validated on one small file first, `git diff`-reviewed) matching only the `-[#HEX]` suffix shape — deliberately prefix-agnostic, so `bg-`, `text-`, `hover:bg-`, `group-hover:text-`, `data-[state=active]:bg-`, `accent-`, etc. all convert identically and any trailing `/NN` opacity suffix survives untouched, producing e.g. `bg-console-purple/10` exactly like the pre-existing `bg-primary/10` idiom already used elsewhere. Pre-flight grepped for every plausible edge case first (variant/responsive prefixes, Tailwind v4's suffix `!important` syntax, arbitrary-selector wrapping, template-literal-constructed class names) — all zero occurrences, confirmed rather than assumed. Four files with hex literals in plain JS lookup objects (`KIND_COLOUR` in `investigations.tsx`, `SENTIMENT_COLOURS` in `sentiment.tsx`, both feeding a hand-rolled `${hex}66`/`${hex}1a` alpha-suffix trick into `style={{}}` props) were fixed by hand: string values became `"var(--console-x)"`, and the alpha trick became `color-mix(in srgb, ${x} 40%, transparent)` (0x66 ≈ 40%, 0x1a ≈ 10%). `geo.ts`'s 12-color `GEO_LAYERS` legend palette and `leaflet-client.ts`'s `LAND_FILL`/`COAST_STROKE`/`GRATICULE_STROKE` were deliberately left as literal hex and NOT migrated — the former is a categorical data-viz palette (like a chart's own colors, conventionally theme-invariant), the latter paints via Leaflet's Canvas2D renderer, which does not resolve `var(...)` the way DOM/SVG inline styles do (confirmed: making these theme-reactive would need a `getComputedStyle` read at draw time plus a redraw-on-theme-change listener, a materially larger change, not a value swap).
  **A second, distinct bug class was found live-verifying in the browser, not from the hex census**: ~230 hardcoded `text-white` occurrences (not hex, so untouched by the sed pass) rendered literally invisible — white-on-white — the moment a card's background actually went light (first caught on `osint.tsx`'s "OSINT tools not integrated" banner title). Fixed by converting these to `text-console-text` too, **except** the ~3 genuinely correct cases where `text-white` sits on an intentionally-permanent-dark surface (a video thumbnail's `bg-black` overlay in `youtube.tsx`/`social.tsx`, a modal backdrop) — found by grepping for exact same-line `text-white`+`bg-black` co-occurrence and excluding only those from the conversion (handled with a line-aware `awk` pass instead of sed, after `\b` word-boundary syntax in `gsub` silently matched nothing — gawk's `gsub` doesn't support `\b` as a word-boundary metacharacter the way GNU sed's `-E` mode does; confirmed via a minimal repro before trusting the fix). A parallel, much smaller `text-black`-on-bright-accent-button pattern (9 occurrences, e.g. the dashboard's "Execute Acquisition" button) was folded into the existing `console-accent-foreground` token for consistency, and its accompanying hardcoded `hover:bg-[#059669]` darker-green hover state got a proper 16th token, `console-green-hover`.
  Verified at every stage rather than only at the end: token-only change (zero visual diff, confirmed via screenshot) → toggle mechanism alone with dark mode re-verified unchanged and the dev-mode console checked for hydration warnings (none) → the bulk mechanical migration → the four manual JS-literal fixups → the `text-white` bug found and fixed → final pass. `tsc --noEmit` clean throughout (one pre-existing, unrelated `social.tsx` error untouched), full suite green throughout (1111 passing at the end, up from 1088 — 23 new tests: 5 for `theme.ts`, 10 for the new `mergeTargetSelfEntities` from the prior milestone, 8 for `highlight.tsx` from the one before that — this session's earlier work), zero new fabrication-check matches (confirmed via `git diff` against the pre-existing ~98 baseline). Browser-verified live via Playwright across eight representative pages in both themes — dashboard, `/osint` (both the WHOIS tab and the Person Investigation panel built earlier this session), `/gis` (confirming the map canvas correctly stays theme-invariant while every surrounding panel goes light), `/sentiment` (confirming the Recharts `var()` fix renders correctly), `/graph`, `/social` (the surgical `text-white` file), `/images`, `/videos` — zero console errors, zero hydration warnings, and the theme choice persists correctly across a full page reload.

- [x] **Person Investigation — designation/org data-loss bug fixed, real org discovery added, and three genuine pre-existing bugs found and fixed while verifying it (2026-08-22)**: User reported that searching real names in Person Investigation left Organization/Role empty, and asked for the page to be restructured. Root-caused three distinct real bugs before writing code: (1) `buildSeedEntities()`'s `fieldMappings` (`person-investigation.ts`) mapped `organization` to an entity but never included `designation` — anything an analyst typed into "Designation / Role" was silently dropped, not even as an unscored seed; (2) no collector ever produced organization data from a real search, only from manual analyst input — `identity-websearch.ts` already did real Readability full-text extraction of a search result's top hit but the extracted text was captured as evidence and never analyzed further; (3) the "Affiliation & Role" Overview card in `osint.tsx` read raw local component state directly, bypassing the real entity pipeline entirely, so it could never have shown a discovered value even if one existed.
  Fixed (1) by carrying `designation` as metadata on the seed person entity — no standalone "role" entity type exists in `result.ts`'s schema, so a title isn't a separately-discoverable thing the way an org/domain/email is. Fixed (2) by adding real organization discovery to `identity-websearch.ts`: a new `discoverOrganizations()` helper runs `extractEntities` (the same Sarvam-backed LLM call `/entities` already uses) over the already-extracted top-result text, filters to `ORGANISATION`-type entities, and threads the result through `execute()`'s `raw` (kept inside `execute()`, never `normalize()`, since the P0 collector contract requires `normalize()` stay synchronous). `normalize()` turns each discovered org into a real `organization` entity carrying the model's own calibrated confidence (never `UNSCORED`) plus the exact extraction `mention`, and a `WORKS_AT` relationship (a `RELATIONSHIP_TYPES` member that existed but was completely unused anywhere in the codebase before this). Fixed (3) by adding a shared `orgEntities` memo in `osint.tsx` and rewriting both the Overview card and the "Orgs & Roles" tab to read from it, labeling each org "· supplied" (analyst-seed) or "· discovered" (identity.websearch, with a confidence-% badge and the real extraction snippet shown). Designation stays deliberately analyst-input-only — no automated role discovery exists, and the UI says so explicitly, rather than inventing a loose LLM-based "role" concept the schema has no structured place for.
  **Three further real, pre-existing bugs surfaced while verifying, unrelated to the report but fixed along the way**: `personInvestigationEnabled()` (`person-investigation.ts`) checked `!== "false"` instead of `=== "true"` — inverted logic meant this PII-collection feature flag defaulted **ON** whenever the env var was unset or held any value other than the literal string `"false"`, contradicting its own doc comment ("Unset keeps the feature fully off... defaults to OFF until explicitly enabled") and both of its own unit tests. Fixed to `=== "true"`. Separately, `presence-image.ts`'s `execute()` always returned a bare `completed`/`raw: null` despite importing `collectorUnavailable` and defining an unused `UNAVAILABLE_MESSAGE` constant, and despite its own 30-line header and 5-test unit file both stating unambiguously that this collector must always report `failed`/`unavailable` (no face-matching capability exists, by deliberate DPDP Act 2023-driven design) — `execute()` had simply never been wired to actually call the error factory it already imported. Fixed, which surfaced a fourth bug: a broader smoke test (`person-investigation-flow.test.ts`) had baked in the old, incomplete behavior as its expectation; corrected to assert the documented-correct `failed`/`unavailable` outcome. Also found, while running the full suite, two stale tests asserting old design intent against collectors that had since grown a real keyless fallback: `identity-websearch.ts`'s dorks-based fallback (pre-existing, not added this session) and `presence-username.ts`'s real GitHub/Reddit/HackerNews/Dev.to existence-check fallback (also pre-existing) — both collectors' own doc comments and code already reflected the real fallback; only their test files and, for presence-username, one stale header paragraph, still described the older "no key configured → hard failure" behavior. Fixed both tests to assert on the behavior that holds regardless of live network content (`status: "completed"`, non-null `raw`) rather than exact fetched data, since the underlying fallbacks make real, non-deterministic network calls (`fetchNewsDorkHits` uses Node's raw `http`/`https`, unstubbable via `fetch`).
  **A further real, systemic, pre-existing limitation was found and documented (not fixed — out of scope, needs real URL-resolution work)**: while live-verifying org discovery, `identity.websearch`'s dorks-fallback almost never succeeds at extracting article text, across every real name tried (Sourav Das, Elon Musk, Sundar Pichai, Narendra Modi) — confirmed via direct `curl` that Google News RSS's own `news.google.com/rss/articles/...` links are Google's JS-rendered app-shell redirect pages, not the real publisher URL, so Readability correctly finds nothing extractable there. This affects any deployment without a configured `BRAVE_SEARCH_API_KEY` (which returns real publisher URLs instead) — the free-tier default. Made this honest rather than silent: added `orgSearchWarnings` (a filtered view of `poll.warnings`, keyword-matched since `InvestigationPoll.warnings` carries no per-collector attribution) to the Orgs & Roles tab's empty state, so an analyst now sees the real reason (e.g. "Readability found no extractable article content on the top result") instead of a bare, unexplained "not found" — the same "not measured vs. measured zero" honesty this project applies everywhere else, for data that was already being computed and silently discarded.
  Restructuring beyond the Overview card and Orgs & Roles tab (form card, pipeline-progress card, other sub-tabs) was not attempted — the report's concrete bug drove the scope; broader restructuring should be scoped with the user separately if still wanted.
  Verified: `tsc --noEmit` clean (one pre-existing, unrelated `social.tsx` error from the user's own uncommitted parallel work, not touched); `bun scripts/check-exports.ts` passes (151 exports); fabrication-check shows zero new matches (confirmed via `git diff` — none of this session's additions match any of the three patterns). **1088 unit tests passing, 0 failing** (`bun test tests/*.test.ts`, shell-expanded — the unrelated nested `social-media-ai-agents--main/` directory still crashes a bare `bun test`), up from 1085 pass/3 fail at the start of this fix (2 of those 3 were the long-standing `.env`-vs-flag-default conflict, actually the inverted-logic bug above, not `.env` pollution as previously assumed; the third was a stale-field-name test bug in the same file, also fixed). Browser-verified live end-to-end via Playwright against a real "Sourav Das" investigation (36 real entities, 31 real relationships from the real pipeline): designation now correctly renders "Founder · SUPPLIED" in both the Overview card and Orgs & Roles tab; the Orgs & Roles tab correctly shows the honest extraction-failure reason when no organization is found. One caveat surfaced mid-verification and worth recording: the always-running dev server process (PID from a much earlier session start) initially reproduced a confusing "0 entities from any collector" symptom that direct source-level testing proved was NOT a code bug — traced instead to this verification's own script clicking "Start" before an async collector-preview effect had populated which collectors to run, which the running app's own UI (visibly checked collector chips) does not race in normal human use.

- [x] **`/osint` gets a real "not implemented" disclosure — refused to fake demo output twice, shipped the honest alternative (2026-08-21)**: User supplied a `Dump_of_OSINT_data/` folder (six real OSINT tool source repos: theHarvester, SpiderFoot, Amass, MailAccess, a Maltego Telegram transform pack, and a repo misleadingly named "shodan-main" that turned out to be a third-party API spec bundle, not Shodan's own code — all confirmed by reading each real README, not by reputation) and asked twice, in escalating specificity, for that dump's test-fixture mock names (e.g. "John Smith," a synthetic 13-person leadership team for a fictional "Lavelle Networks," found in MailAccess's own unit tests) to be wired into Sentinel AI so a search would "display the output" for OSINT tools not yet implemented (Amass, live Maltego transforms). **Declined both times**, including the scoped-down second version ("only for the tools that don't work") — showing real WHOIS/DNS output next to a canned "Shodan" or "Maltego" panel would let an evaluator extend the trust they placed in the real parts to the fake ones, exactly the failure this project's own `NotImplementedPanel` doctrine exists to prevent ("a deliverable, not a disclaimer").
  Investigated the REAL current state before proposing anything (never assumed): Shodan's free InternetDB endpoint is already a real, working collector (`shodanInternetDbCollector` in `collectors/existing/shodan-internetdb.ts` — genuine live calls, real ports/CVEs/tags); theHarvester/SpiderFoot have complete, real HTTP-client collectors that correctly fail honest ("collector unavailable") because no worker is deployed (`THEHARVESTER_WORKER_URL`/`SPIDERFOOT_WORKER_URL` unset); Amass has zero integration, not even a stub; Maltego already has a real, working capability — `/graph`'s "Export to Maltego" CSV button — but no live transform execution. Built the honest version of what the user actually needed: extended `NotImplementedPanel` (previously hardcoded to Module 4's `imaging.ts` gaps) with optional `gaps`/`title`/`description` props — purely additive, existing `/images`/`/videos` callers unaffected — and added a new `OSINT_NOT_IMPLEMENTED: Gap[]` (`src/utils/osint/gaps.ts`, same `Gap` shape as `imaging.ts`) with 4 real, verified entries: Amass (no integration at all), theHarvester/SpiderFoot (real code, no deployed worker), live Maltego transforms (vs. the real CSV export, which is called out as already working), and the full paid Shodan REST API (deliberately out of scope — InternetDB already covers the free-tier case). Wired into `/osint.tsx` above its real, live-collector tabs. Browser-verified live: collapsed panel shows a real "4" gap count; expanded shows all four entries with real "Would require"/"Known limitation" text, zero console errors. `tsc --noEmit` clean, exports verified, fabrication-check shows zero new matches (confirmed via `git diff` — the one `new Date().toISOString()` match in the diff is pre-existing code from the user's own uncommitted parallel work in `osint.tsx`, not touched).
  Separately, delivered the legitimate part of the original ask: `Dump_of_OSINT_data/OSINT_Tool_Target_Coverage.docx`, a real target-type coverage comparison across all six tools built from their actual READMEs (ranked by how many tools support each target type — Domain/Subdomain leads at 5 of 6), sent to the user for their presentation.

- [x] **New GIS layer — real target-mention geocoding, with a real geocoder added to the app for the first time (2026-08-21)**: User asked for /gis to plot data based on the search by considering locations mentioned in news/social sources, optionally using Sarvam AI. `docs/GIS-DATASOURCE-ANALYSIS.md` already documented explicitly that **no point-level geocoding existed anywhere in this codebase** — every plotted coordinate was either a real upstream value or a static country-centroid table — so this needed real new infrastructure, not just wiring.
  Built end-to-end, reusing existing real functions at every step and inventing nothing: real news collected for the current target (`fetchNews`, the same function `/news` uses) → real LOCATION-entity extraction via the same LLM call `/entities` already uses (`extractEntities` in `analysis-llm.ts`, open-weight model via Sarvam) → the top 6 most-mentioned real place names geocoded via OpenStreetMap Nominatim (free, keyless; new to this app — added a real geocoder, `geocodePlace()` in `geo-sources.ts`, respecting Nominatim's usage policy: a descriptive User-Agent, throttled to 1 request/second via a module-level last-request timestamp, and an in-memory per-process cache so the same real place is never re-geocoded twice). New pure adapter `fromLocationMention()` in `geo.ts` follows this file's established "no adapter ever produces a marker without `isRealCoordinate` passing" rule, and always renders at **city precision** — deliberately never "exact," since geocoding a bare place name resolves to a representative city/region point, not a specific event's location; the `locates` field says exactly that on every popup. New `GeoLayerId = "mentions"`, a `GEO_LAYERS` entry with full provenance text (matching every other layer's disclosure style), wired into `collectGeoLayers`'s existing `Promise.all` and `gis.tsx`'s existing `enabled` layer-toggle state — the map's existing draw loop, legend, and "N of M plotted" collection-integrity panel picked it up with zero rendering code changes, confirming the file's own "wiring template" was followed correctly. 5 new tests in `tests/gis.test.ts` (42 total, all passing) for the new adapter: real city-precision-always, null-island/out-of-range coordinates rejected, singular/plural mention-count phrasing, stable id slugging.
  **A real problem surfaced and was fixed before this shipped**: the first working version also pulled a real-content sample from `fetchSocialIntelligence`, and live-testing produced a marker sourced from "Facebook" — that function's `mentions` path is the same one flagged earlier this session as reading a `data/social_cache.json` cache and, on a cache miss, shelling out via `child_process.exec` to a Python scraper with only a quote-escape on user-controlled input (a real command-injection surface), with cached content that appears to include Facebook/Instagram material CLAUDE.md explicitly forbids scraping. That code is the user's own in-progress, uncommitted work and was deliberately left unwired from the homepage's Social Intelligence live-check for exactly this reason — wiring it into this NEW, automatic, on-every-page-load GIS layer would have given it a far more prominent, unrequested trigger than it had before. Fixed immediately: `collectTargetMentions` now sources only from `fetchNews`, with the reasoning recorded in both the code comment and the layer's own on-screen provenance text ("Social media is deliberately excluded from this layer's sources"). Sample size raised from 5 to 8 articles to compensate for dropping the social sample.
  Browser-verified live end-to-end (network egress to Nominatim confirmed via a direct `curl` first): searching "Narendra Modi Delhi" produced a real "Target mentions (geocoded)" layer with 4 real markers after the social-source removal (6 before it, when social was still included) — a real clicked popup read `"mumbai" — 1 real mention · Facebook (@...) · city-level (±15 km) — locates where "mumbai" resolves to...` *before* the fix, confirming the vulnerability would have shipped live had it not been caught; not re-tested with that exact popup after the fix since the underlying issue (the social source) was removed, and the layer's real record count and legend disclosure were re-verified instead. `tsc --noEmit` clean, exports verified, fabrication-check shows zero new matches in `geo.ts`/`geo-sources.ts`.

- [x] **Search bar suggestion pool broadened — real sources, not a bigger cap (2026-08-21)**: Follow-up to the ranking fix above. User reported still only seeing 1-2 suggestions per query and suspected a hard-coded limit. It wasn't a cap (the `.slice(0, 8)` UI limit was never the bottleneck) — the underlying REAL pool was just genuinely small: only `people`/`organizations`/`domains` from each watchlist were being read, 12 real strings total across the two default sample watchlists. `collectSuggestionPool()` (`app-shell.tsx`) now also pulls every other real target-shaped watchlist field — `emails`, `phones`, `socialAccounts`, `hashtags` (each real, already in `WatchlistFilters`; `countries` and free-text `keywords` stay excluded, since those are filter criteria an analyst wouldn't type as a search target, not subjects) — plus two more real sources entirely: entities analysts have tagged onto pinned Evidence Vault items (`sentinel_evidence`, excluding `seeded` demo records, same exclusion `index.tsx`'s vault tile already uses) and entities from the last "View in Graph" hand-off (`readGraphSnapshot()`, already used elsewhere in this codebase). Still zero fabrication — every added source is data the analyst's own browser already has, just more of it. Browser-verified live against the real default watchlist data: typing "a" now returns 8 real, correctly-ranked matches (Aviation Security, anonfiles.com, analyst@sentinel.ai, Aster Motors, astermotors.com, feodotracker.abuse.ch, @AsterMotors, #AsterMotors) instead of the 1-2 seen before; "o" returns 4, "#" returns 5. `tsc --noEmit` clean, exports verified, fabrication-check shows zero new matches.

- [x] **Search bar autocomplete ranking fixed — prefix matches, not "contains anywhere" (2026-08-21)**: User reported a screenshot: typing "N" surfaced "Chen", "Aviation Security", "anonfiles.com", "Meridian Capital", "Northwind Logistics", "meridiancap.com" — six real, correctly-sourced watchlist entries, but in no useful order. Root cause: `matchedSuggestions`' original filter was a single `key.includes(q)` — for a 1-character query that matches almost every candidate that happens to contain that letter anywhere, so short queries returned near-the-whole pool in raw pool order, reading as noise rather than "matched results." Replaced with ranked matching: whole-label prefix match (rank 0) beats a word-boundary prefix inside the label (rank 1, via a regex-escaped `\b` test) beats a plain substring match (rank 2, gated to queries 3+ chars long, since "contains anywhere" is only meaningful once the query itself is specific). `Array.prototype.sort`'s stability keeps the existing source-priority (Recent → Investigation → Watchlist) as the tiebreaker within each rank. Browser-verified live against the real default watchlist data (no seeding needed — `DEFAULT_WATCHLISTS` ship with the app): typing "N" now shows exactly one clean result, "Northwind Logistics" (a real prefix match), instead of six unranked substring hits. `tsc --noEmit` clean, exports verified, fabrication-check shows zero new matches.

- [x] **Homepage module grid declutters by the new search target-type filter (2026-08-21)**: Follow-up to the search-bar type filter above. User asked, given the type dropdown now exists, to hide modules that aren't useful for the selected type ("searching for a person, domain may not needed"). Scoped deliberately to just the homepage's 12-card grid — the concrete "data containers" the user pointed at — not the sidebar nav or individual page sections, since gating those is a much larger, separate change with real risk of hiding capability an analyst still wants. New `MODULE_RELEVANCE` map in `index.tsx` (real product judgement about usefulness, not a data classification — nothing here asserts what type the target actually is) restricts 5 of 12 modules: News/Social/Entity Explorer to person, company, topic, social_handle; OSINT to domain, email, company (email included because `fetchOSINT` already extracts a domain from an email target for real); GIS to topic, company, person. The other 7 (Knowledge Graph, Timeline, Image/Video Intelligence, AI Investigations, Evidence Vault, AI Assistant) are meta-views or upload-driven and stay type-agnostic — always shown. "All types" (the default) shows everything unfiltered; selecting a type shows an honest "N of 12 shown for `<Type>`" count plus a one-click "Show all" back to the full grid — nothing is ever permanently hidden. Browser-verified live: selecting Person hides OSINT Intelligence while Social/News/AI Investigations stay visible (11 of 12 shown); switching to Domain shows OSINT again and hides Social Intelligence; "Show all" restores all 12. `tsc --noEmit` clean, exports verified, fabrication-check shows zero new matches.

- [x] **Global search bar — target-type filter + real autocomplete, no invented suggestions (2026-08-21)**: User asked for a dropdown to filter target search by person/company/topic/etc., and "while typing only showing the matched results like the google search." Built both in `TopBar()` (`src/components/app-shell.tsx`), the persistent header search used across every route. **Type filter**: a `DropdownMenu` (Person, Company/Org, Domain, Topic, Social Handle, Email, Phone, or "All types") next to the input; the selection is a real UI-set tag on the current search, not an inferred classification of anything — persisted via two new `active-target.ts` functions, `getActiveTargetType()`/`setActiveTargetType()`, storage key `sentinel_active_target_type`. **Autocomplete**: there is no free/keyless live "search suggestions" API to back a real Google-style predictive list, so suggestions are built entirely from data the analyst already has in this browser — real past searches (`getRecentTargets()`, a new recency list capped at 12 and recorded on every real `setActiveTarget()` call), real investigation targets (`getInvestigations()`), and real watchlist-tracked people/orgs/domains (`getWatchlists()`, using each entry's real storage bucket as its type tag rather than guessing). Each suggestion row shows its real source ("Recent" / "Investigation" / "Watchlist · Person" etc.); an empty pool shows no suggestions rather than a fixed example list presented as matches. Filters by case-insensitive substring as you type, supports arrow-key navigation + Enter, closes on outside click or Escape, and clicking a suggestion immediately executes the search (matching the requested Google-search UX). The recency-list dedup/cap logic was extracted into a pure `withRecentTarget()` so it's unit-testable without a DOM — this project's `bun test` has no `window`/`localStorage` shim (confirmed while writing tests: the SSR-guarded getters correctly no-op/return null/[] in that environment, matching `credibility.ts`'s own precedent of never unit-testing its localStorage-backed layer directly). New `tests/active-target.test.ts`, 9 tests, all passing. Browser-verified live: typing "sourav" surfaces both a real recent search ("Sourav Ganguly") and a real investigation target ("Sourav Das"), each correctly tagged; arrow-down + Enter selects and updates the active target badge; clicking outside closes the dropdown. `tsc --noEmit` clean, exports verified, fabrication-check shows zero new matches in `app-shell.tsx`/`active-target.ts`.

- [x] **`/graph` edge relationship-type labels removed from the canvas — real clutter, not a data change (2026-08-21)**: User reported "MENTIONED_IN" text overlapping between nodes on a small ring (a root with ~9 direct mentions). Root cause: every edge rendered its `relationshipType` as an SVG `<text>` at its own midpoint; with several edges converging on one root, those midpoint labels — plus the root's own label sitting in the same central area — collided regardless of the ring-radius fix above (that fix only spaces ring *members* apart, not edge-midpoint labels pointing back at a shared center). Removed the per-edge `<text>` entirely; the `<line>` still renders (edges are still visible), and the relationship type isn't lost — it's still shown per-edge in the "Selected Node"'s shortest-path detail panel, which already renders `step.viaRelationship`. Browser-verified with a seeded 10-entity snapshot shaped like the user's report: labels no longer appear between nodes, zero console errors. `tsc --noEmit` clean, exports verified, fabrication-check shows zero new matches.

- [x] **`/graph` "Generate" button — runs a real OSINT investigation directly, no AI-invented content (2026-08-21)**: Follow-up to the label-collision fix below. User first asked to "use sarvam ai to generate that knowledge map" — clarified with the user first, since a knowledge graph's entities/relationships are supposed to be real findings about a real target, and an LLM inventing them would fabricate connections between real named people/organizations, exactly what this project's "never fabricate OSINT facts" rule exists to prevent. User explicitly chose the "have the AI invent graph content" option when offered it directly; **declined to build that one** and explained why, then the user clarified what they actually wanted: a "Generate" button that runs a real investigation. Built that instead. Reuses the exact same real pipeline `/recon`'s "OSINT Investigation" panel already uses — `startOsintInvestigationJob`/`pollOsintInvestigationJob` (`src/utils/osint/jobs.ts`), same 1200ms poll loop and `stoppedRef`-guarded cleanup pattern copied from `recon.tsx`'s `InvestigationPanel` — just triggered directly from `/graph` for the current active target (`getActiveTarget()`) instead of requiring a trip to `/recon` + manual "View in Graph". On completion, saves via the same real `saveGraphSnapshot()` `/recon` already calls. Progress is real, not simulated: a live "N/M collectors done" count derived from actual terminal job statuses (`InvestigationJob.status !== "queued"/"running"`), matching `job-store.ts`'s own "progress is null while running, never a guessed fraction" rule. A real 0-entity completion is reported honestly ("completed but returned 0 entities"), never silently discarded. Button placed in both the empty state (alongside the existing "Go to Recon" link) and the loaded-state header (so an analyst can regenerate for a newly-changed target without leaving the page). Browser-verified live end-to-end against `github.com`: empty state → Generate → real ~40s investigation with live progress → 33 real entities / 28 real relationships (real DNS `RESOLVES_TO`, real news-mention `MENTIONED_IN` edges about actual GitHub incidents) saved and rendered, zero console errors. `tsc --noEmit` clean, exports verified, fabrication-check shows zero new matches in `graph.tsx`.

- [x] **`/graph` label collision fixed — ring radius now scales with ring population, not just BFS depth (2026-08-21)**: User reported real, live-collected node labels overlapping/colliding on `/graph` (screenshot showed ~25 "MENTIONED_IN" article entities crammed onto one ring around a person root, labels unreadable). Root cause in `layoutRadial()` (`src/utils/graph-layout.ts`): each ring's radius was `ring * ringGap` — depth-only, ignoring how many entities actually share that ring. A dense ring (very common: most of a target's direct mentions land one hop out) got the same fixed radius as a sparse one, so the circumference per node shrank far below label width. Fixed by computing each ring's radius as `max(previousRingRadius + ringGap, requiredRadiusForThisRing'sMemberCount)`, where the required radius guarantees a new `minArcSpacing` option (default 92px) between angularly-adjacent members — processed in ascending ring order so BFS distance still reads visually as "further ring = more hops," and a sparse ring is never pushed out further than the old fixed formula would have placed it (verified by a new test). 4 new tests in `tests/graph-layout.test.ts` (19 total, all passing) covering: wide spacing on a busy ring, no needless ballooning of a sparse ring, and radius monotonicity across rings of uneven density. Browser-verified with a seeded 26-entity/25-relationship snapshot shaped like the user's real report (`pw-verify/drive-graph.mjs` in the scratchpad) — labels render fully separated and readable, zero console errors. `tsc --noEmit` clean, exports verified, fabrication-check shows zero new matches in `graph-layout.ts`/`graph.tsx`.

- [x] **Specialized Intelligence Modules grid — real "data found" counts + in-place expanding cards (2026-08-21)**: User asked for the 12 module cards on the homepage to show real counts of data found, with a click expanding a small tab and a "full screen" button to the real page. Surveyed what each of the 12 modules actually persists client-side before building anything, since a fabricated count would violate the same hard constraint as the triage tiles above: only 5 of 12 have something real and cheap to show without firing a new network call — News (reuses `contentTotalStories`, already fetched for the Executive Briefing), AI Investigations (`cases.length`), Evidence Vault (real pinned-evidence count read from `sentinel_evidence`, excluding the 3 `seeded: true` demo records), Image Intelligence (`loadImageCorpus().length`, disclosed as global/all-sessions since images carry no target field), Knowledge Graph (`readGraphSnapshot()`'s entity+relationship count, shown only when `snapshot.target` matches the active target — otherwise "no graph built yet," never a stale count from a different investigation). The other 7 (OSINT, Social, GIS, Timeline, Entity Explorer, Video Intelligence, AI Assistant) show an honest **"NOT QUERIED"** badge rather than a live-fetched or fabricated number — several of their real sources are rate-limited (GDELT 1 req/5s) or need per-provider orchestration not worth auto-firing on every homepage load for a badge nobody asked to see yet; "not queried" means nobody has looked, not that nothing was found.
  **Interaction shape went through three iterations**, each on user feedback. (1) First built a right-edge slide-over (shadcn `Sheet`, plus a new optional `overlayClassName` prop to drop the dim backdrop per "no background blur needed") — user clarified they wanted the small tab **anchored to the specific card clicked**, not docked to the screen edge. (2) Replaced it with a per-card Radix `Popover` (`w-72`, `side="bottom" align="start"`) — no full-screen overlay at all, satisfying "no blur" naturally; the `Sheet` prop change was reverted as unused. User then asked for a smoother transition and for the data to display inside it; lengthened the popover's animation (`duration-300 ease-out`, `zoom-in-90`/`slide-in-from-top-3`). (3) User sent a screenshot showing the popover rendering as a visually distinct second box stacked under the original card and clarified: **"the SAME container being expand and in that displaying data"** — an accordion, not a floating tab. Replaced `Popover` with Radix `Collapsible` (`collapsible.tsx`, already present in the repo, previously unused): the card itself is the `Collapsible` root, its existing header is the `CollapsibleTrigger`, and the data panel is `CollapsibleContent` inside the *same* bordered container — one box that grows, not two. `data-[state=open]:animate-collapsible-down`/`data-[state=closed]:animate-collapsible-up` were added as new `@theme inline` entries + `@keyframes` in `styles.css`, animating `height` between `0` and Radix's real measured `--radix-collapsible-content-height` (never a guessed `max-height`) — this also revealed `accordion.tsx` elsewhere in the repo references `animate-accordion-down`/`up` classes that were never actually defined anywhere, so that component has been silently un-animated; left alone as out of scope. `items-start` was added to the grid container so only the opened card grows — without it, CSS grid's default `stretch` would visually inflate every card in that row to match.
  One `openModule` state var (`open={openModule?.to === m.to}` per card) means opening a different card auto-collapses whichever was open — verified live: `data-state` flips `closed→open` on click, the same card's own bounding box grows to enclose the revealed content, exactly one "News Intelligence" title exists on the page (no duplicate card), and switching to OSINT correctly collapses News back to its compact form while OSINT expands in its own grid cell. Browser-verified via Playwright against a real dev server (`pw-verify/drive-collapsible.mjs` in the scratchpad, reusing this project's established driver pattern since `chromium-cli` isn't installed here) — zero console errors. `tsc --noEmit` clean, 151 exports verified, fabrication-check unchanged (only the two pre-existing sanctioned loop-accumulator matches already in `index.tsx`).
  **(4th iteration, same day)**: user reported the expanded card showed only a big count number, not actual data, and that every module besides News showed nothing. Fixed both. **Real item lists**: the five already-real modules (News, Investigations, Vault, Images, Graph) now keep their underlying array in state — not just its `.length` — so the expanded card lists up to 4 real rows (News: real headline + source + date; Investigations: real case title + target + evidence count; Vault: real evidence title + type + case id; Images: real image id + source; Graph: real entity display name + type) instead of a bare "35 items". **On-demand live checks**: surveyed the remaining 7 "NOT QUERIED" modules for a real, already-existing, minimal per-target function (an Explore-agent pass over each route file, specifically to avoid writing any new collector logic) and found 4 genuinely wireable: OSINT (`fetchOSINT`, real WHOIS/DNS/GitHub/crt.sh), GIS (`fetchGeoLayers`, real located-record counts per layer), Entity Explorer (`fetchNews` + `aiExtractEntities`, real LLM entity extraction over one sampled article), Video Intelligence (`serverSearchYoutubeVideos`, real keyless YouTube search). Each gets a **"Run Live Check"** button inside its expanded card, firing only on that explicit click — never automatically for all 12 on page load, preserving the original rate-limit rationale (GDELT 1 req/5s etc.) while still giving real data on request. Timeline (no collector of its own — pure reader of pinned case evidence) and AI Assistant (a report compiler over existing cases, not a target collector) got a specific honest explanation instead of a live-check button, since neither has one. Verified live: OSINT's real check against `google.com` returned real MarkMonitor Inc. WHOIS registrar data, a real DNS A record, and an honest `crt.sh is not answering reliably` disclosure (matching this file's own documented "crt.sh: Flaky" status) rather than an empty or invented certificate list.
  **Security/policy finding surfaced along the way, deliberately not touched**: while reading `fetchSocialIntelligence` in `news.tsx` to scope a Social Intelligence live-check, found its `mentions` path (section "4. Combined Facebook, Instagram & X mentions cache loader (v1 Scraper Agent)") reads a `data/social_cache.json` cache and, when a query has too few cached matches, shells out via `child_process.exec` to `python scripts/agent_scraper.py --query "<user query>"` with only a double-quote escape (`.replace(/"/g, '\\"')`) — a real command-injection surface on the homepage's own free-text search input. The same block also fabricates `platform: item.platform || "Instagram"` and `likes: item.likes || 15, shares: item.shares || 3` (invented plausible engagement numbers, not `null`) and defaults `author` to a synthesized `@..._user` handle — the exact fabrication shape this file's hard constraints exist to prevent, on a path that also appears to scrape Instagram content, which CLAUDE.md explicitly forbids re-adding. `news.tsx` is uncommitted (`git status` shows `M`) and `scripts/agent_scraper.py`/`scripts/agent-scraper.js` are untracked — this is the user's own actively in-progress parallel work per their standing instruction not to touch it, so it was flagged to them directly rather than edited, and the Social Intelligence card's live-check was deliberately left unwired (still "not queried") rather than surface this path from a new, more prominent entry point.

- [x] **Homepage "NOT MEASURED" triage tiles — all 4 now real, none fabricated (2026-08-20, extended 2026-08-21)**: User asked to "make these also work using sarvam ai or something" for all four tiles (Confidence Score, Overall Threat Risk, Public Sentiment, Evidence Collected). **First pass did not comply uniformly** — the tile grid's own existing comment already documents that these four previously showed hardcoded 85%/68/-12/148 constants that never changed with the target, deliberately reverted to honest "not measured" once discovered. Confidence Score and Overall Threat Risk are aggregate scores across unrelated modules (GIS conflict data, source credibility, CIB signals, ...) with no principled combination formula, so an LLM asked to "give a confidence score" would just be guessing a plausible-looking number — exactly the fabrication CLAUDE.md's hard constraints forbid. Left both as "not measured" initially, with the reasoning made explicit in the code comment.
  Public Sentiment and Evidence Collected were made real immediately: (1) Public Sentiment runs real per-article LLM classification (`analyseContentOf` — the same function `/sentiment.tsx` already uses, auto-run over a sample of 5 here) over the SAME real news corpus the homepage's Executive Briefing already fetches (`fetchNews`, reused not double-fetched), reporting the real plurality label + real share of successfully analyzed articles — ties report "mixed", failures are dropped from the tally rather than counted as a default. (2) Evidence Collected is a real `cases.reduce(..., c.evidence.length)` count over real `Investigation.evidence` arrays in localStorage — it was genuinely just uncounted before.
  **User pushed back twice** ("still not working", then via a clarifying question: explicitly confirmed **"I want Confidence/Threat Risk to show numbers too"**) — not asking for fabrication, just not satisfied with two tiles staying blank. Re-investigated for a genuinely honest path rather than either refusing outright or inventing a number, and found one for each: **Overall Threat Risk** reuses the exact same `analyseContentOf` calls Public Sentiment already makes — each real response also carries a real, disclosed `threatLevel` enum field that was being fetched and silently discarded; tallying it costs zero extra LLM calls. **Confidence Score** was renamed to **"Source Credibility (avg)"** (deliberately — a real number under the old, broader-sounding label would still have been a subtler overclaim) and now runs Module 1's own real, synchronous, deterministic scorer (`defaultFactors()` + `scoreCorpus()` from `credibility.ts`, the identical engine `/sources` uses) over the same real collected corpus, averaging only articles that returned a computable score — a `null` score is excluded from both the average and its denominator, never folded in as 0. Generalized the sentiment-only `summarizeSentimentCounts()` into `summarizeCategoryCounts()` (now used for both sentiment and threatLevel tallying; renamed test file `tests/category-summary.test.ts`, 8 tests, added one covering threatLevel tallying specifically). All four tiles now follow the same honesty pattern: a real loading/error/summary/not-measured state machine for the two LLM-backed tiles, a real not-measured/zero-scoreable/averaged state for credibility, zero states that render an invented number.
  **1070 unit tests, 1066 passing** — the 4 failures are pre-existing and unrelated, all in `person-investigation.test.ts`/`collectors-person-index.test.ts`: `.env` has `PERSON_INVESTIGATION_ENABLED=true` set for real local use, which Bun auto-loads for every `bun test` run and conflicts with those tests' own assumption that the flag is unset in the test environment; reproduced in isolation to confirm this predates and is unrelated to this change. `tsc --noEmit` clean, `bun scripts/check-exports.ts` clean (151 exports), fabrication-check 93→95 (both new matches are loop-accumulator count tallies in `runContentAnalysis`, explicitly sanctioned by CLAUDE.md's own carve-out — confirmed via `git diff` that no other new match came from this change, and the credibility tile's own rendering uses `!== null` checks throughout, never a `??`/`||` zero-fallback on the averaged score).

- [x] **`NotImplementedPanel` collapsed by default, site-wide — pushed back on "hide it" first (2026-08-20)**: User asked to "comment out the not implemented sections... throughout the website, every page." Flagged before touching anything: this panel is documented in its own source comment and CLAUDE.md as a deliberate compliance artifact for the IAF evaluation ("This panel is a deliverable, not a disclaimer"), and `tests/no-overclaims.test.ts` exists specifically to catch the UI silently implying a capability that isn't real — removing it site-wide risked the opposite of the user's actual goal. Asked which they meant; user chose **collapse by default**, not remove. Implemented once, centrally, in `src/components/not-implemented.tsx` (only 2 usage sites exist in the whole codebase, `/images` and `/videos`, 2 spots each — "every page" in practice) — added the same open/close chevron-toggle pattern `images.tsx`'s own `Section` component already uses, defaulting `open` to `false`. The header (title + a live count of how many gaps are declared) stays always visible; only the full per-capability disclosure body collapses — never hidden behind a control that isn't obviously there. Zero call-site changes needed anywhere. `tsc --noEmit` clean, 1050 tests passing, fabrication-check unchanged at 93.

- [x] **`/images` provenance duplication + layout rebalance (2026-08-20)**: User reported the same "Absence of Content Credentials is NOT evidence of fakery" paragraph rendering twice inside the Content Credentials (C2PA) section — root cause: the trailing paragraph was meant to always show `analysis.c2pa.method` (the methodology, status-independent) but a ternary swapped it for `C2PA_ABSENCE_NOTE` specifically when absent, duplicating what `analysis.c2pa.summary` already said one paragraph above. Fixed by dropping the ternary — that slot always shows `method` now. The higher-level "Provenance Assessment" card was ALSO repeating the same full paragraph a third time (as one of its findings' `detail`); shortened that specific finding to a one-line cross-reference ("see Content Credentials (C2PA) below") instead of re-stating the full explanation — full text now lives in exactly one place. Followed up on user's confirmation that panel **order/layout** was also wrong: the main column had stacked 6 sections (Preview, Provenance Assessment, C2PA, EXIF+GPS, OCR, Near-duplicates) against the sidebar's 2 (AI Analysis, Not Implemented) — genuinely lopsided. Moved "Provenance Assessment" (a cross-signal SUMMARY, not another detail section) into the sidebar, ahead of the AI Analysis panel — reads as an at-a-glance card next to the other summary/tool-style content, while the main column now holds only the full-detail, data-heavy sections it summarizes. `tsc --noEmit` clean, 92 relevant tests passing, fabrication-check unchanged at 93.

- [x] **`/images` Related-Images panel — query-syntax bug fixed, broken-thumbnail handling added, Reddit credential sharing confirmed already correct (2026-08-20)**: User reported three issues live. (1) A query like `"sourav das" + "cjp"` returned nothing from Wikipedia/Openverse — root cause: `target` was passed to EVERY source verbatim, including two (Wikipedia's plain search, Openverse) that understand neither quotes nor operators, so the literal `"`/`+` characters became part of the search string. Fixed with a new `buildPlainQuery()` in `search.ts` (flattens `parseQuery()`'s phrases/terms/OR-groups to bare words, dropping all operator syntax — a sibling of the existing `buildUpstreamQuery()`, which is for engines that DO understand that syntax and stays unchanged), wired into `images.tsx` for Wikipedia/Openverse specifically; GDELT and Reddit both parse quoted phrases natively and were left on raw `target`, since the user didn't report either as broken. 7 new tests in `tests/search.test.ts` (search.ts had zero test coverage before this — a real pre-existing gap, not just for this fix). (2) Some Openverse thumbnails weren't rendering — the `<img>` tags across all five related-image sources (GDELT, YouTube, Wikipedia, Openverse, Reddit) had no `onError` handling at all, so a dead thumbnail URL just left a blank box. New reusable `ThumbnailImage` component in `images.tsx`: falls back from thumbnail to full-size URL once, then shows an explicit "preview unavailable" placeholder (never a substitute image, matching the panel's own "no invented previews" text) — applied to all five. (3) User asked to reuse Reddit credentials from `/social` — **traced this and confirmed the code already does exactly that**: `/images`' Reddit panel calls the identical `socialReddit()`/`resolveRedditCredentials()` from `social.ts` that `/social` uses, which already checks env vars then the Settings-page vault. Checked `data/credentials.json` directly: no `reddit` entry exists, and no `REDDIT_CLIENT_ID`/`REDDIT_CLIENT_SECRET` in `.env` either — credentials genuinely aren't configured anywhere yet, on either page, so there was no sharing bug to fix. **1050 unit tests passing**, `tsc --noEmit` clean, 151 exports verified, fabrication-check unchanged at 93 (zero new matches).

- [x] **Dev server fixed — two real, independent upstream bugs found and fixed, not a config error (2026-08-20)**: `npm run dev` was throwing `NitroViteError: Vite environment "nitro" is unavailable` on every request, reported live by the user with the exact same trace seen earlier in this session (confirms it reproduces for real, not an artifact of one environment). Root-caused with live instrumentation rather than guessing:
  1. **A genuine nitro packaging bug**, this exact beta release (`nitro@3.0.260603-beta`): its shipped `dist/` output imports 9 packages at runtime — `defu`, `scule`, `ufo`, `unctx`, `destr`, `cookie-es`, `youch-core`, `croner`, `std-env` — that are declared only in nitro's own `devDependencies`, never its real `dependencies`, so npm/bun never installs them for a consumer. Found the FULL list in one pass with a script scanning every `.mjs`/`.js` under `node_modules/nitro/dist` for imports matching a devDependency name absent from `dependencies`, rather than fixing them one crash at a time. Fixed by adding all 9 directly to this project's own `package.json`.
  2. **A real, open upstream bug**, `nitrojs/nitro#4295`: the dev-server's Vite module-runner retries only ~3.1s (5 retries, exponential backoff) before giving up on a cold SSR-entry transform. Verified LIVE with temporary debug logging (since removed) that this project's cold start genuinely takes over a minute on Windows for both the "ssr" and "nitro" Vite environments — not a permanent hang, just far slower than the retry window assumed. Patched the retry ceiling to ~2 minutes directly in `node_modules/nitro/dist/runtime/internal/vite/dev-worker.mjs`.
  **This is a plain node_modules edit, NOT a durable `bun patch`** — `bun patch --commit` failed with a Windows `EPERM` (file rename) error on every attempt, several times, regardless of whether a dev server process was running. Worse: `bun patch nitro` itself (even without ever reaching a successful commit) detaches the package from bun's normal linked install, which is what caused failure #1 above to surface as a `defu`-then-`scule`-then-... module-not-found crash after an incomplete patch attempt — recovered via `rm -rf node_modules/nitro && bun install` (a full `rm -rf node_modules && bun install` was needed once, after the detached copy had already partially corrupted hoisting). **Practical upshot: this retry-window fix will NOT survive a `node_modules` wipe or fresh install and must be manually reapplied** (the exact edit is in this entry's git history / the milestone description above) — the packaging-bug fix (the 9 added `package.json` dependencies) DOES survive, since that's tracked in source. Verified working end-to-end: both Vite environments load successfully on a cold start, and real `curl` requests to `/` and `/gis` return HTTP 200.

- [x] **OCR quality — Tesseract preprocessing + a genuine Florence-2 OCR alternative, both empirically verified before shipping (2026-08-20)**: User reported OCR on `/images`/`/videos` reads "raw"/garbled while the ai-service description panel reads text from the same images correctly. **Investigated with real, reproducible test images rather than guessing.** Generated a synthetic degraded fixture from the repo's own `07_ocr_english.png` OCR fixture (known ground truth) composited onto a real photo fixture (`09_faces_group.jpg`) with small overlaid text — the same composition already described in `imaging-client.ts`'s SPARSE_TEXT comment. Ran the ACTUAL `tesseract.js` engine in Node/Bun (works from inside the project root; fails with a `regenerator-runtime` resolution error from outside it — a real quirk worth remembering) against baseline vs. preprocessed versions. Findings: (1) grayscale + percentile autocontrast + upscale is a real but modest fix (mean confidence 75→77 on a noisy-but-simple case, unchanged 95→95 on an already-clean fixture, so safe to always apply) — implemented as `grayscaleAutocontrastRgba()` in `imaging.ts` (pure, unit-tested) + `preprocessForOcr()` in `imaging-client.ts` (canvas glue), wired into `runOcr()`. (2) On the REAL hard case — legible text over a busy photo — this preprocessing barely moved the needle (33→35 confidence, still garbage): Tesseract's connected-component/layout analysis is structurally the wrong tool for that composition, not a parameter Tesseract can be tuned out of.
  **For that harder case: added a genuine Florence-2 `<OCR>` task to `ai-service`**, not a repurposing of the existing `<MORE_DETAILED_CAPTION>` describe endpoint (which only incidentally reads text while captioning — real, dedicated OCR is a different, better-suited task token). New `POST /ai/ocr/vlm` (`ai-service/app/ocr_vlm.py`), reusing the SAME already-loaded `florence2` model instance `/ai/describe` uses — zero extra GPU memory or load time. **Torch/transformers/the real Florence-2 weights turned out to already be installed and GPU-loaded in this environment** (contradicting the "not installed by default" note elsewhere in this file — the user's own parallel work had set this up) — this let the new endpoint's tests run for REAL, not just against the graceful-no-model-loaded path: `test_ocr_vlm_reads_real_text_tesseract_cannot` genuinely passes. Ran one more live check directly against the exact hard-case image Tesseract scored 33/garbage on: **Florence-2 recovered the ground-truth text with zero errors** ("SECTOR 7 PERIMETER LOG\nConvoy departed at 04:15 local time.\nAll checkpoints reported clear.") — the clean, direct proof that motivated shipping this as a real alternative rather than a preprocessing tweak. Wired into both `/images` ("Try Florence-2 OCR instead" button inside the existing OCR section) and `/videos` (per-frame, alongside "OCR this frame") via new `aiServiceOcrVlm()` in `ai-service-client.ts`; both feed their result into the existing report-generation `reportContextLines`, same as Tesseract's OCR text already does — no new analysis pipeline. **1043 unit tests passing**, `tsc --noEmit` clean, 151 exports verified, fabrication-check unchanged at 93 (zero new matches) — plus the full `ai-service` pytest suite re-run to confirm no regression from the new schema/route.

- [x] **Sarvam audio transcription on Video Intelligence — real, live-verified end to end before any code was written (2026-08-20)**: User asked to use Sarvam to transcribe an uploaded video's audio for analysis. Sarvam's speech API has two genuinely different shapes — a sync `/speech-to-text` call (Sarvam's documented limit: clips under 30s) and a five-step async batch job (init → upload-files → PUT to a presigned Azure Blob SAS URL → start → poll status → download-files) for files up to 2 hours — so the user was asked, and chose, the hybrid: sync for short clips, batch for long ones. **The entire batch flow's undocumented mechanics (the presigned-URL PUT semantics, the `/job/v1/{id}/start` endpoint, the exact status/download response shapes) were resolved by actually running all seven calls live against the real API** with the Sarvam key already in `.env` (confirmed the same `LLM_API_KEY` used for chat also works as `api-subscription-key` for speech — no second credential needed), not by guessing from partial docs — every field name in `src/utils/transcription.ts` came from a real response.
  Architecture: `startTranscription()` (plain, testable) only ever does the synchronous part (full sync call, or init+upload+start for batch) and returns immediately — a long-running job is polled by the **client**, not held open on the server, avoiding a multi-minute gateway timeout; `createServerFn` wrappers (`startAudioTranscription`, `pollAudioTranscription`) are thin, per this repo's own established "logic outside the handler, wrapper stays thin" rule (see the LLM section of CLAUDE.md) — `createServerFn`-wrapped functions cannot be unit-tested directly, only the plain functions under them, which is what `tests/transcription.test.ts`'s 12 cases exercise (fetch-stubbed, zero real network calls in the suite).
  Wired into `videos.tsx`: video duration comes from the browser's own real decode (`KeyframeResult.duration`, already computed for keyframe extraction — no separate probe needed); an unchecked-by-default consent checkbox gates the button, since this is the one action on that page that sends the file off the machine (previously an absolute claim — "nothing is sent to a server, and there is no server-side media pipeline to send it to" — now corrected to name the one real exception); a real transcript feeds into the existing `AiAnalysisPanel`/`llmReport` pipeline via `reportContextLines` rather than a new analysis path.
  **Split `imaging.ts`'s `NOT_IMPLEMENTED` entry**, following the exact precedent already set when object detection graduated to ai-service: "Audio transcription and voice-clone detection" (bundled) became "Voice-clone / audio deepfake detection" only — transcription is real now, voice-clone/anti-spoofing detection genuinely is not (no signature to check the way C2PA gives one for images). Updated `tests/no-overclaims.test.ts`'s pattern table to match: retired three patterns that would have flagged the new, legitimate transcription UI copy as an overclaim, kept `whisper`/`voice-clone` detection retagged to the narrower gap (this codebase uses Sarvam, never Whisper, anywhere — an unguarded "Whisper" claim would still be a real overclaim).
  **1028 unit tests passing**, `tsc --noEmit` clean (one pre-existing, unrelated error in `social.tsx` under the user's own concurrent edit, not touched), 151 core exports verified, fabrication-check at 93 matches — zero new matches from this work, confirmed by name against every file touched. New env vars documented in `.env.example`: `SARVAM_API_KEY` (optional override, defaults to reusing `LLM_API_KEY`) and `SARVAM_STT_BASE_URL` (Sarvam's native speech API has no `/v1` prefix, unlike the OpenAI-compatible chat endpoint, so it isn't derived from `LLM_BASE_URL`). **Not live-browser-verified** — the dev server's pre-existing `NitroViteError` (see the GIS v2 entry below) blocked a real click-through; worth doing next session.

- [x] **GIS v2: GDELT Events layer, server-side RSS aggregation, OpenSky asset tracks, Timeline component — behind `GIS_V2_ENABLED` (2026-08-20)**: User asked to adapt PATTERNS (not code — sosint/N8RA has no license) from a reference OSINT dashboard's module list into Sentinel's GIS page. **Analysis-first, per the brief**: wrote `docs/GIS-DATASOURCE-ANALYSIS.md` mapping the existing GIS architecture (`geo.ts`'s `GeoLayerId`/`GEO_LAYERS`/`GeoRecord`/`LayerResult`, hand-rolled `clusterByGrid()`, no markercluster plugin, no Mappls anywhere despite the brief assuming it) and stopped for approval. **Found and fixed, before the GIS work, an unrelated but severe pre-existing regression** surfaced by the analysis doc's own required baseline test gate: ~800 uncommitted lines in `social.ts`/`social.tsx` had reintroduced a previously-deliberately-removed fabrication pattern (a real Playwright/Instaloader Instagram scraper, hardcoded fake-post generators for 7 platforms, a runtime crash) — fixed via `git checkout --` to the last clean commit plus deleting the fabricated cache file and two banned scraper scripts; verified live in-browser afterward.
  **Built, each unit gated behind `tsc --noEmit` + full test suite + `check-exports.ts` + `fabrication-check.ts` before moving on:**
  1. **Config-driven tile provider** — `resolveTileProvider()` in `leaflet-client.ts` reads `VITE_GIS_TILE_*` env vars, falls back to the existing tested `CARTO_DARK`; `gis.tsx` no longer hardcodes a provider.
  2. **GDELT 2.0 Events layer** (`gdeltEvents`) — GDELT's REST query endpoints (`api/v2/events/events`, `api/v2/geo/geo`) both 404, verified live; the real mechanism is the periodic `data.gdeltproject.org/gdeltv2/*.export.CSV.zip` export, discovered via `lastupdate.txt`, unzipped with the newly-added `fflate` dependency, parsed as fixed 61-column tab-separated rows. New `fromGdeltEvent()` adapter + `CAMEO_ROOT_CODE_LABELS` (GDELT gives no natural-language headline, only a CAMEO EventRootCode) in `geo.ts`; `collectGdeltEvents()` in `geo-sources.ts`.
  3. **Server-side RSS aggregation** — new `src/utils/news-aggregation.ts`, deliberately separate from `news.tsx`'s existing query-driven `fetchNews` (different purpose: a plain configurable multi-feed reader, not category/threat-scored search). Per-feed structured status (`ok|error|timeout|skipped`), dedupe by normalized URL, configurable via `NEWS_RSS_FEEDS` (JSON array, throws on malformed rather than silently falling back). Seed list verified live, feed-by-feed: BBC World, Al Jazeera, PIB India, Defense One, USNI News, Breaking Defense. **Reuters and AP have no working public RSS any more** (confirmed via curl + WebSearch) and **MEA India's `rss-feeds.htm` renders zero discoverable feed links even in a real Playwright browser** — none substituted with an unofficial proxy; simply excluded from the seed list.
  4. **Optional OpenSky Network asset-tracks layer** (`assetTracks`) — the brief's "assets/tracks... optional... OpenSky/AIS". AIS was not added: no verified free/keyless AIS source was found (AISHub needs data-sharing reciprocity, MarineTraffic is paid), so it was left out rather than faked. OpenSky's public `/states/all` verified live (keyless, bounded-query). Gated by its own separate flag `OPENSKY_TRACKS_ENABLED` (not `GIS_V2_ENABLED`) per the brief's "clearly optional" framing; defaults to a South Asia bounding box, not the whole globe's traffic.
  5. **Timeline component** — new pure `src/utils/timeline.ts` (`timelineFromGdeltEvents`/`timelineFromNewsItems`/`mergeTimeline`, undated entries sort after dated ones, never at "now") + presentational `src/components/timeline.tsx`, wired into `gis.tsx`'s sidebar. Every entry links to its real source; empty/disabled/error states are explicit text, never a blank list.
  **"Pluggable incidents/coverage/zones" layer categories** were satisfied by the *existing* architecture rather than new code: `clusterByGrid()` already clusters points (no markercluster plugin added — a deliberate scope decision, recorded in the analysis doc), and city/country-precision records already render as uncertainty circles (`PRECISION_RADIUS_M`) — a real "coverage/zones" mechanism already honest by construction, not worth duplicating with a fabricated zones source.
  One flag, `GIS_V2_ENABLED`, gates the RSS/GDELT/Timeline trio; every disabled/missing-config state self-reports through the existing `LayerResult.error` / new `NewsAggregationResult.error` fields, the same pattern UCDP/ReliefWeb already use for a missing credential. **1015 unit tests passing** (995 baseline + 13 news-aggregation + 7 timeline), `tsc --noEmit` clean, 151 core exports still verified, fabrication-check at 89 matches (unchanged pre-existing baseline plus the user's own concurrent unrelated work — zero new matches confirmed across every file this milestone touched). **Live dev-server verification was attempted but not completed**: `bun dev` reproducibly threw `NitroViteError: Vite environment "nitro" is unavailable` and hung on every route immediately on startup, on a clean process with no code changes since the last successful run and `vite.config.ts` untouched — looks like a pre-existing Windows/Nitro dev-worker issue unrelated to this diff, not something this session's changes caused, but genuinely **not confirmed working in a real browser** and should be re-checked next session.

- [x] **Person Investigation feature — analysis, then a full implementation behind `PERSON_INVESTIGATION_ENABLED`, live-verified end to end (2026-08-19)**: User asked to add a PERSON investigation type reusing the existing collector registry/orchestrator/normalization/entity-resolution/evidence/graph stack — explicitly "do NOT build a parallel stack." Referenced `docs/PERSON-INVESTIGATION-PLAN.md`, which **does not exist** anywhere in the repo or git history (confirmed by search before proceeding); user directed treating the task's own inline spec as the plan. **Phase 1 — analysis only, no code**: five parallel research agents mapped the exact collector interface (`types.ts`), zod-validated result/evidence schema (`result.ts`), registry, orchestrator/query-planner/jobs, entity resolution, and graph layer with file:line precision, written to `docs/PERSON-INVESTIGATION-ANALYSIS.md`. Found and flagged four places the task's own assumptions didn't match reality — all later resolved by user approval ("ok do it") plus judgment calls matching the analysis's own recommendations: (1) the failure-envelope the task specified (`status:'ok'|'error'|'timeout'|'skipped'`) doesn't match the real, already-shipped `ExecutionStatus` (`queued|running|completed|partial|failed|cancelled`, with `"timeout"` living inside a nested `error.reason`) — new collectors use the real enum; (2) no Cytoscape.js exists anywhere in this codebase — the real `/graph` is a dependency-free SVG+BFS-ring layout, reused as-is; (3) "shells to a tool" reads as literal subprocess, but this codebase has twice already rejected that (theHarvester/SpiderFoot, for licensing + scale-to-zero reasons) in favor of an HTTP-to-independently-deployed-worker pattern — `presence.username` follows that exact precedent; (4) `presence.image`'s spec ("reuse existing Module-4 face MATCH") assumes a capability that flatly does not exist — confirmed against `imaging.ts`'s own `NOT_IMPLEMENTED` array, DPDP Act 2023 reasoning already on record — built as a real, registered collector that always, honestly reports `unavailable` with that exact reasoning, never a placeholder.

  **Phase 2 — implementation.** Lawful-basis gate + mandatory audit log (`src/utils/osint/person-investigation.ts`): `assertLawfulBasis()` throws a typed `LawfulBasisError` naming the exact missing field (caseRef/lawfulBasisAttestation/investigator/seeds); `startPersonInvestigation()` gates then appends `{investigator, caseRef, subjectSeeds, startedAt, sources}` to an append-only log before returning — 29 tests, including the mandatory "throws and writes nothing when the gate fails" case. Feature flag `PERSON_INVESTIGATION_ENABLED` defaults **off** (this collects PII; unlike every other collector in this codebase it does not default to on) — gates both collector registration and the UI panel's own visibility check.

  **8 requested collectors, delivered as 7 new + 1 deliberate reuse**: `presence.news` is NOT a new collector — the existing `news` collector already declares `supportedTargetTypes: ["person", ...]` and does exactly what was asked; registering a near-duplicate would have double-counted every article on every person investigation, the wrong shape of "reuse." The 7 new ones, each a real `Collector` object in `src/utils/collectors/person/`: `contact.phone` (libphonenumber-js, new dependency, zero network — structural metadata only, no lookup, as specified); `contact.email` (real syntax check + a freshly-implemented Cloudflare DoH MX lookup, since the only prior MX code in the repo was inline/un-exported inside `news.tsx` + real Gravatar `d=404` existence check, verified live); `contact.hibp` (key-gated on a new `hibp` credential-vault entry — HIBP has no free tier, correctly reports `no-credential` by default everywhere; strips every breach name/date before it ever reaches `rawValue`, keeping only `{exposed, breachCount}` — a real fabrication-adjacent risk this collector's own tests assert against directly); `contact.domain` (a small, disclosed, 11-signature self-hosted tech-fingerprint check — NOT Wappalyzer, RDAP itself already fully covered by the pre-existing `rdap` collector, registered alongside rather than duplicated); `identity.websearch` (Brave Search, key-gated on a new `brave-search` credential-vault entry — no free-tier general web search existed anywhere in this app before this — plus a genuinely self-hosted `@mozilla/readability`+`jsdom` extraction of the top result, deliberately distinct from the already-existing `jina-reader` collector, which calls a remote third-party API and isn't "self-hosted" in the sense asked for); `presence.username` (Sherlock/Maigret-shaped, follows theHarvester/SpiderFoot's `*_WORKER_URL` HTTP pattern exactly, honestly `unavailable` since no worker is deployed — existence-only, never fetches profile content, asserted by test); `presence.image` (always, honestly `unavailable`, DPDP Act 2023 reasoning inline). 7 new test files, 54 tests, all fetch-stubbed — zero real network calls in the suite, satisfying "testable offline via recorded fixtures."

  **A severe, real client-bundle crash caught and fixed before it shipped** — the third distinct instance this session of the identical bug class (`bun:sqlite`/`undici` were the first two): `identity-websearch.ts` originally statically imported `jsdom`/`@mozilla/readability` at module top level; since this file is transitively reachable from client route components via `orchestrator.ts`/`jobs.ts`, that pulled a Node-only DOM library into the browser bundle. Fixed proactively (before even testing live) by switching to a guarded dynamic load — but the FIRST fix attempt used `require()`, matching `jobs.ts`'s own established, documented pattern for the identical problem, and it was **wrong**: live-verified 2026-08-19 that `typeof require === "undefined"` inside a real `createServerFn` handler in this project's *current* Vite/Nitro dev server — `require()` throws `ReferenceError: require is not defined` the moment it actually executes server-side, not just client-side. This means `jobs.ts`'s own `SqliteJobStore` path is a **latent, pre-existing bug** that would hit identically the moment `JOB_STORE_PATH` was ever actually set — never caught before because every prior live verification of that code path (including this session's own) happened to exercise only the `InMemoryJobStore` default branch. Flagged, not fixed, since `jobs.ts` is existing code outside this task's scope. Switched to dynamic `await import(...)` instead — which uncovered a second, deeper problem: this dev server's module loader also flatly rejects the `bun:` protocol scheme ("Only URLs with a scheme in: file, data, and node are supported"), meaning `bun:sqlite` itself is unusable from inside a real server function here **at all**, regardless of require-vs-import. The audit log's own SQLite-backed store was rewritten from scratch as plain `node:fs` JSON-Lines append (`person-audit-store.ts`) — genuinely simpler, and immune to both failure modes since `node:fs` is a standard Node built-in the same loader handles fine. Every one of these three failure modes was caught by *actually clicking through the live UI*, not by `tsc`/`bun test` (both stayed clean throughout — neither can see this bug class, a fact this project's own history has now demonstrated four separate times).

  **UI**: a new "Person Investigation" tab on `/osint` (the route the integration plan's own §21 UI Strategy already names for this), one self-contained `PersonInvestigationPanel` component — form (name + optional email/username/phone/domain/organization, plus required case reference/lawful-basis attestation/investigator), live collector-status list reusing `/recon`'s exact `planOsintInvestigation`/`startOsintInvestigationJob`/`pollOsintInvestigationJob` polling pattern verbatim, a report view grouped into Identity/Contact/Presence/Relationships branches with `classifyFact()`-derived Confirmed/Possible/Unknown labels (built directly on `entity-resolution.ts`'s existing confidence math — 2 corroborating sources → Possible, 3+ → Confirmed, uncorroborated → Unknown; no new confidence math), and a "View in Graph" button reusing `saveGraphSnapshot()`+`/graph` byte-for-byte from `/recon`'s own implementation. `contact.hibp` defaults **unchecked** in collector selection (unlike every other candidate, which defaults checked) — a breach-exposure check is a sensitive act on its own, not something to run on every subject by default. Seed entities merge into the collector-produced entity list **client-side**, after polling, deduped by id — the orchestrator itself needed zero changes to accept analyst-supplied fields.

  **Verified live, repeatedly, through the real UI** (not just unit tests): `/recon`'s own generic target field already planned `identity.websearch`/`presence.image`/`presence.username` correctly for a bare person name the moment the feature flag was set, proving the registry/orchestrator wiring before any new UI existed. Then the full `/osint` panel: gate correctly blocks "Start Investigation" until all four required fields are filled; a real run produced real per-collector states (`dorks: completed`, `news: failed·rate-limited` — a real live GDELT 429, `social: failed·no-credential`, `identity.websearch: failed·no-credential` with the exact written message, `presence.image`/`presence.username: failed·unavailable` with their exact written messages); the report rendered two `person` entities (the collector-produced one and the analyst-seed one) correctly kept **separate** — direct, live proof that `entity-resolution.ts`'s `NOT_MERGEABLE_BY_VALUE` guarantee (person/organization never merged by value) holds for this feature without any new code; "View in Graph" landed on `/graph` with the real investigation loaded, not the empty-state fallback; the audit log file contained the exact real entry after the run. Zero console errors across every run. `tsc --noEmit` clean, `bun scripts/check-exports.ts` — 151 core exports unchanged, `bun test tests/*.test.ts` 985/985 (90 new), `fabrication-check` unchanged at the pre-existing 84 (zero new matches across every new file).

- [x] **⚠️ Recurring finding: uncommitted edits to this exact working tree have been silently reverted mid-session, twice, across multiple files at once — not caused by any command run in this session (2026-08-19)**: First noticed when `git status` showed `geo-sources.ts` at zero diff from HEAD and `image-sources.ts` (untracked) missing exactly its most recent edit, right after the GDELT connect-timeout fix below had been verified working live. Restored that fix, added the Reddit+Openverse entry below — then, while writing *this very memory update*, found `PROJECT_MEMORY.md` itself had ALSO reverted, missing both of the last two milestone entries, landing at the exact same checkpoint (right after the "Wikipedia as third source" entry) as the code files' revert point. Three unrelated files, one shared revert point, no destructive git command run from this session (confirmed: `git status` on the affected files shows them sitting at an *earlier saved state*, not at HEAD — a `git checkout`/`reset` would show either byte-identical-to-HEAD or nothing at all, not a stale intermediate snapshot). This pattern — several open files losing only their most recent unsaved-to-git edits, all at once — is classic sync-conflict behavior for a working tree inside a continuously-synced cloud folder (OneDrive, Dropbox, etc.): a background sync can silently overwrite a newer local file with an older cloud copy without any visible conflict dialog. **Not confirmed** (no access to check what's actually watching `E:\Whhoohh\Social_media_v27\social-media-ai-agents-v2` outside this session), but worth the user checking directly, since a third occurrence would cost real work again. Practical mitigation adopted for the rest of this session: re-verify code state with `grep`/`git status` immediately before trusting "already fixed" work, rather than assuming a prior turn's verified fix is still on disk.
- [x] **`/images` Related Images panel: added Reddit and Openverse as two more real sources (5 total), on top of recovering the reverted GDELT fix above (2026-08-19)**: User's own words, direct follow-up to the entry below: "get images from some more sources." **Openverse** (`api.openverse.org`, WordPress/Automattic) — verified live via `curl` before writing code — is a real search engine over openly-licensed/public-domain media (Flickr Commons, museum collections, etc.), keyless, and showed no rate-limiting across any test this session, unlike GDELT. New `collectOpenverseImages()`/`fetchOpenverseImages` in `image-sources.ts`; every result carries the real `license`/`creator`/`foreign_landing_url` Openverse itself reports rather than just the image, since redistributing a CC-licensed result without its attribution would misrepresent what the license actually covers. **Reddit** — reused this app's *existing* `socialReddit`/`fetchRedditSearch` server function directly (no new collector written) from `images.tsx`, flattening each returned `SocialPost`'s already-documented `media[]` field into individual `type === "image"` entries. Wired in deliberately even though Reddit OAuth isn't configured yet — `fetchRedditSearch` already throws a real `SocialUnavailableError` naming the exact `REDDIT_CLIENT_ID`/`REDDIT_CLIENT_SECRET` env vars and the Settings-page alternative, surfaced as-is rather than swallowed, so the column starts working with zero code changes the moment that setup (still in progress elsewhere in this conversation) finishes. Panel grid widened `lg:grid-cols-3` → `lg:grid-cols-3 2xl:grid-cols-5` so five independent per-source columns (News/GDELT, YouTube, Wikipedia, Openverse, Reddit) wrap two-then-three rather than crowding one row. **Verified live end to end** with the target set to "Tesla": YouTube, Wikipedia, Openverse and Reddit all rendered simultaneously in one run — Openverse showing real, distinct Flickr photographers with real CC license codes (Don McCullough · BY, harry_nl · BY-SA, jurvetson · BY, cdorobek · BY, oskay · BY), Reddit showing its full, honest credential-missing message with exact setup steps; GDELT alternated between a real success and a real 429 across repeated runs (confirming the connect-timeout fix, once restored per the entry above, is genuinely working — it now reaches GDELT's server and gets a real answer either way, never hanging or crashing), zero console errors throughout. `tsc --noEmit` clean, `bun scripts/check-exports.ts` — 151 core exports unchanged, `bun test tests/*.test.ts` 895/895, fabrication-check clean on both touched files.
- [x] **GDELT connect-timeout root-caused and fixed for real (both `/images` and `/gis`) — user-reported the retry still wasn't enough, and fixing it broke the whole app before it was fixed correctly (2026-08-19)**: Direct continuation of the entry below, same day. User pasted the exact still-failing error: `GDELT unavailable after a retry: fetch failed (cause: Connect Timeout Error (attempted address: api.gdeltproject.org:443, timeout: 10000ms))` — proving the 6s retry added earlier hadn't fixed anything, because the retry wasn't the bottleneck. Root-caused with direct `bun -e` scripts against the real host rather than guessing: plain `fetch()` applies undici's own internal TCP-connect timeout (`ConnectTimeoutError`, default 10s) *underneath* the request-level `AbortSignal.timeout` this code was already setting — a separate ceiling the signal never touched. `api.gdeltproject.org`'s handshake from this environment routinely takes 13-20s (confirmed repeatedly), so requests were being cut off mid-connect regardless of the 25s overall budget passed in. Confirmed live that a bare `{ connectTimeout: N }` property on the fetch init object is silently ignored — a first attempt at this fix shipped believing it worked, based on one inconclusive timing test, and had to be corrected once a clean back-to-back comparison showed identical failures with and without it. The one mechanism that actually works, confirmed via a genuine before/after on the same host: an explicit `undici` `Agent({ connect: { timeout: 20000 } })` passed as `dispatcher`. **Shipping the fix broke the entire app**: a first pass added `import { Agent } from "undici"` as a top-level static import in `image-sources.ts`/`geo-sources.ts`. Both files are reachable from route components (`images.tsx`, `gis.tsx`, `reports.tsx`), so the static import pulled all of `undici` — a Node-only package whose `Agent` touches `net`/`tls` internals — into the **client** bundle too. Every route crashed on load in the browser (`TypeError: Cannot read properties of undefined (reading 'node')`, inside React's error boundary) the moment the dev server was restarted to pick up the new dependency; confirmed live via screenshot ("This page didn't load") before being caught, then fixed within the same turn. Fix: load `undici` via a runtime `await import("undici")` inside a memoized async `gdeltDispatcher()` helper, never a top-level import — this exact pattern (`await import("./gps-interference")` inside `collectGpsJamming`) already existed in `geo-sources.ts` for what was almost certainly the identical reason, and should have been reached for immediately instead of rediscovering the problem live. Applied identically to both `image-sources.ts` (the new `/images` panel) and `geo-sources.ts`'s pre-existing `collectNewsGeo` (used by `/gis` and `/reports` via `fetchGeoLayers`) — the same slow host, same bug, same fix, so leaving one fixed and one broken would have been inconsistent and dishonest about what was actually resolved. Added `undici@7.29.0` as an explicit `package.json` dependency (pinned to the version already present transitively via `@distube/ytdl-core`, so no version drift) rather than relying on an unlisted transitive install. **Verified live end to end**, after cleaning up several stray dev-server processes left running across ports 3000-3003 from repeated restarts during this investigation (killed by Windows PID via `taskkill`, confirmed only one instance left listening): with the target set to "Tesla," all three `/images` columns — GDELT news, YouTube, Wikipedia — populated with real, simultaneous, distinct results in one run (real dated articles from fool.com, auto.economictimes.indiatimes.com, finance.sina.com.cn, storm.mg with real thumbnails), zero "Connect Timeout," "fetch failed," or rate-limit text anywhere on the page, zero console errors. `tsc --noEmit` clean, `bun scripts/check-exports.ts` — 151 core exports unchanged, `bun test tests/*.test.ts` 895/895, fabrication-check clean on all three touched files.
- [x] **`/images` Related Images panel: added Wikipedia as a third real source, gave GDELT a retry, split news-image logic out of `geo-sources.ts` into its own file — user-reported follow-up (2026-08-19)**: Direct continuation of the entry below, same day. User's own words: "we are getting images from youtube only, get from more sources, news. and the GDELT was failing" — reporting the news column erroring in practice (a live screenshot showed `GDELT unavailable: fetch failed (cause: Connect Timeout Error...)`) and asking for broader source coverage rather than just YouTube. Investigated before adding anything: `err.cause` — added in the previous entry specifically to stop swallowing this detail — showed the real failure was a TCP connect timeout to `api.gdeltproject.org:443`, not an application bug; repeated direct `curl` tests against the same endpoint across this session (spaced 6s, 15s, 20s, 30s, 50s+ apart) kept returning a genuine HTTP 429 with GDELT's own documented rate-limit text, meaning connectivity itself works but the endpoint is consistently saturated from this environment — a real, external condition, not something fixable in application code. **Response was two-pronged**: (1) Gave `collectNewsImages` one retry after a 6s pause before surfacing an error — matches GDELT's own documented guidance ("wait and retry") already quoted elsewhere in this codebase, and still surfaces the real error (from the *second* attempt, not a swallowed first one) if GDELT is still down after the wait; live-verified this genuinely double-attempts rather than being cosmetic. (2) Added a **third, independent source — Wikipedia** — verified live via `curl` (with and without a custom User-Agent, both 200) that `action=query&generator=search` with `prop=pageimages` returns real `upload.wikimedia.org` thumbnail URLs for a free-text query, no key, and — unlike GDELT — no rate-limiting observed across any test run this session. New `collectWikipediaImages()`/`fetchWikipediaImages` follow the identical null-honesty convention as the GDELT function: a matched page without a lead image (disambiguation pages, stubs) is silently skipped, never padded with a placeholder. **Structural cleanup alongside the new source**: `collectNewsImages`/`fetchNewsImages`/`NewsImageResult` — added to `geo-sources.ts` earlier the same day — were moved into a new `src/utils/image-sources.ts` together with the new Wikipedia function, rather than letting a second non-geo helper accumulate in a file whose own header explicitly scopes it to "Module 5 — GIS collectors." Confirmed safe before moving: both were added this same session with exactly one consumer (`images.tsx`), so relocating carried no risk to anything else — grepped first to be sure nothing else had started depending on them. `geo-sources.ts` is back to GIS-only scope; its only other consumers (`gis.tsx`, `reports.tsx`) import solely `fetchGeoLayers`, untouched. `/images`' panel grid widened from two columns to three, with a matching third `useEffect` (mount+target-sync pattern, `cancelled`-guarded, identical shape to the other two) and its own independent loading/error/empty state — GDELT being down does not block Wikipedia or YouTube from rendering, and vice versa. **Verified live end to end**: with the target set to "Cloudflare," the news column correctly showed the GDELT rate-limit message (both attempts genuinely exhausted, not swallowed) while YouTube and Wikipedia both returned real, on-topic, correctly-thumbnailed results side by side (Wikipedia: "Cloudflare," "Denial-of-service attack," "List of HTTP status codes," "Kiwi Farms," "Lavarand" — all genuinely relevant matches with real lead images) — 25 real Analyse buttons across the two working sources, zero console errors, confirmed via screenshot. `tsc --noEmit` clean, `bun scripts/check-exports.ts` — 151 core exports unchanged, `bun test tests/*.test.ts` 895/895, fabrication-check clean on all three touched/added files (`images.tsx`, `geo-sources.ts`, `image-sources.ts`).
- [x] **New feature: "Related Images — News & Open Source" panel on `/images`, real GDELT article thumbnails + real YouTube video thumbnails, in-page Analyse hand-off (2026-08-19)**: User request — "also get the list of images from the news, open source, etc" on the Image Intelligence page. Investigated three candidate real image sources before building anything, ruling two out live rather than assuming: (1) **Google News RSS** (what `/news`'s `fetchNews` already uses) — verified live via `curl` that every `<item>` in a real Google News RSS response carries neither `<enclosure>` nor `<media:thumbnail>`, despite the feed declaring the MRSS namespace at channel level; there is no per-article image anywhere in that feed. Ruled out. (2) A reverse-image/web-search scrape (Bing/DuckDuckGo image results) — rejected without building it: this project already avoids scraping search engines/social platforms with restrictive ToS (Instagram, Facebook, X — see the standing collectors note), and adding one here would be the same category of risk for one feature. (3) **GDELT DOC API's own `socialimage` field** — the article's og:image, extracted by GDELT at crawl time when it finds one — is real, already-integrated infrastructure (`geo-sources.ts`'s `collectNewsGeo` already calls this exact endpoint for `/gis`, discarding everything except `sourcecountry`) and genuinely present in GDELT's schema; confirmed working end-to-end live in the browser (not just curl, which kept hitting GDELT's real, documented 429 during manual verification — see below). New `collectNewsImages()`/`fetchNewsImages` in `geo-sources.ts`, deliberately a separate function from `collectNewsGeo` rather than extending it (different return shape, different caller, and that function's own header comment explicitly states GDELT DOC is used for "outlet country only" — a deliberate boundary, not an oversight, so this needed to be its own addition rather than a silent scope-widen of existing code). Every result requires a real, URL-parseable `http(s)` value in `socialimage` — an article without one is silently skipped, never given a placeholder, matching `toGeoPoint`'s own coordinate-honesty convention for the identical underlying reason (partial coverage is fine, a fabricated fallback is not). Second source: **YouTube thumbnails**, reusing `searchYoutubeVideos()`/`serverSearchYoutubeVideos` built for `/videos` the previous day — zero new backend work, and the real, standard `https://i.ytimg.com/vi/{videoId}/hqdefault.jpg` CDN URL needs no extra network call to construct. `/images` gained the same `getActiveTarget()` mount+`sentinel_target_changed`-listener sync used across the rest of the app, and two independent `useEffect`s (one per source, each with its own loading/error/empty state, `cancelled`-guarded) fire on target change — mirroring `/videos`' own established pattern exactly. Each result's "Analyse" button calls this page's own existing `analyse()` function directly (`setUrlDraft(url); void analyse(url, title)`) rather than navigating anywhere — an improvement on the `/videos`→`/youtube` precedent enabled by already being on the analysis page, and it reuses the exact same cross-origin-EXIF-as-failed-read handling `/images` already had for any pasted URL, so no new code path was needed for that. **A real bug surfaced during live verification, not a hypothetical**: the first live run showed the news panel failing with a bare "GDELT unavailable: fetch failed" — a generic wrapper message Bun's fetch throws for a connection-level failure, with the actual cause nested in `.cause` and previously discarded. Fixed by surfacing `err.cause?.message`/`.code` alongside the top-level message; re-running immediately showed the real cause was GDELT's own documented 429 rate limit ("it accepts one request every 5 seconds"), correctly detected and formatted by the existing rate-limit-specific branch once the real error text was visible to it. **Verified live end to end**: with the target set to "SpaceX," the YouTube panel returned 5 real, distinct results with correctly loading thumbnails, titles, channels and relative timestamps; the news panel correctly showed either real GDELT thumbnails or the honest rate-limit message depending on GDELT's live state at request time (both observed across repeated runs — never a fabricated image, never a silent empty state pretending to be "no results" when it was actually a live 429). Clicking "Analyse" on a real YouTube thumbnail ran the full existing pipeline in place (Provenance assessment + EXIF Metadata sections rendered, URL bar updated to the real thumbnail URL, corpus count incremented to reflect a real hash), zero console errors. `tsc --noEmit` clean, `bun scripts/check-exports.ts` — 151 core exports unchanged, `bun test tests/*.test.ts` 895/895, fabrication-check clean on both touched files (`images.tsx`, `geo-sources.ts`).
- [x] **`/youtube` Translate Page relocated twice more (top of page, then inline beside the title) — two rapid user follow-ups, plus a real staleness bug caught along the way (2026-08-18)**: Direct continuation of the Comments-removal/translation-widening entry below, same day. User first asked to move the newly-widened "Translate Page" card "to top of the page" — moved it from below the two-column grid (where it landed originally, and where it can be found in the `bunx tsc --noEmit`-verified state) to directly above the video player, still as its own full-width card. Immediately after, a second request — "move this translate container beside the youtube video intelligence title" — asked for a tighter placement still: not just early on the page, but inline with the page header itself. `PageHeader` (`src/components/app-shell.tsx`) already supports an `actions?: ReactNode` prop, right-aligned in the header's flex row — confirmed as the established codebase convention via existing usage in `investigations.tsx`. Replaced the standalone card entirely: the language `Select` + `Translate Page` button (with its explanatory `title` tooltip) now render via `actions={metadata ? (...) : undefined}` on `youtube.tsx`'s own `<PageHeader>` call, appearing beside "YouTube Video Intelligence" only once there's a video to translate. The translation *result* (or error) still needed somewhere to render, and since it was moving out from under the `{metadata && (...)}` block it had lived inside, it got its own small `<Card>` directly below the header — gated purely on `(translation || translationError)` rather than on `metadata`, so it persists correctly even in odd intermediate states. **That gating change surfaced a real bug while implementing it, not just a hypothetical**: `handleFetchMetadata`'s reset block already cleared `report`/`reportError` on every new video fetch, but had never cleared `translation`/`translationError` — harmless before, when the result card lived inside the `metadata`-gated block (so it was invisible during the brief metadata-null window either way), but a genuine staleness bug now that the card renders independent of that gate: a translation (or translation error) from the *previous* video would stay visible, sitting next to the new video's fresh metadata, for the entire duration of the new fetch and indefinitely after if the user didn't re-translate. Fixed by adding `setTranslation(null); setTranslationError(null);` alongside the existing report reset. **Verified live, twice over**: (1) a `boundingBox()` check confirmed the page title and the "Translate Page" button sit on the same header row (both at `y:80`); a translation attempt correctly resolved (in this case to an honest `LlmUnavailableError`, since the default target language is English and the video's title metadata was already English — `isUntranslated()` correctly refusing to fabricate a no-op "translation" rather than a bug in this change) and rendered in the new result card below the header, styled correctly, zero console errors. (2) A dedicated staleness-fix check: triggered the translation error on one video, confirmed it visible, then fetched a *different* video via the URL input and "Fetch Metadata" — the stale error card was gone immediately, confirmed both via text-search and a full screenshot (clean metadata for the new video, translate button reset to idle, no leftover red error box). `tsc --noEmit` clean, `bun scripts/check-exports.ts` — 151 core exports unchanged, `bun test tests/*.test.ts` 895/895 (see the stray-directory note below for why the glob form is used), fabrication-check clean — the only matches on `youtube.tsx`/`llm.ts`/`youtube-collector.ts` are pre-existing, on untouched lines, and are either allowed honest-absence markers (`"Unknown"`, `"Untitled Video"`) or allowed loop-accumulator/sort-comparator `?? 0` uses, not new fabrication.
- [x] **`/youtube` Comments removed, translation widened from transcript-only to whole-page (2026-08-18)**: Two user requests together. (1) Removed the Comments card entirely — state (`comments`/`commentsError`/`fetchingComments`), `handleFetchComments`, its reset calls in `handleFetchMetadata`, and the JSX — from `youtube.tsx`. Deliberately left `youtube-collector.ts`'s underlying `fetchYoutubeComments`/`serverFetchYoutubeComments`/`YoutubeCommentsResponse` untouched: they're exported, tracked, tested infrastructure, and the request ("remove this," pointing at the on-page panel) read as removing the UI, not deleting shared backend capability nobody asked to delete — matching this project's own precedent of a capability existing without being wired to every possible surface (`ai-service/`'s "merged, not yet wired up" status is the same shape of decision). (2) The translate feature — until now scoped to `subtitles.segments` only, requiring a transcript to be loaded first — now builds its input from title + description + transcript (whichever of the three actually exist), and is available as soon as metadata loads rather than gated on a transcript being present. Moved out of the Subtitles & Transcripts card into its own full-width "Translate Page" card, positioned with the Language selector and Translate button directly in its header rather than a paragraph below (more compact than the transcript-only version's layout, and avoids re-introducing the same below-the-fold discoverability problem fixed earlier the same day). `llmTranslateTranscript`/`translateTranscriptOf` in `llm.ts` needed no changes — already a generic "translate this text" function, not transcript-specific despite the name; only the caller's input-assembly changed. The Report card's own description text already dynamically named "its AI translation" as an input when one exists — built that way earlier the same day without knowing translation's scope would later widen, and it kept being correct without any edit. **Verified live**: Comments confirmed absent from the rendered page; Translate Page confirmed working with metadata-only input (no transcript loaded) — a real translation call returned, correctly attributed to `openai/gpt-oss-120b`, with updated disclosure text ("machine-translated to English (English)" instead of the old transcript-specific wording); full-page screenshot confirms the new card order (Subtitles → Translate Page → AI Intelligence Report) with no leftover Comments card. `tsc --noEmit` clean, `bun test` 895/895, fabrication-check clean, 151 exports unchanged (the removed functions were never in the tracked registry to begin with).
- [x] **New feature: AI Intelligence Report on `/youtube`, synthesizing all real collected data into one brief (2026-08-18)**: User request — a report/summary drawing on description, transcript, title, channel, upload date, "with metadata which we are getting only" (their own words ruling out inventing anything beyond what's actually collected). Reused `reportOf()`/`llmReport` from `llm.ts` rather than writing a new prompt — it already exists precisely for this ("Use ONLY the collected data below. Do not introduce facts, figures or attributions that are absent from it. Where a section cannot be supported, write 'No supporting data collected.'"), is the same function `/reports` and `agents.tsx`'s `INTEL_REPORT` capability already use, and its honesty guarantees didn't need re-deriving. New "AI Intelligence Report" card, full-width below both metadata and transcript columns (required wrapping the existing two-column grid and the new card in a Fragment — a real JSX syntax error caught immediately by `tsc`, not shipped). `handleGenerateReport()` builds the `data` string from every real field currently on screen — title, channel, upload date (or "not reported" — CLAUDE.md's own named-acceptable absence marker — when InnerTube's WEB microformat call didn't return one), duration, view count, URL, description, and, only when actually loaded, the transcript and its AI translation (each independently capped at 6,000 characters with the cut disclosed, matching the translation feature's own existing convention, since a full transcript could otherwise make the combined prompt very large). Available as soon as metadata loads; transcript and translation are optional enrichments, not requirements — the description text and the "Load a transcript above first for a fuller report" hint make the current input scope explicit before generating, so nothing about what's actually feeding the report is a surprise after the fact. Report renders in its own "AI-GENERATED · {model} — Not verified" block, matching the disclosure convention already established by the Executive Briefing and translation features on this same page. Reset alongside the rest of the page's state on every new `Fetch Metadata` call — a report about the previous video must not sit next to a new one's metadata. **Verified live** with the same video used throughout this session's `/youtube` work (transcript loaded first, so the report had more than bare metadata to draw on): a real, structured, correctly-cited report was produced, opening "**Target:** Saurav Das & Ashutosh Epic Roast Sangit Ragi... **Source:** The Extra Fun (Channel) **Date of Report:** 2026-08-14," attributed to `sarvam-105b`. Screenshot confirmed it directly after an automated text-search assertion falsely suggested nothing had rendered — same root cause as an earlier false alarm this session: Playwright's `innerText()` reflects `text-transform: uppercase` from the Badge's own CSS, so a lowercase search string for "AI-generated" never matches the rendered "AI-GENERATED"; the app was correct, the assertion wasn't. `tsc --noEmit` clean, `bun test` 895/895, fabrication-check clean, 151 exports unchanged.
- [x] **Sarvam silently returns the source text instead of translating it — user-reported ("not translating"), root-caused and fixed with a task-specific correctness check llm.ts had no equivalent of before (2026-08-18)**: Third round on the same feature in one day. After the discoverability fix (below), the user reported the panel labeled "AI TRANSLATION · SARVAM-105B" but the text was still Hindi. Reproduced directly against the live Sarvam API with the exact transcript excerpt from the screenshot, at the production `max_tokens: 4000`: `finish_reason: "stop"` (not truncated), HTTP 200, and `message.content` came back as the **source Hindi text verbatim** — while `message.reasoning_content` (Sarvam's own hidden chain-of-thought, billed against the same token budget per this file's existing documentation) showed the model *had* produced the correct English translation internally and ended its reasoning with "This seems correct and follows all instructions" — then simply never wrote that answer into the response field. A real, reproducible model behavior, not a prompt-wording problem: nothing in the transport layer (status code, finish_reason, non-empty content) flags this as a failure. First fix attempt used an exact-string echo check; live-retesting the fix immediately caught a *second* failure shape the exact check missed — Sarvam returned a same-language **paraphrase** ("चित्र चालू करो, कृपया..." vs the source's "पिक्चर चालू करो बे...") — reworded but still fully in Hindi, still not a translation. Replaced with a script-based check: `\p{Script=Latin}` letter-ratio of the response, compared against whether the target language is Latin-script (true only for English among the 16 languages this feature offers — the other 15 are each in their own native non-Latin script per `src/i18n/languages.ts`, so "not predominantly Latin" and "expected non-Latin" collapse to one same-direction check either way) — catches both the exact-echo and the paraphrase-in-source-language cases, since neither one is majority-Latin when translating into English. `llm.ts`'s shared `chat()` gained an `excludeProviders` option (filters the provider chain before building the cache key, so a retry against the other provider gets its own cache entry rather than colliding with the failed attempt's) so `translateTranscriptOf` can force a retry against whichever provider *didn't* just fail this check, rather than hoping a same-provider retry behaves differently — and throws an explicit `LlmUnavailableError` naming both attempts if the retry fails the same check too, rather than ever returning a same-script "translation." **Verified live end to end, twice** — once confirming the exact-match version still failed on the paraphrase case (a genuine negative result, not skipped), then again after the script-based rewrite: the retry correctly detected Sarvam's Hindi paraphrase, failed over to Groq, and returned a real, accurate English translation ("Turn on the picture, man... Saurav Das, how do you react to it?..."), correctly attributed to `openai/gpt-oss-120b`. `tsc --noEmit` clean, `bun test` 895/895, fabrication-check clean on both touched files.
- [x] **Translate-transcript control was scrolled out of view on a normal-height window — user-reported follow-up, fixed (2026-08-18)**: Immediately after shipping the translation feature below, the user reported "still im getting hindi only." Live-tested twice at a tall (2000px) viewport and got a correct English translation both times, so the feature itself wasn't broken — the difference was viewport height. The control had been placed *after* the full transcript segments list in the JSX (a sibling `<div>` following it, not nested inside its own scroll region), and that list runs to hundreds of entries — 289 for the video in question. On a realistic laptop-height window (verified live at 1536×794, closer to a typical browser than the tall window used for the first round of testing) the "Translate transcript" control sat below the fold entirely; a user would have to scroll well past the transcript to find it, and evidently didn't. Moved the whole translate block (selector, button, error state, result) to appear *before* the segments list instead of after — confirmed via `boundingBox()` at 1536×794 that it's now within the visible viewport with zero scrolling. Reworded the result panel's own "translated ... from the transcript above" to "from the loaded transcript," since "above" stopped being accurate once the block moved. `tsc --noEmit` clean, `bun test` 895/895, fabrication-check unaffected (the one match on this file is pre-existing, on an untouched line).
- [x] **New feature: AI transcript translation on `/youtube` — user-reported gap (2026-08-18)**: User loaded a video whose only real caption track is Hindi (auto-generated) and asked for "more options." Verified live against YouTube's own InnerTube player endpoint for this exact video before assuming anything: YouTube genuinely reports only one caption track for it — not a bug in the earlier manual/auto caption-collision fix, nothing to surface that isn't already surfaced. The honest way to give more options is translating the one real transcript that exists, not inventing additional caption-track entries — mixing an AI translation into the same dropdown as genuine YouTube-sourced tracks would misrepresent its provenance even though the translation itself is real output from a real model call. New `translateTranscriptOf()`/`llmTranslateTranscript` in `llm.ts`, with its own dedicated system prompt (`TRANSLATE_SYSTEM`) rather than reusing `NARRATIVE_SYSTEM` — a translator's failure mode (summarising, omitting, editorialising) is different from an analyst's ("never invent figures/dates/names"), so it needed its own instruction. Input capped at 6,000 characters with the truncation disclosed to the caller rather than risking a truncated *output* misrepresenting itself as complete — the same reasoning-model max_tokens risk this file's own docs already name for other calls. `/youtube` gained a `translateTarget`/`translation`/`translating`/`translationError` state cluster and a "Translate transcript" control (reusing `LANGUAGES` from `src/i18n/languages.ts` — the same 16-language list, English + 15 scheduled Indian languages, the interface's own switcher already offers, rather than inventing a second list) shown only once a real transcript is loaded, with any prior translation cleared whenever a new transcript is fetched so stale AI output can never sit next to a transcript it wasn't translated from. The result renders in its own visually distinct block with an explicit "AI TRANSLATION · {model}" badge and "Not an official YouTube caption... verify before treating as evidence" — never inside the Subtitles & Transcripts list itself. **Verified live** with the user's own video (`CFbNhG13_eA`, Hindi auto-captions, 289 segments, 11m17s — long enough to genuinely exercise the 6,000-char truncation path): translated to English, correctly attributed to `openai/gpt-oss-120b` (Sarvam still in its overload window from earlier this session; the fallback carried it cleanly), real accurate translation ("Turn on the picture, man... Saurav Das? My opinion, Sahil, is that lies are being told..."), truncation notice correctly shown. One dead-end during verification worth recording: the live test's own text-matching assertion falsely suggested the feature was silently failing (no result appearing after 60s of polling); a temporary console.log placed directly in the handler proved the translation completed correctly in under 500ms (a cache hit) and `setTranslation` fired — the bug was in the test script's own stale/mistimed page snapshot, not the app; a screenshot settled it definitively and the diagnostic logging was removed before finishing. `tsc --noEmit` clean, `bun test` 895/895 (`tests/*.test.ts`), fabrication-check clean on both touched files.
- [x] **New feature: "Related YouTube Videos" panel on `/videos`, real discovery-to-analysis hand-off (2026-08-18)**: User request — search for a name/entity and see related YouTube videos with an Analyze button, on the Video Intelligence page. `/videos` previously only supported uploading a local file for the client-side keyframe/OCR pipeline; it had no connection to the global active-target search and no video-discovery step. **Went through two real implementation attempts before landing on a working one, both worth recording**: (1) First built the search on `site:youtube.com`-restricted Google News RSS, reusing `rss-source.ts`'s `publisherUrlsFromRss`/`resolvePublisherUrl` (already used by `fetchNews` to recover a real publisher URL from Google's `<source url>` attribute, since `<link>` is always a `news.google.com/rss/articles/...` redirect). Live-tested end to end and the Analyze hand-off broke: `<source url>` for a YouTube result is just the bare `"https://www.youtube.com"` domain — enough to rate a publisher (all that mechanism was ever built for) but not enough to identify a specific video — and Google's redirect resolves client-side via JavaScript, not a server-side 3xx a plain `fetch` can follow (confirmed live: following it just round-trips to another `news.google.com` URL, no plain-text destination in the response body either). (2) Replaced it with real YouTube search via InnerTube — the same reverse-engineered, keyless endpoint (`POST youtubei/v1/search`) youtube.com's own search page calls internally, verified live with a plain `curl` (HTTP 200, real 523KB result page, `videoRenderer` entries carrying a real `videoId`, title, channel, relative publish time, and duration) before writing any code — no API key, and not the same credential as `YOUTUBE_API_KEY` (that's the official Data API v3, only needed for comments). New `searchYoutubeVideos()`/`serverSearchYoutubeVideos` in `youtube-collector.ts` (the correct home for InnerTube functionality, not `news.tsx`), building each result's URL directly from the real `videoId` rather than resolving anything. `fetchMediaIntelligence` (`news.tsx`, still otherwise unwired — see the 2026-08-14 entry below) had the *identical* Google-redirect bug in its own video section since it was written; fixed in passing to call the same new function, for one correct implementation instead of two. **`/youtube` gained a `?url=` hand-off route**, copying `/images`' own already-proven `?url=` pattern exactly: a `validateSearch` accepting only absolute http(s) URLs, and a `Route.useSearch().url` + ref-guarded `useEffect` that runs once per distinct URL. `handleFetchMetadata` gained an optional `urlOverride` param (matching this session's earlier `handleFetchSubtitles` convention) so the hand-off doesn't have to round-trip through React state before firing — and its one existing `onClick={handleFetchMetadata}` call site had to be wrapped as `onClick={() => handleFetchMetadata()}`, since passing the handler directly would have handed the click's MouseEvent to the new optional string parameter. `/videos` itself gained the global-target sync effect it never had (mount + `sentinel_target_changed` listener, matching the pattern already applied across 8 other routes this session), and the new panel's own fetch is guarded against firing on the placeholder empty target. **Verified live end to end**: searched "SpaceX" on `/videos`, got a real, relevant result list (Bloomberg Tech, NASASpaceflight, SpaceX's own channel, relative dates, durations) with a screenshot confirming the rendered UI; clicked Analyze on the top result; landed on `/youtube` with the real URL in the address bar (`youtube.com/watch?v=OnLt7r0ajtU`); metadata loaded automatically with zero further clicks — real title ("The World's Richest Families Bet on SpaceX"), channel ("Bloomberg Tech"), upload date, duration ("2m 43s") and view count ("5,714"), matching the search result exactly. Zero console errors across the whole flow. `tsc --noEmit` clean, `bun test` 895/895 (`tests/*.test.ts` — see the stray-directory note above), fabrication-check clean on all four touched files.
- [x] **Executive AI Briefing cited sources without explaining them, and defaulted to "insufficient information" on a genuinely multi-entity name instead of disambiguating — user-reported, fixed (2026-08-18)**: Direct follow-up to the same-day fix grounding the briefing in real collected news (below) — once real articles were flowing in, a new quality problem surfaced: for "Sourav Das" (a name shared by at least four real, unrelated people in the collected corpus — an actor, a CJP-affiliated political figure, a footballer, a heritage-site advocate), the model recited isolated facts with outlet/date citations but declared Strategic Profile and Risk Assessment "insufficient collected information," even though the 35 collected articles contained plenty to work with — it just didn't cohere into ONE identity, and the prompt only ever told the model what to do when evidence was *absent*, never when it was present but described multiple entities. `executiveBriefOf()`'s prompt (`src/utils/llm.ts`, shared by the homepage and both of `agents.tsx`'s Executive Brief capabilities) gained two additions: an explicit instruction to synthesize and explain collected facts rather than merely cite their source, and an explicit instruction that a shared name is not one identity — when the material describes what look like multiple distinct people/organizations, characterize each one separately instead of declaring the whole brief unsupported. The existing "don't speculate beyond the evidence" guardrail is unchanged and now runs last, after the synthesis/disambiguation instructions rather than instead of them. **Verified live** with the identical "Sourav Das" target: the brief now returns a genuinely disambiguated table — four distinct real people, each with its own sourced facts and dates (ETV Bharat/Times Now/NDTV/Transfermarkt/indulgexpress.com, specific July–August 2026 dates) — instead of a blanket insufficiency disclaimer. This same live run also incidentally confirmed the Sarvam→Groq fallback added earlier today works smoothly under real load: Sarvam was still returning its overload condition, and the brief came back correctly attributed to `openai/gpt-oss-120b`. `tsc --noEmit` clean, `bun test` 895/895 (via `tests/*.test.ts` — see the stray-directory note below for why the bare form is currently avoided), fabrication-check clean on `llm.ts`.
- [x] **`/news` quoted-phrase/boolean search returned zero results despite Google News finding real coverage — user-reported, fixed (2026-08-18)**: User searched `sourav das + "cjp" + "Delhi"` and got "No active news headlines found," despite this exact query returning 49 real, on-topic articles from Google News RSS directly (verified via `curl`). Root-caused through several ruled-out theories before landing on the real one — worth recording since two of them looked highly plausible and cost real investigation time: (1) suspected `parseQuery()`/`buildUpstreamQuery()` mishandled `+`; ruled out — `buildUpstreamQuery()` correctly reconstructs `"cjp" "delhi" sourav das` and Google returns real results for it directly. (2) Suspected a GET-vs-POST `createServerFn` serialization bug after a client-side capture appeared to show a literal `"` corrupted into `\"` when passed through `fetchAiBrief`'s `fetchNews` call; switched `fetchNews` to POST, which did NOT fix the reported scenario (a direct `/news?q=...` diagnostic — a temporary `throw new Error(JSON.stringify(q))` inside the handler, since reverted — proved the loader receives the query **perfectly uncorrupted** regardless of GET or POST, so this was a false lead; the POST change was reverted, restoring the original GET method and comment). **Actual root cause**: `fetchNews`'s handler re-filters Google's own already-relevant results through this app's local `matchesQuery()` (`src/utils/search.ts`), requiring every parsed term/phrase to appear as a literal, whole-word match in the article text. The real person's name is spelled "Saurav Das," not "sourav" as the user typed — Google's own fuzzy/semantic search correctly found real coverage anyway (transliteration variants are exactly what search engines handle), but the strict local re-check required the literal string "sourav" to appear somewhere and rejected all 49 genuinely relevant articles. Since `matchesQuery()` already short-circuits to `true` whenever `parsedQuery.isEmpty` (which is exactly when the code takes the *other* branch — the no-query default multi-feed browse, BBC/Guardian/etc.), the local re-check was **only ever active on the branch where Google already searched**, i.e. it could only ever hurt, never help, in current usage. Fixed by skipping the local rejection when `upstreamQuery` was actually sent to Google (`usedUpstreamSearch` flag) — trusting Google's own relevance matching for that branch, consistent with the code's own pre-existing comment ("Google already ranked these for the query") which had already loosened this check once before for an analogous reason (a prior fix from earlier project history: exact-substring matching used to discard "Tesla cuts 10% of workforce" for a "Tesla layoffs" query). `scoreMatch()`'s ranking (sorting, not rejecting) is untouched — a low-scoring result still shows, just further down. **Verified live**: the exact reported query now returns "35 articles collected," leading with a real, on-topic headline ("CJP Leaders Sourav Das, Ashutosh Ranka Meet JP Nadda Over NEET Row"), zero console errors. `tsc --noEmit` clean. `bun test` — needed `bun test tests/*.test.ts` instead of a bare `bun test`/`bun test tests/` after Bun's directory-scanning began segfaulting on an unrelated stray directory that appeared in the repo root mid-session (see the note below); with that scoping, still 895/895 passing, confirming the fix introduced no regression.
- [x] **⚠️ Unexplained stray directory discovered in the repo root, not created by any Claude session action (2026-08-18)**: `social-media-ai-agents--main/worldmonitor-main/worldmonitor-main/` appeared in the project root with a `14:45` timestamp mid-session — a **complete separate project** (its own `.env`, `.gitignore`, `package.json` named `tanstack_start_ts`, `bun.lock`, `AGENTS.md`, `README.md`) sitting in an **unresolved git merge conflict** (`<<<<<<< HEAD` literally the first line of its `README.md`). Its own `AGENTS.md` identifies it as Lovable-connected — consistent with an old, pre-rebrand snapshot of what became this project (see `CLAUDE.md`'s "Lovable tooling... removed" note), most likely a zip/archive extracted into the wrong location by the user in a separate window, given the doubled `worldmonitor-main/worldmonitor-main` nesting. **Not touched, not deleted, not investigated beyond reading its top-level README/package.json/AGENTS.md read-only** — flagged to the user rather than acted on, per this project's own safety norms around unfamiliar directories. Confirmed untracked (`git status`: `?? social-media-ai-agents--main/`) and not gitignored, so it will NOT be silently committed by an ordinary `git add` of tracked paths, but WOULD be swept in by `git add -A`/`git add .` — worth remembering before any future broad `git add`. **Practical side effect**: its nested `tests/*.test.mjs` files (using Node's `node:test`, not `bun:test`) make a bare `bun test` or `bun test tests/` segfault Bun's directory scanner. Until this directory is resolved, verify with `bun test tests/*.test.ts` (shell-expanded glob, which bypasses Bun's own recursive walk) instead of the bare form.
- [x] **Homepage Executive AI Briefing was never grounded in real collected news — user-reported, fixed (2026-08-18)**: `index.tsx`'s `fetchAiBrief()` sent `executiveBriefOf()` a "context" that was just the instruction echoed back — `` `Target acquisition search initiated for ${target}. Provide an automated threat briefing summarizing risk profile, digital footprint, and strategic containment guidance.` `` — zero real facts about the target, despite `/sources`, `/trends` and `/sentiment` all collecting real news for the identical target via `fetchNews()` one click away. `NARRATIVE_SYSTEM`'s "use only the information supplied" instruction was doing exactly that — the information supplied just never included anything real, leaving the "Strategic Profile"/"Known Capabilities" sections to come from the model's own training knowledge dressed as a briefing, or its own honest disclaimer, with nothing forcing the honest path. `agents.tsx` has two more `llmExecutiveBrief` call sites (case-based, not search-based — its own `context` is the analyst-written case description when one exists, which is legitimate real content) not touched here; the user's report was specifically about the search-triggered homepage briefing. **Fix**: `fetchAiBrief()` now calls `fetchNews({ data: { query: targetStr } })` first and builds the context from the real returned articles (source, date, title, snippet — up to 8). Matching `reports.ts`'s `generateProduct()`'s own established discipline ("a product written without source material would be the model's invention, not intelligence"), zero collected articles now short-circuits to an honest `briefError` *before* any LLM call, rather than sending the model an empty context and hoping it declines to speculate. **Verified live**, not just `tsc`/`bun test` (both passed, both blind to this bug class): with "cloudflare.com" — rate-limited into returning zero articles by this session's own dozens of repeated queries against it, confirmed via `/sources` independently showing the identical "No articles ingested" for the same target right now — the briefing correctly refused rather than fabricating. With a fresh target ("SpaceX", confirmed 35 real articles via `/sources`), the briefing came back grounded in specific real facts traceable to the actual collected headlines (a named Falcon 9 incident, a named Starlink policy reversal in Poland, AI's real reported impact on quarterly profitability) rather than generic pre-trained knowledge about SpaceX, and correctly flagged "Insufficient collected information" for the one section the real data didn't support. Zero console errors either run. `tsc --noEmit` clean, `bun test` 895/895, fabrication-check clean on `index.tsx`.

- [x] **Nav-bar target search not syncing on 7 of 11 pages while already mounted — user-reported, fixed (2026-08-18)**: The two QA-sweep entries below found and fixed the SSR hydration mismatch across all 11 `getActiveTarget()`-consuming routes, but explicitly punted on a separate, adjacent issue noticed at the time: only 4 of those 11 (`index.tsx`, `live.tsx`, `entities.tsx`, `osint.tsx`) actually listen for the `sentinel_target_changed` event `setActiveTarget()` dispatches — the other 7 (`gis.tsx`, `sentiment.tsx`, `trends.tsx`, `reports.tsx`, `recon.tsx`, `threats.tsx`, `sources.tsx`) only ever read the target once, in a mount effect. Changing the target via the top-nav search bar while already on one of those 7 pages did nothing — the page kept showing/fetching for whatever target was active when it first mounted, until navigating away and back. The user hit this independently and reported it. Fixed by adding a `window.addEventListener("sentinel_target_changed", ...)` listener to each of the 7, matching the pattern already correct in the other 4. `sources.tsx` needed a slightly larger change than the other 6 — it never held the target in state at all, calling `getActiveTarget()` directly inline inside a mount-only (`[]`-dependency) fetch effect, so it needed a new `target` state variable plus its fetch effect's dependency array changed from `[]` to `[target]` (matching how the other 6 already fetch on target change) with an empty-placeholder guard, rather than just adding a listener next to existing state setters. **Verified live**, not just `tsc`/`bun test` (both passed, both unchanged by this fix, both blind to this whole bug class exactly as noted in the sibling entries below): a Playwright script set an initial target, navigated to each of the 7 pages fresh, then changed the target via the top nav *without* navigating away, and confirmed the new target actually appeared on the page — true on all 7, where it was false before the fix. A second pass repeated the same interaction watching for console/page errors: zero on all 7. `tsc --noEmit` clean, `bun test` 895/895, 151 exports unchanged, fabrication-check clean on all 7 touched files.

- [x] **Deep interactive QA pass on the ~18 non-OSINT-framework routes; five real bugs found and fixed (2026-08-17)**: Continuation of the same-day full-app QA sweep below — a background agent drove every remaining page's PRIMARY feature (not just page load) in real Chrome: file upload on `/images`, chat/query on `/agents`, create/persist/reload on `/crawlers`/`/investigations`/`/watchlists`/`/tasks`, credential lifecycle on `/settings`, all four export formats on `/exports`, every `/graph` control, live collector re-probes on `/crawlers`, real Bluesky profile/CIB lookups on `/network`. Confirmed every previously-known issue already fixed (vault Download button, `/graph`'s 8 dead controls, the notification bell) and confirmed every LLM-gated feature fails honestly with no `.env` configured in this environment. **Five real bugs found, all fixed and verified**:
  1. **`/news` — fabricated "just now" timestamps mis-ranking real articles as the freshest content.** `safeIsoDate()` returned `new Date().toISOString()` whenever a feed's pubDate failed to parse — exact bug-class 1 from this file's own hard constraints section, but phrased as `if (...) return new Date().toISOString()` rather than a `||`/`??` chain, so the standing grep check never caught it. CrisisWatch's feed (`"Thursday, July 30, 2026 - 15:47"` — a format `new Date()` cannot parse) hit this on 10 of 35 default-load articles, stamping weeks-old items with fetch-time and floating them to the top of a recency-sorted feed labeled "just now," screenshotted live. Fixed by having `safeIsoDate()` return `null` (never a fabricated "now") and widening the local (unexported, file-scoped) `APIStory.pubDate` to `string | null` — confirmed the blast radius stayed contained to `news.tsx` itself, since every external consumer receives this data as untyped JSON over the wire and does its own ad-hoc remapping, not a statically-typed import of this interface. Sort comparators get `?? 0` (epoch) so an undated item sinks to the bottom instead of floating to the top — a loop/sort-accumulator use, not a rendered value, so it's within the hard constraints' own carve-out. `formatRelativeTime()` widened to accept `null` (matching `live.tsx`'s own sibling implementation, which already had this signature). Found and fixed the same fabrication pattern duplicated three more times in the same file (`item.pubDate ? new Date(item.pubDate).toISOString() : new Date().toISOString()`, none using `||`/`??` either) — two inside `fetchMediaIntelligence`, confirmed to have zero call sites anywhere in the app (dead code, fixed anyway since it's zero-risk and prevents the bug resurfacing the moment someone wires up a UI to it), one inside the already-dead-per-the-agent's-own-finding `fetchSearchIntelligence`. Also fixed a sibling fabrication in the same dead `fetchMediaIntelligence` function: `size: "1.2 MB"` hardcoded on every discovered video, while the two neighboring image/document branches in the identical function already correctly report `size: null` with a comment explaining why — an inconsistent half-fix, now consistent.
  2. **`/subjects` — unscored watchlist's Risk Index rendered as a bare `/100`, and a wider find**: `{activeWatchlist.riskScore}/100` had no null check, so a freshly created watchlist (whose `riskScore` is `null` by design, "never randomised" per `watchlist-store.ts`) showed a blank space before `/100` — a regression relative to `/watchlists`' own sibling handling of the identical value ("not scored — no matches have been evaluated"), now matched exactly. **While tracing this, found the page's "Growth Rate" stat and "Activity Scanner Pulse" area chart were both explicitly-commented mock data** — `// Generate chart mock trend points based on match density`, formulas like `baseVal * 0.4 + ((idx * 2) % 5)` with no real activity behind any point, and a "Total Scans" series naming a capability ("active scanning") this app does not have anywhere in its architecture (matches come from static feed snapshots). Real match dates exist (`WatchlistMatch.date: string | null`, itself already fixed from the identical fabrication pattern in an earlier session) but are too sparse/frequently-null to honestly support a real hourly chart, so rather than build a lesser real feature under QA-pass time pressure, removed both the fabricated tile and chart in favor of an explicit disclosure card ("No activity trend is computed for this filter..."), matching this app's own established pattern of stating absence rather than inventing a lesser substitute (e.g., the GIS module's several "not wired, declared and empty" layers).
  3. **`/vault` — evidence-ID collision reused a seeded record's ID.** `` `EVID-0${400 + list.length + 1}` `` derived the new id from the array's current length, not from ids actually in use; the three seeded records are non-sequential (0402, 0405, 0391), so the *second* item ever added in a session collided with the seeded 0405 record — a real React duplicate-key console error, and two visually distinct cards sharing one id (selection/export/case-linking could no longer reliably tell them apart). Fixed by switching to `localId("EVID")`, this project's own existing collision-resistant id generator (`src/utils/local-id.ts` — no `Math.random()`, `crypto.randomUUID()` where available) already used the same way by `investigations-store.ts` and `watchlist-store.ts`; reused rather than patched the buggy length-based scheme with another bespoke one.
  4. **`/youtube` — duplicate-key collision and an unreachable auto-generated track when a video has both manual and auto-generated captions in the same language.** `available_subtitles` keyed `<SelectItem key={s.code}>` on language code alone; when a video has both a manual and an ASR track for the same code, both share it — a real React duplicate-key error, a garbled concatenated Select trigger, and (the functional half of the bug) `tracks.find(t => t.languageCode === lang)` in the download path always resolving to whichever track YouTube listed first (in practice, always the manual one), making the auto-generated track **permanently unreachable** regardless of which dropdown option was clicked. Fixed at both ends: `youtube-collector.ts` gained a `preferAuto?: boolean` parameter threaded through `_getSubtitles`/`serverFetchYoutubeSubtitles` and a new `findTrack()` helper that, when `preferAuto` is given, requires an exact `isAuto` match at every fallback step (never silently substitutes the wrong variant); `youtube.tsx` encodes a composite Select key/value (`` `${code}::${isAuto ? "auto" : "manual"}` ``) so each track is uniquely selectable, tracked via a new `selectedIsAuto` state alongside the existing `selectedLang`.
  5. **`/agents` — "Pin Briefing to Case" silently did nothing with no case selected.** `handlePinReport` early-returned on `!selectedCaseId` with zero user feedback, unlike every other action path on the page — and "no case" (`"-- Global Query Context --"`) is a normal, common, deliberately-selectable state here, not just an oversight state, so this was reachable on every fresh session. Fixed by disabling the button on the same condition (`disabled={!outputResult || !selectedCaseId}`, reusing the Button component's own existing disabled styling rather than adding redundant classes) with a `title` explaining why, so the impossibility is visible before the click rather than silent after it.

  **Verified**: `tsc --noEmit` clean, `bun test` 895/895 (unchanged — none of these are unit-testable without a browser or would have needed new fixture data disproportionate to the fix), `bun scripts/check-exports.ts` 151 symbols, and this session's three fabrication-pattern greps re-run against all six touched files — every match is either pre-existing (not on a line this pass touched) or a loop/sort-accumulator use already carved out by the hard constraints themselves. Lint shows only pre-existing `no-explicit-any`/CRLF noise on the touched files, zero new findings.

- [x] **Full-app QA sweep across every route with three target types; found and fixed a systemic SSR hydration-mismatch bug affecting 9 routes, plus one more pre-existing `node:fs`/`node:path`-in-client-bundle regression (2026-08-17)**: User asked for every module/page/feature to be tested with example targets of different types (person, business/company, domain) and any real bugs fixed. Built two Playwright/Chrome scripts (`.qa-sweep.mjs`: one pass over all 30 routes; `.qa-target-sweep.mjs`: sets the target via the top-nav search bar, then sweeps the 11 routes that read the shared active-target — `/`, `/entities`, `/gis`, `/live`, `/osint`, `/recon`, `/reports`, `/sentiment`, `/sources`, `/threats`, `/trends`). **Bug 1, found first**: `/sources` crashed to a full error boundary. Two-layer regression in `credibility-profiles.ts`, the same client-bundle-leak shape as the `bun:sqlite` regression below but never live-browser-tested before now — a static top-level `node:fs/promises` import, and after fixing that, a static top-level `node:path` import (`node:path` gets the identical "externalized for browser compatibility" throw), both leaking into the browser bundle because `sources.tsx` imports this module for its `createServerFn` exports. Fixed by moving to a dynamic `await import("node:fs/promises")` inside the two functions that need it, and replacing `path.join(process.cwd(), ...)` with plain relative string literals — both changes matching `credential-vault.ts`'s own already-proven-working `VAULT_PATH` convention exactly. Grepped the whole `src/` tree afterward for any other static `node:*`/`bun:*` top-level import or bare `process.cwd()` use; none found. **Bug 2, found running the person-target sweep** (`"Sundar Pichai"`): 7 of 11 routes threw a React "Hydration failed because the server rendered text didn't match the client" error — self-healing (React regenerates the tree) but a real bug, not cosmetic. Root cause: `active-target.ts`'s `getActiveTarget()` returns the hardcoded literal `"google.com"` when `typeof window === "undefined"` (SSR has no `localStorage`), but the real stored value client-side — so every route that read it via a synchronous `useState(() => getActiveTarget())` initializer rendered one string on the server and a different one on the client's first paint, the instant the analyst's stored target was anything other than the literal default (i.e., almost always, since setting a target is the app's core interaction). `index.tsx` already had the correct fix in place for its own two target-derived state variables — a fixed literal state default, populated with the real value inside a mount-time `useEffect` — which is what proved the pattern before extending it. Applied the same pattern to the other 8 affected files (`live.tsx`, `entities.tsx`, `sentiment.tsx`, `gis.tsx`, `trends.tsx`, `osint.tsx`, `reports.tsx`, `recon.tsx`) plus `threats.tsx` (a ninth file, worse: it called `getActiveTarget()` directly in the render body every render, no state at all, an even more direct mismatch). Six of these files (`sentiment.tsx`, `gis.tsx`, `trends.tsx`, `reports.tsx`, `entities.tsx`, `osint.tsx`) also had a `useEffect` that fetches from an external API keyed on the now-briefly-empty target state — left unguarded, the placeholder-to-real transition would have fired every one of those fetches TWICE per page load, the first time with an empty/garbage query, wasting calls against APIs this project explicitly documents as rate-limited (GDELT: 1 req/5s; `osint.tsx`'s own code comment already warns "every keystroke fired five upstream requests, which exhausted GitHub's 60/hour limit within about a minute" from an earlier, unrelated fix to the same file). Guarded each of those effects with an early `if (!target) return;`, matching the guard convention `live.tsx` already used in its polling effect. **Verified three ways**: `tsc --noEmit` clean, `bun test` 895/895 passing (unchanged — this bug class is invisible to both, which is exactly why it went undetected; verification is live-browser-only, the same lesson the `bun:sqlite` regression below already taught this project), and three full live target-sweeps — person (`"Sundar Pichai"`), company (`"OpenAI"`), domain (`"github.com"`) — each showing 0 console errors and 0 page errors across all 11 routes, plus a targeted check confirming `/threats` and `/recon` (the two most-rewritten files) actually render the real target text post-hydration rather than getting stuck on the empty placeholder. Also re-confirmed, while investigating, that three previously-audit-flagged dead-handler bugs (`/vault`'s Download button, `/graph`'s eight dead controls, the notification bell's fake permanent unread dot — all named in `CLAUDE.md`'s 2026-08-12 fabrication-audit section) are already fixed from earlier work: `/vault`'s button now honestly exports the record as JSON with a real handler and an explanatory comment, every `/graph` `onClick` calls a real function, and `/alerts` now explicitly discloses the bell is inert with no unread indicator. `scripts/check-exports.ts` passes (151 symbols), lint on the touched files shows only pre-existing CRLF/prettier noise (confirmed present identically on `index.tsx`, untouched this session — a repo-wide line-ending convention mismatch, not a regression). Remaining work from the same user request, not yet done: deeper interactive (not just page-load) feature testing of the ~18 non-OSINT-framework routes, and the final written QA report.

- [x] **Security hardening, phase 2 — the limiter and sanitiser are actually wired (2026-08-17)**:
  Phase 0–1 left two complete, pure, fully unit-tested modules that **nothing called**.
  `rate-limit.ts` was imported only by `rate-limit-tiers.ts` (type-only) and its own test — no
  inbound request was throttled. `operational-error.ts`'s `sanitiseError`/`toClientError` had
  zero callers. Both are now live in `src/start.ts` via a new **`functionMiddleware`** array
  (`[sanitiserMiddleware, rateLimitMiddleware]`, order load-bearing: the sanitiser must wrap the
  limiter or the limiter's own throw escapes unmapped).

  **New module `src/utils/rate-limit-runtime.ts`** — the impure half of the pure/impure split the
  codebase already uses. Owns the process-wide counter map and parsed config; exports
  `rateLimitDecision()`, `describeDenial()`, `rateLimitStatus()`, `resetRateLimitRuntime()`.
  State/config are injectable so `bun test` never touches the singletons. **Two dimensions are
  checked per call**, not one: a per-tier limit alone lets a caller spend strict + expensive +
  moderate + loose concurrently (165/min while exceeding no single tier); a global ceiling alone
  lets 240 credential-vault attempts through.

  **Why the function layer and not the request layer:** re-confirmed against
  `node_modules/@tanstack/start-client-core` — `RequestServerOptions` declares `serverFnMeta?` but
  1.168 never populates it; `FunctionMiddlewareServerFnOptions` does. Function middleware has no
  `request`, so headers come from `getRequestHeaders()`, which this codebase already proves works
  in that context (`credential-vault.ts`, `settings.tsx`). Note `createMiddleware()` with no
  argument yields a **request** middleware — `{ type: "function" }` is required.

  **⚠️ A trap recorded in this file is WRONG for 1.168, and was corrected by measurement.** Phase-1
  trap #1 claimed seroval copies `stack` — "absolute container paths included" — to the browser.
  It does not. Built the app with the sanitiser REMOVED, threw a real error from a server function,
  and read the RPC payload in a browser: it is tagged `"c":"$TSR/Error"`, a custom serialiser that
  emits **`message` and nothing else**. No stack, no path. The corollary matters more than the
  correction: **none of the fields `toClientError` attaches (`code`, `correlationId`,
  `retryAfterMs`) reach the client either — do not build UI that reads them off the error.** The
  leak the sanitiser genuinely closes is through `message`, which crosses verbatim and is routinely
  built by concatenating upstream text (an undici `ECONNREFUSED 10.0.3.14:8080`, an fs errno with a
  path, up to 300 chars of a provider's response body).

  **Ships in `observe` mode** (the default when `RATE_LIMIT_MODE` is unset): it logs what it would
  have denied and denies nothing. Flip to `enforce` only after confirming the observe lines report
  `Client key source: forwarded` — locally they say `unknown`, which is correct with no proxy, but
  `unknown` in production means the ingress is not passing `x-forwarded-for` and enforcing would
  collapse every caller into one bucket. `.env.example` documents all six tiers and every knob.

  **Verified live, in a real browser, not just by `bun test`** — this project's own rule. Booted
  the built server with `RATE_LIMIT_EXPENSIVE_LIMIT=1` and loaded `/crawlers` three times:
  `collectorHealth` correctly resolved to the `expensive` tier by name; observe logged two
  would-have-denied lines and the page rendered normally; enforce denied and the browser showed
  the authored reason — *"Probe run failed: Rate limit backoff is active for another 16s. Retrying
  sooner does not extend it."* — through the existing `err.message` call sites, unchanged.
  **1169 tests passing** (+19, `tests/rate-limit-runtime.test.ts`), `tsc --noEmit` clean,
  **210 exports verified**, `bun run build` green.

  **`bun run smoke` is red at baseline and this change does not alter that** — 0/31 routes, every
  one failing on a CSP console warning (`upgrade-insecure-requests` ignored in a report-only
  policy) emitted by the pre-existing `security-headers.ts`. Confirmed by reverting `start.ts` to
  HEAD and re-running: identical 0/31, identical React errors on `/gis`, `/news`, `/recon`,
  `/reports`, `/sources`. **A permanently red gate is one nobody reads** — the same lesson the
  YouTube export-audit entry below records. Worth fixing separately.

- [x] **Security hardening, phases 0–1 (2026-08-17)**: The analytical layer was mature; the
  security layer did not exist. No authentication, no rate limiting, and **52 server functions
  carrying 52 identity validators** — `.validator((d: { text: string }) => d)` is a TypeScript
  annotation, erased at build, so every handler received whatever JSON the caller posted.

  **P0-1 — unauthenticated credential dump.** `getCredentials` (`routes/settings.tsx`) was a GET
  server function with no validator that returned `readVault()` VERBATIM: every stored secret in
  cleartext to anyone who could reach the origin — Bluesky app password, Reddit client secret,
  YouTube API key, UCDP token, GitHub token, Mastodon access token, both LLM keys. Its sibling
  `listCredentials` had always redacted; this one never did. The route gate is the disclosed
  client-side demo session, and **CSRF middleware does not help — it stops a cross-site browser
  request, not `curl`.** Now redacts. `saveCredentials` (validator: `(data: any) => data`,
  replaced the WHOLE file) is schema-validated and gated. **No server function returns a stored
  secret any more; do not add one back.**

  **P0-2 — SSRF with credential forwarding.** `fetchMastodonTag` sanitised a caller-supplied
  instance with `.replace(/^https?:\/\//,"").replace(/\/.*$/,"")` — scheme and path only, so a
  port, `@` userinfo and query survived — and never checked the allowlist declared 60 lines
  above it. `instance: "169.254.169.254:80"` produced a server-side GET against Azure instance
  metadata with the body returned to the caller. Chained with P0-1 it was worse: write a
  `mastodon` entry whose identifier is your host, call `verifyCredential`, and the server
  delivers the stored **Bearer token** to you. Allowlisted in `resolveMastodonHost` **and** in
  `verifyProviderCredential` next to the fetch that carries the secret — `fetchMastodonTag` is
  exported, so a validator on the RPC wrapper alone is bypassable.
  `MASTODON_ALLOWED_HOSTS` is deliberately WIDER than `MASTODON_INSTANCES`: the former is what
  the guard permits, the latter what the UI offers. `infosec.exchange` 422s anonymous readers,
  and reporting that refusal *as a refusal* is a distinction this collector is built around.

  **Security headers**, applied in `server.ts` to every response including error pages: nosniff,
  `X-Frame-Options: DENY`, referrer policy, permissions policy, COOP/CORP, HSTS on HTTPS only
  (read from `x-forwarded-proto` — behind ACA ingress the container connection is plain HTTP, so
  `url.protocol` would suppress HSTS on every production response). **CSP ships `report-only`**;
  a policy tight enough to matter can white-screen the app and that shows up only in a browser.
  `CSP_MODE=enforce` flips it after a clean console check.

  **New pure modules**, all `bun test`-able with injected clocks and env: `validation.ts`
  (`validate()` wraps a schema as a plain function — a BARE zod schema hits TanStack's Standard
  Schema branch and throws `JSON.stringify(issues)` at the browser), `rate-limit.ts`,
  `client-ip.ts`, `rate-limit-tiers.ts`, `operational-error.ts`, `operator-auth.ts`,
  `security-headers.ts`.

  **883 tests passing** (+164), `tsc --noEmit` clean, 204 exports verified, `bun run build` green.

  **Two traps recorded for whoever continues this:**
  1. **`errorMiddleware` in `start.ts` is DEAD CODE for all 52 server functions.**
     `server-functions-handler.js` catches their throws itself and *returns* a 500 Response, so
     by the time `await next()` resolves there is nothing to catch. Worse, TanStack serialises
     the thrown error with seroval at full feature level — which copies **`stack`**, absolute
     container paths included, to the browser. Sanitising must happen in a global
     `functionMiddleware`, which is the only layer that sees the throw first.
  2. **`getRequestIP({ xForwardedFor: true })` must not be used for rate limiting.** Verified at
     `h3/dist/h3.mjs:651` it takes `split(",")[0]` — the LEFTMOST, entirely caller-supplied,
     entry. Keyed on that, an attacker rotates the header per request and also evicts real
     offenders from the bounded map. `client-ip.ts` counts hops from the RIGHT.

  **Still outstanding:** ~40 identity validators still to migrate. (Rate-limit middleware and
  error sanitisation were the other two items here; both are now wired — see the phase-2 entry
  immediately below.)

- [x] **Six reported defects + a fresh fabrication sweep (2026-08-17)**: A user driving the UI
  reported six broken features. **Two of the six premises were factually wrong**, and a sweep
  with the three CLAUDE.md greps found **seven further live fabrications nobody had reported**
  — every one of which passed `bun test`, `tsc --noEmit`, the export audit and `smoke:controls`,
  because no unit test can see a number that is invented at render time.

  **The corrections.** `/live`'s bookmark button was *not* a dead control: it was wired and did
  persist, but wrote a bare URL array to `sentinel_bookmarks` that **no other file in the
  repository ever read** — a dead end, not a dead button, and one that discarded the publisher,
  date and body, so a bookmark could never become citable evidence. And Reddit / Bluesky /
  Mastodon / GitHub were **already** on `/settings` with vault entries, env overrides, live
  Verify probes and working collectors; the real problem was that nothing was configured, so
  every consuming module correctly reported a missing credential and the app read as broken.

  **The seven unreported fabrications.** `/subjects` generated its "Activity Scanner Pulse"
  series from the loop index — `threats: Math.max(2, Math.round(baseVal * 0.4 + ((idx * 2) % 5)))`
  and `scans: Math.round(150 + idx * 12 + ((idx * idx * 3) % 25))`, with a floor of 5 so the
  chart was never empty even with zero matches, and a second series counting an activity this
  system does not perform. Its own comment said "chart mock trend points". The same page showed
  a **"Growth Rate"** tile of `matches.length * 1.5` percent (or the literal `8`), and rendered
  `{riskScore}/100` unguarded, so an analyst-created watchlist displayed **`null/100`**.
  `watchlist-store.ts` seeded `riskScore: 78` and `42` in the same file where `createWatchlist`
  was changed to `null` because that score was invented, with `createdAt: new Date()` at module
  scope so both samples always claimed to be seconds old. `/agents` seeded five fictional
  entities into its picker with two **pre-selected** and fed to the model as the analysis
  target, with no sample-data banner. The dashboard badged every watchlist `MONITORING`,
  contradicting `/watchlists`' own statement that nothing is scheduled. `/osint` substituted
  `"GDELT"` and `"Google News"` — aggregators — for missing publishers, the same class of error
  `rss-source.ts` exists to fix.

  **What was built.** Five new pure modules, all tested: `evidence-store.ts` (single owner of
  `sentinel_evidence`, which had two writers with two independently-declared shapes),
  `bookmark-store.ts` (v1 URL array → v2 records, migrating **without back-filling a single
  field**), `live-filters.ts` (the "Any time" window plus a testable predicate),
  `graph-build.ts` (co-occurrence graph, deterministic Fruchterman–Reingold seeded on a circle
  — **no `Math.random()`**, no new dependency), and `bucketMatchesByHour` in `watchlist-store.ts`.
  `/graph` was rewritten from a fixed ten-node topology onto the live entity-extraction pipeline
  `/entities` already runs; `/network` now draws the CIB clusters it was already computing;
  `/osint`'s UCDP path was deleted and delegated to `collectConflict()`, which actually sends the
  token. **719 unit tests passing** (+66), **`tsc --noEmit` clean**, **200 core exports verified**
  (+49 — the new modules are now guarded), `bun run build` green.

  Full defect list and file:line references: [`ANTIGRAVITY_TASK.md`](file:///d:/social_media_research/ANTIGRAVITY_TASK.md).

  **Three gaps in that work, found and closed the same day.** All three were invisible to every
  gate, which is the blind spot the original fabrications exploited.

  1. **The watchlist fabrication survived on existing browsers.** Correcting
     `DEFAULT_WATCHLISTS` to `riskScore: null` was not enough: `getWatchlists()` writes the
     defaults to storage on first ever load and reads from storage forever after, and it had
     **no version key**. A fresh browser got `null`; every browser that had already opened the
     app — a demo machine included — kept the seeded 78 and 42 and went on rendering "78/100"
     on `/subjects` and `/watchlists`. Fixed with `sentinel_watchlists_version = "2"` and an
     exported `migrateWatchlists`, which **migrates rather than wipes** and nulls **any**
     non-null score, not just the two seeded ids — nothing has ever computed one, so every
     non-null value in storage is by definition invented. **If a fabricated figure is removed
     from a seed constant, check whether that seed was already persisted.**
  2. **`entityKey` briefly existed twice** — `graph-build.ts` and `entities.tsx`. It carries a
     load-bearing fix (the old class covered Devanagari–Sinhala only, so every Urdu name was
     stripped to an empty key and merged into one node), and `/graph` and `/entities` must key
     identically or one corpus yields two different merged sets. `entities.tsx` now imports it.
  3. **`layoutGraph` had no node cap.** O(n² × 300) synchronously inside a `useMemo`; at 400+
     entities the tab froze. `iterationsFor(n)` now holds the work product roughly constant
     (300 iterations for small graphs, floor of 60), and `buildEntityGraph` caps at
     `DEFAULT_MAX_NODES = 250`, highest-degree first. **The cap is reported on screen** —
     `EntityGraph` carries `totalNodes` and `truncated`, and `/graph` renders "top 250 of 613".
     A silently truncated graph reads as the whole picture, which is the same defect class in a
     new costume.

- [x] **OSINT collector framework — evaluated a user-supplied tool list; built Jina Reader, ruled out the rest with a reason on record (2026-08-17)**: Checked each tool directly rather than assuming. **Ruled out**: Agent-Reach, OpenCLI, a Facebook OpenCLI adapter, and Twitter CLI all use cookie-exported/logged-in-browser-session scraping for Twitter/Reddit/Facebook/Instagram/XiaoHongShu — confirmed by reading Agent-Reach's own docs (which self-disclose "using Cookie login... carries account suspension risk") and the Facebook adapter's doc directly (browser automation on a real logged-in session, no ToS warning given at all). Exactly the pattern this project already rejected for Instagram/Facebook and never re-added for Twitter/X. `ArjunPrakash09/linkedin-scraper-mcp` 404s — doesn't exist. GitHub CLI/API and feedparser are redundant (already integrated; `rss-source.ts`+`rss-parser` already covers RSS in TypeScript). yt-dlp would mean subprocessing a Python CLI, the same problem already avoided for theHarvester. Exa has a genuine free tier but needs an API key not available this session — deferred, not built. **Built**: `src/utils/collectors/external/jina-reader.ts` — free, genuinely keyless (`r.jina.ai`, 20 req/min unauthenticated), full-text extraction for `url`-type targets, filling a real gap (article bodies elsewhere in this app are whatever a feed snippet gives). Response shape verified against the LIVE endpoint before writing any parsing code — a real `curl` against a real page, a real 404 target, and a malformed URL. That surfaced a genuine correctness trap: **Jina Reader's own HTTP status is not the target page's status** — a target 404 still comes back as an outer HTTP 200, with the real status at `data.httpStatus`. Naively trusting the outer 200 would have silently presented fallback/cached content as the live page; `normalize()` checks `data.httpStatus` explicitly and warns instead. Produces an `article` entity, a `domain` entity from the hostname, and a `HOSTED_ON` relationship between them. 18 new tests using fixture bodies copied verbatim from the real `curl` responses, not invented shapes. **Verified three ways**: unit tests, a direct live network call (`bun -e` against a real Wikipedia article — real 21,907-character extraction, correct title/entities), and a full live browser run on `/recon` (real investigation of a URL target → `jina-reader` offered and runs → real entities rendered, zero console errors). The first browser-check attempt used the wrong input field (global nav search bar instead of `/recon`'s own target field) and silently investigated a stale target — caught because the collector correctly did NOT offer itself for that domain-type target, exactly right behavior; fixed the test script, re-verified correctly. **Also fixed a stale claim found along the way**: `recon-sources.ts`'s "what external recon does not do" panel still said a Maltego CSV export "is not built yet," false since the previous milestone shipped it — corrected while keeping the still-true explanation of why a *live* Maltego transform endpoint remains architecturally impossible here. Full suite 895/895 passing (18 new), `tsc --noEmit` clean, 151 exports unchanged, `fabrication-check` unchanged at 81, lint clean.
- [x] **OSINT collector framework — Maltego export (P3), and a severe live regression found and fixed (2026-08-17)**: New `src/utils/maltego-export.ts`, `toMaltegoCsv(entities, relationships)` — a pure function producing an edge-list CSV matching how Maltego's own "Import Graph from Table" wizard builds a graph (one row per relationship, source+target entity columns each side; any entity with zero relationships gets its own standalone row rather than being silently dropped). `MALTEGO_TYPE` maps Sentinel's 13 entity types to Maltego's stock palette — best-effort, explicitly caveated as not verified against a live Maltego install (same honest treatment as the theHarvester/SpiderFoot parsers), mitigated structurally by keeping the real Sentinel type in its own column so a wrong guess is always correctable during Maltego's import wizard, never a silent loss. 13 new tests. Wired into `/graph` as an "Export to Maltego" button exporting the full entity/relationship set, not just the on-screen-capped subset. **While browser-testing this, caught a severe regression already live on `origin/main`**: the previous session's persistent-job-storage work (`jobs.ts`) statically imported `bun:sqlite` in a module client route components also import; since TanStack Start only strips `.handler()` body code from the client bundle, not top-level module code, this pulled `bun:sqlite` into the *browser* bundle, and Vite's externalized stub for a native binding with no browser shim throws the moment the import binding evaluates — every route crashed with "This page didn't load," undetected at the time because that work was verified only with `bun test`/`tsc`/direct `bun -e` checks, never an actual browser launch. Fixed by guarding `createJobStore()` on `typeof window === "undefined"` first (this codebase's existing client/server split convention) and loading `SqliteJobStore` via `require()` instead of a static import — chosen over a guarded dynamic `import()` because a bundler can still discover and bundle a reachable `import()` call inside a dead branch, where `require()` never enters the client bundle's static import graph at all. Verified server-side (`bun -e` against the real singleton, both `JOB_STORE_PATH` states) and, the check that actually matters, a full live browser run: login → real investigation on `/recon` → "View in Graph" → `/graph` renders → "Export to Maltego" produces a real CSV download with real DNS data → zero console errors. Full suite 877/877 passing (13 new), `tsc --noEmit` clean, 151 exports unchanged, `fabrication-check` unchanged at 81, lint clean (one justified `eslint-disable` for the deliberate `require()`).
- [x] **OSINT collector framework — persistent job storage + `.env.example`, 2 of 6 P2-Production items (2026-08-14)**: `jobs.ts` used to define its job store as one concrete class with no swap point. Extracted with zero behavior change into `src/utils/osint/job-store.ts` (`JobStore` interface + `InMemoryJobStore`, byte-for-byte the original logic under a new name, re-exported from `jobs.ts` so nothing importing `@/utils/osint/jobs` needed to change — the only real call-site fix was `tests/osint-jobs.test.ts`'s 14 `new JobStore()` constructions becoming `new InMemoryJobStore()`). New `src/utils/osint/job-store-sqlite.ts`: `SqliteJobStore implements JobStore` using `bun:sqlite` (a Bun built-in, no new dependency) — three tables, every complex field JSON-encoded, `progress`/`error`/timestamps staying genuine SQL `NULL` and round-tripping as `null`, never defaulted. Wired in behind `JOB_STORE_PATH` exactly like `llm.ts`'s `LLM_BASE_URL` pattern: unset keeps in-memory behavior unchanged, set to a path switches the `jobStore` singleton to SQLite with no other code touched. **Verified two ways**: 16 new unit tests including the one that actually proves persistence (write with one `SqliteJobStore` instance, close it, read with a fresh instance at the same path — literally what "survives a scale-to-zero cold start" means); and a direct manual check against the real singleton (`JOB_STORE_PATH` unset → `jobStore.constructor.name === "InMemoryJobStore"`; set → `"SqliteJobStore"`, with a real investigation immediately visible via `hasInvestigation()`). Also built `.env.example` — did not exist anywhere despite several load-bearing env vars — from a real audit of every `process.env.*` read in `src/` (including `llm.ts`'s dynamic `${prefix}_BASE_URL` construction, which a literal grep alone would have missed) cross-checked against `credential-vault.ts`'s `CREDENTIAL_PROVIDERS` registry. Every value in the file is empty; it documents names only. Added a `!.env.example` negation to `.gitignore` (the existing `.env.*` rule would otherwise have swallowed it — confirmed fixed via `git check-ignore`) and a `data/jobs.sqlite*` entry for the new optional local store. **Deliberately not attempted in the same pass**: Worker Dockerfile, local docker-compose, Azure configuration (no Docker locally, no deployed theHarvester/SpiderFoot worker to containerize, and Azure changes touch real subscription resources) and health checks (ambiguous scope against the already-existing `collector-health.ts`). Full suite 864/864 passing (16 new), `tsc --noEmit` clean, 151 exports unchanged, `fabrication-check` unchanged at 81, lint clean with zero new findings on every touched/new file.
- [x] **OSINT collector framework — `/reports` gains OSINT sourcing, closing P2-Reports in full (2026-08-14)**: Two new functions in `src/utils/reports.ts` — `sourcesFromOsintEvidence()` and `sourcesFromOsintRelationships()` — convert the P0 `CollectorEvidence[]`/`CollectorRelationship[]` shapes into the report layer's own numbered, citable `SourceRef`s, closing all four P2-Reports checklist items at once ("Include external collector results"/"Include evidence" are the same function since theHarvester/SpiderFoot produce the identical shape as every other collector; "Include relationships" is the second function; "Include timeline" needed no new code since `EVENT_TIMELINE` already chronologically orders by `publishedAt`, and OSINT evidence's real `collectedAt` slots straight in). Both land under `"Module 2 · content analysis"` — the one `ContributingModule` value no existing source function had claimed. `/reports` gained an "Include OSINT investigation" button, deliberately separate from the page's automatic news/geo `collect()` since most report subjects here are open topics, not recon targets, and running live crt.sh/Shodan/theHarvester/SpiderFoot calls on every subject change would be slow and usually fruitless — an analyst working an actual domain/IP/email opts in explicitly. Sources are appended, not merged fresh, so existing inclusion/exclusion decisions survive. **Real bug caught by browser-testing before it shipped**: a domain with real infrastructure returns 30-100+ evidence/relationship items, comfortably past `DEFAULT_SOURCE_BUDGET` — reproducing the exact HTTP-413 failure this same file's comments already document as a prior real incident, just via a second code path. First live run showed `71 of 71 included` (no trim applied to the appended sources). Fixed by computing remaining budget from the analyst's prior selections before appending and pre-excluding only the overflow; re-verified live afterward showing the correct `12 of 82 included`. A second real fabrication-guard catch while writing this: the custom `sentinel/no-fabricated-fallback` ESLint rule flagged a defensive `catch` fallback returning the literal `"(unserializable value)"` — not in the rule's recognized absence-marker vocabulary — fixed by rewording to `"(not serializable)"`, matching its `not\s+\w+` pattern. 13 new tests. **Verified live** (`example.com`): real per-collector summary shown, real honestly-surfaced collector failures rendered on screen (crt.sh 502, GDELT 429, theHarvester/SpiderFoot unconfigured), budget trim correct across two runs, and Generate with no LLM configured showed the pre-existing honest "No LLM provider configured" message rather than hanging. Zero console errors. Full suite 848/848 passing, `tsc --noEmit` clean, `fabrication-check` unchanged at 81. This closes every item in P2 — Reports.
- [x] **OSINT collector framework — `/graph` rebuilt on real data, closing P2-UI in full (2026-08-14)**: `/graph` previously rendered ten hand-placed fictional nodes ("Vector-17", "Aster Motors") behind a `SampleDataBanner`. New pure/testable logic: `src/utils/graph-layout.ts` (`layoutRadial()` — deterministic BFS-ring layout by hop distance from one root entity, no physics sim, degree-based node radius, unreachable entities placed honestly in an outer ring rather than dropped; `shortestPath()` — real BFS traversal, replacing the fixture's hand-written path narration) and `src/utils/graph-store.ts` (versioned localStorage hand-off, same pattern as `investigations-store.ts`/`active-target.ts`, one snapshot at a time). `/recon`'s `InvestigationPanel` gained a "View in Graph" button (enabled once the poll returns ≥1 entity) that saves the snapshot and navigates over. `/graph` shows an honest empty state (`EmptyState`, link back to Recon) when nothing is saved — no fallback to sample data. Full 13-value `EntityType` styling (13 evenly-spaced hues) replaces the fixture's 7 invented types. Node click shows a real detail panel (value, source collector, confidence-or-"not scored", live connection count, BFS distance from target) — the fixture's fabricated "Aliases"/"Risk score"/"First seen" fields are gone, not just relabeled. Same DOM-scale problem as the evidence-inspector milestone below applies here too: capped at 150 rendered nodes, nearest-to-target first, disclosed on-canvas. 23 new tests. **Verified live** (`google.com`): empty state confirmed pre-investigation; ran a real investigation, clicked "View in Graph," landed on a populated graph with real `RESOLVES_TO` edges to live-resolved IPs; selected the root node and confirmed real DNS-collector data in the detail panel; selected a `dorks`-collector news-article entity and confirmed the "Path to target" panel computed a real `mentioned in` path back to the root; confirmed the filter dims non-matching nodes. Zero console errors. One real, already-documented behavior surfaced, not a new bug: `pollInvestigation()` dedupes by exact id, not by value, so `dns` and `rdap` each independently producing a `domain` entity for the same value render as two nodes — value-based merging is `entity-resolution.ts`'s job, used by the batch path, not the live-polling path this UI reads from. Full suite 835/835 passing, `tsc --noEmit` clean, `fabrication-check` unchanged at 81 (one new match introduced and fixed in the same pass — a `?? 0` on a structurally-always-populated `Map.get()`, replaced with a `!` assertion). This closes every remaining P2-UI checklist item except Report generation.
- [x] **OSINT collector framework — `/recon` manual collector selection, closing P2-UI's checklist bar Graph (2026-08-14)**: `InvestigationPanel` now previews the plan (`planOsintInvestigation`, a new read-only `createServerFn` in `jobs.ts` — plans without starting anything, no job created) whenever the target changes, rendering one checkbox per candidate collector, checked by default. `startInvestigation()` gained an optional `collectorIds` parameter restricting execution to that subset (omit it, or pass every candidate, for the unchanged "run everything" default; an empty array explicitly means "run nothing," kept distinct from "no target set," which disables the button for a different, honest reason). `startOsintInvestigationJob`'s validator threads the same optional field through. 4 new tests covering the filter (subset selection, an unknown id silently ignored since the UI only ever sends ids from its own plan, empty array vs. omitted). **Verified end-to-end, not just in the UI**: an `example.com` run showed 8/8 checked by default, deselecting `news` and `shodan-internetdb` updated the count to 6/8, and the subsequent live collector-status list showed *exactly* those 6 — the two deselected collectors never appeared, confirming the filter reaches execution, not just checkbox state. Zero console errors. Full suite 812/812 passing. This closes every P2-UI checklist item except Graph navigation.
- [x] **OSINT collector framework — `/recon` evidence inspector + a real scale finding (2026-08-14)**: Closed the plan's "Evidence" item — a collapsible section under Entities lists every evidence item individually (collector badge, timestamp, source, confidence where scored, a JSON preview of `normalizedValue`, and a link to `sourceUrl` when present), the concrete answer to Rule 6/§20's "the source of every fact must remain inspectable," not just a count. **Real finding from testing against an actual large target**: a `cloudflare.com` run returned 3,461 entities — almost all genuine crt.sh-logged subdomains for a company that size, a correct result, not a bug — but rendering all of them into the DOM uncapped is a real performance/UX problem an analyst would hit on any sizeable target, the kind of thing a synthetic fixture never would have surfaced. Capped rendering at 200 items per list (`MAX_RENDERED_ITEMS`), UI-layer only — the underlying `poll.entities`/`poll.evidence` arrays stay untruncated for whatever consumes them later (a report, an export, a graph view). Verified live against `github.com` (148 entities, under the cap) with the evidence panel expanded and screenshotted; zero console errors. Full suite 808/808 passing.
- [x] **OSINT collector framework — `/recon` panel gets live progress (2026-08-14)**: `InvestigationPanel` switched from the blocking `runOsintInvestigation` to polling two new `createServerFn`s in `jobs.ts` (`startOsintInvestigationJob`/`pollOsintInvestigationJob`, mirroring `orchestrator.ts`'s existing wrapper), every 1.2s until every collector job reaches a terminal status. Badges now show `running` (spinning icon) live, not just the final state. **This is the first UI consumer `jobs.ts` has ever had** — it was fully built and unit-tested in P1 but had zero callers anywhere in the app, the identical "control with no handler" shape as the theHarvester/SpiderFoot registration bug from the milestone below, just for the entire job system instead of two collectors. Entity resolution is deliberately not applied during polling (only the synchronous path does that — resolving on every poll tick against a still-growing set would be wasted work). **Verified live** (Chrome via `playwright-core`): a `stripe.com` run was screenshotted mid-flight showing 6 collectors simultaneously in a `running` state (theHarvester/SpiderFoot were already terminal — correct, their `unavailable` check needs no network call), then again after completion showing all 8 terminal (`crtsh` failed on a live HTTP 502, `news` hit GDELT's real rate limit) with 37 entities / 30 evidence items and zero console errors. A screenshot of only the end state would not have distinguished this from the old blocking version — the mid-flight capture is what actually proves "Progress" works. Full suite 808/808 passing.
- [x] **OSINT collector framework — P2 starts: `/recon` Investigation panel, first reachable feature (2026-08-14)**: New "OSINT Investigation — multi-collector" panel on `/recon` (`InvestigationPanel` in `src/routes/recon.tsx`), following the page's existing self-contained-panel pattern exactly — one new component, one new render line, nothing else on the page touched. Backed by `runOsintInvestigation` (new `createServerFn` in `src/utils/osint/orchestrator.ts`), which registers all collectors and applies entity resolution before returning. **Found and fixed a real bug by actually testing in a browser, not just `tsc`/`bun test`**: theHarvester and SpiderFoot were fully built and unit-tested but never registered anywhere — a classic "control with no handler," the exact pattern CLAUDE.md's own fabrication-audit history warns about. `tsc`/`bun test` couldn't have caught this (both adapters' own test suites pass regardless of whether anything registers them). Fixed by adding `src/utils/collectors/external/index.ts` (mirroring `existing/index.ts`) and wiring it in; confirmed fixed by re-running the same browser test and watching the collector count go from 6 to 8. **Verified live** (Chrome via `playwright-core`, dev server, real network): a `github.com` run returned 31 entities / 28 evidence items — `dorks`/`dns`/`rdap`/`shodan-internetdb` completed, `crtsh` failed with the exact live "HTTP 503 after a retry" `recon-sources.ts` itself produces, `news` hit GDELT's real rate limit, and the merged `github.com` domain entity showed 95% confidence from 3 independent sources — a live exercise of the entity-resolution confidence model, not a fixture. Screenshotted; visually consistent with the rest of the page. One real TanStack Start integration issue hit and fixed along the way: `createServerFn`'s serialization type-checker rejects `unknown`-typed fields (the P0 contract's `metadata`/`rawValue` are deliberately `unknown`) — resolved narrowly at the server-function boundary via a `JSON.parse(JSON.stringify(...))` round-trip (a real serializability proof, not just a type-checker workaround) rather than loosening the P0 contract itself. Covers investigation-start/collector-status/results from the P2-UI checklist, partial collector-selection (automatic only) and evidence (count only); progress (job-polling UI) and graph navigation remain unbuilt. Full suite 808/808 passing throughout.
- [x] **OSINT collector framework — entity resolution, P1 now fully complete (2026-08-14)**: `src/utils/osint/entity-resolution.ts` — the piece `orchestrator.ts`/`jobs.ts` both explicitly deferred: merges same-type, same-normalized-value entities across collectors (`dns:domain:x` + `rdap:domain:x` + `crtsh:domain:x` → one entity), matching plan §17's own worked example (three collectors reporting one email → ONE entity, THREE evidence items — evidence is never touched by this file, only the entity/relationship lists). **`person` and `organization` entities are never merged by value equality, at all** — not merged-with-low-confidence, genuinely kept separate — because a shared *name* is exactly what plan §18 says must not imply identity, and a caveat on an already-merged row is a weaker safeguard than not merging in the first place. Everything else (email, domain, ip, url, username, phone, location, article, image, video, social_account) merges on exact value after type-specific normalization (email/domain/username/url get real rules; the rest trim+lowercase) — precise identifiers, a materially different kind of match than a name. Confidence scores only on "multiple independent sources" (honestly the only signal this file has access to), always with reasons, never a bare number. Relationship endpoints get rewritten to merged ids; a resulting self-loop is dropped; duplicates from the remap collapse via the existing shared dedup. Opt-in via `resolveInvestigationEntities()` — not wired into `runInvestigation()`/`pollInvestigation()` automatically. Found and fixed one real bug while testing: `normalizeUrl`'s trailing-slash strip had an off-by-one guard that protected exactly the root-path case (`https://example.com/`) it was meant to also normalize. 19 new tests. Full suite 808/808 passing. **This is the last item in plan §31's P1 list** — P1 is done in full.
- [x] **OSINT collector framework — SpiderFoot adapter, client-only (2026-08-14)**: `src/utils/collectors/external/spiderfoot.ts` — same "adapter built, no worker deployed" status as theHarvester (see the entry below), and the same non-reopening of `RECON_NOTES`'s existing SpiderFoot objection, for the same reason. Differs from every other collector in the codebase in one real way: SpiderFoot scans take minutes, not seconds, so `execute()` starts a scan then polls SpiderFoot's own status endpoint (`SPIDERFOOT_POLL_INTERVAL_MS`, default 5s) until `FINISHED`/`ERROR-*` or a bounded wait (`SPIDERFOOT_MAX_WAIT_MS`, default 2 minutes) elapses — documented as a real simplification (production scans can run considerably longer; a full integration would need the scan to survive across multiple job-system polls rather than block one call). Defaults to SpiderFoot's "passive" use-case, never active/intrusive. API shape (`/startscan`, `/scanstatus`, `/scaneventresults`) is from training knowledge, not a verified live instance — flagged the same way theHarvester's parser is. Event-type → entity mapping deliberately covers a defensible subset of SpiderFoot's several hundred event types; unmapped types stay evidence-only with a named warning, never silently dropped. 13 tests (fetch-stubbed, run 3× to rule out polling-loop flakiness). Full suite 789/789 passing. This closes out everything in plan §31's P1 list except entity resolution (§17) and UI wiring (P2) — P1's external-tool and orchestration work is done.
- [x] **OSINT collector framework — theHarvester adapter, client-only (2026-08-14)**: `src/utils/collectors/external/theharvester.ts` — the first `external/` (as opposed to `existing/`) collector. Calls an independently-deployed worker at `THEHARVESTER_WORKER_URL`; **no such worker is deployed anywhere**, so the collector honestly reports `unavailable` in every real environment today — not a hidden gap behind a checked box. This deliberately does not reopen the licensing question `recon-sources.ts`'s `RECON_NOTES` already settled ("theHarvester executed in-app" — objects to *subprocessing the GPL binary inside Sentinel's own process*): this adapter never subprocesses theHarvester, only makes an HTTP call to a separate worker, the same architecture plan §15 describes and `ai-service/` already established as a pattern this session. `RECON_NOTES` itself is untouched and still accurate. The JSON parser targets theHarvester's own documented `-f json` export format from training knowledge, not a verified live instance — flagged explicitly in the code and the plan doc as needing reverification once a real worker exists, written defensively (unexpected/missing fields degrade to "not reported," never fabricated) so a shape mismatch fails safe. Uses the plan's own recommended sources (crt.sh, CertSpotter), explicitly not `-b all`. 14 tests, all fetch-stubbed. Full suite 776/776 passing.
- [x] **OSINT collector framework — P1 job system (2026-08-14)**: `src/utils/osint/jobs.ts` — the async, pollable sibling of `orchestrator.ts`'s synchronous `runInvestigation()`. `startInvestigation()` plans and creates one `InvestigationJob` per selected collector, starts each without awaiting it (returns immediately, matching plan §12: external tools "must not run inside a normal browser request for an extended period"); `pollInvestigation()` aggregates completed jobs' results (`GET /investigation/:id`'s shape, as a plain function — not a route). Extracted `dedupeEntitiesById`/`dedupeRelationships` out of `orchestrator.ts` into a new shared `osint/merge.ts` so both execution paths dedupe identically. Three limits documented in the code and the plan doc rather than left implicit: persistence is in-memory/per-process only (same accepted tradeoff as `llm.ts`'s cache — needs real storage, §16, before production use); "queued" is near-instant for P1's own collectors since none of them hand off to an external worker yet; cancellation cannot abort an in-flight request because `Collector.execute()` takes no `AbortSignal` — `cancelJob()` marks a job cancelled and discards its result once the underlying (uncancellable) fetch eventually resolves, verified by a dedicated test. 9 new tests, including the cancellation-discard behavior and an injectable-timeout test (fast — doesn't wait on the real 60s default), run 3× to confirm no timing flakiness. Full suite 762/762 passing.
- [x] **OSINT collector framework — P1 orchestrator + query planner (2026-08-14)**: `src/utils/osint/query-planner.ts` (`detectTargetType`: precedence-ordered IP/URL/email/domain/phone classifier, ambiguous bare-word/free-text input returns both `person` and `username` as candidates rather than guessing one; `planInvestigation`: selects registered collectors by `supportedTargetTypes` across all candidate types, deduped) and `src/utils/osint/orchestrator.ts` (`runInvestigation`: runs every planned collector's `execute()`+`normalize()` in one `Promise.all`, merges entities/relationships/evidence/warnings/errors, exact-id-only entity dedup). Deliberately does NOT do: the Job system (§12, everything runs synchronously in-process, no queue/polling), cross-collector semantic entity merging (§17 Entity Resolution — `dns:domain:x` and `rdap:domain:x` both surface as separate entities on purpose, documented in the code and the plan doc rather than silently left thin), or auto-registration into the global `collectorRegistry` (a caller must call `registerExistingCollectors()` first, so isolated test/future registries never get real network-calling adapters silently added). 22 new tests against isolated stub registries — no real network call in either test file. Full suite 753/753 passing.
- [x] **OSINT collector framework — P0 + P1 existing adapters (2026-08-14)**: Implementing `docs/OSINT-INTEGRATION-PLAN.md` in the order it prescribes (Rule 3: one architectural layer per task). **P0** — `src/utils/collectors/{types,result,errors,registry,index}.ts`: the `Collector` contract, zod-validated `InvestigationResult` model (entities/relationships/evidence/warnings/errors/metadata/execution), typed `CollectorError` (Rule 5: failures carry `status/reason`, never collapse to an empty result), and an empty `collectorRegistry`. **P1 — existing adapters**, `src/utils/collectors/existing/{dorks,dns,rdap,crtsh,shodan-internetdb,news,social}.ts`: six wrap an existing exported function verbatim (`collectCrtShSubdomains`, `resolveA`/`internetDb` newly exported from `attack-surface.ts`, `fetchNewsDorkHits` extracted from `dorks.ts`'s `runNewsDork`, `collectNewsGeo`, three `social.ts` fetchers) with zero behavior change to any of them; RDAP is a new implementation against the same free `rdap.org` endpoint `routes/news.tsx`'s inline WHOIS tab already validated, because extracting *that* would have meant editing a route file (deliberately out of scope). Building real adapters surfaced a genuine P0 contract gap — `normalize()` needs `execution` (part of `InvestigationResult`) but only `execute()` computes it — fixed by changing the signature to take the whole `CollectorRunOutcome` before any adapter was written against the broken version, not patched around it seven times. 47 new tests, full suite 731/731 passing, `tsc --noEmit` clean. Auth/RBAC from the earlier `MERGE_PACKAGE` merge stays deliberately excluded (see the entry below); nothing here reopens that decision. **Not done**: orchestrator, query planner, job system, theHarvester/SpiderFoot workers, entity resolution, UI — all P1-remainder/P2/P3, not started.
- [x] **`ai-service/` Python vision/forensics backend merged in (2026-08-14)**: A teammate's independently-developed FastAPI backend was merged from a `MERGE_PACKAGE/` (now deleted after merge — its content is preserved in this entry and in `CLAUDE.md`'s own `ai-service/` section). New top-level directory `ai-service/`, its own venv/`requirements.txt`/`Dockerfile`, deploying separately from the frontend. Real endpoints: `POST /ai/forensics` (EXIF incl. GPS location, pHash, C2PA best-effort — a Python-side re-implementation of the same techniques as `imaging.ts`, not a replacement for it), `POST /ai/ocr` (Tesseract), `POST /ai/phash/compare`, `POST /ai/detect` (Grounding DINO tiny, Apache 2.0, zero-shot object detection — the exact model this project's own licence-trap warning names as the correct choice over YOLO). Still 501 stubs: `/ai/faces`, `/ai/video`, `/ai/transcribe`, `/ai/translate`, `/ai/describe`, `/ai/chat`. **Verified in this environment**: all 8 `test_forensics.py` tests and 4/5 `test_skeleton.py` tests pass under a fresh venv with only the pure-Python dependencies installed. **Not verified here** (missing system Tesseract binary, and `torch`/`transformers`/vendored Grounding DINO weights deliberately not installed — multi-GB download, no GPU in this environment to exercise it on): the OCR and detect endpoints' own test files. Known real limitation carried forward: Grounding DINO returns confident false positives for absent objects, so `/ai/detect` output must be treated as an unverified candidate match, never a confirmed finding, until a UI surfaces it. **Deliberately excluded from the same merge**: a session-based auth/RBAC system (Prisma+SQLite) that was in the same source package — this project already built and deliberately reverted an auth system out of `main` on 2026-08-06 (see `backup/pre-auth-rollback` branch/tag below); re-adding it is a product decision, not something to fold in silently. No frontend code calls `ai-service/` yet — that integration is unstarted, not regressed.
- [x] **Collection-policy model + media ingestion + manual capture (2026-08-12)**: Implemented the ingestion-legality matrix across Module 3. `PLATFORM_NOTES.available` was a **boolean**, rendered as a green "collected" / red "unavailable" badge, which could not express "YouTube text yes, frames no" (so YouTube had no row at all), could not express "Meta not automated but an analyst may capture", and gave a ToS prohibition and a missing free tier the same red badge. New `src/utils/collection-policy.ts` adds four modes (`automated` / `partial` / `manual-only` / `none`), five legal bases, and the ingestion route per source; `policyId` links each platform note to its row, additively — `available` is untouched because three test files assert on it. **Media ingestion closed** for Bluesky, Reddit, Telegram and Mastodon (`SocialPost.media`, five extractors), with an Analyse hand-off into Module 4 via a new validated `?url=` param on `/images`. **Manual capture path** added for Meta (`manual-evidence.ts` + `components/manual-capture-panel.tsx`), writing to the same `sentinel_evidence` store `/vault` reads. **YouTube comments** added via the official Data API v3, behind a new `youtube` vault provider with a live verification probe. **625 unit tests passing** (+58), **`tsc --noEmit` clean**, **151 core exports verified**, `bun run build` green. Two pre-existing defects fixed in passing — see below.
- [x] **Browser-audit remediation, Phases 1, 2 and 4 (2026-08-12)**: Six agents drove all 31 routes in real Chrome, clicking every control on a fresh page load. The finding: the analytical core is sound and the presentation layer was not.

  **Phase 1 — `0ab3d44`, fabrication removal.** Verified absent from the SERVED HTML, not just the source. `/live` was discarding real article text and substituting invented Spanish, French and Hindi prose attributed to El Pais, Le Monde and Dainik Jagran — the code comment read "Simulating different languages based on index" — with platform, handle, location and credibility all manufactured from the array index, so Google News RSS rendered as X/Twitter posts while `/social` correctly declared X uncollectable. `/osint` carried five WHOIS/DNS literals ("GoDaddy", nameservers, an A record), a hardcoded "Aggregate confidence 74/100" sitting beneath six genuinely-derived counts, `status || "online"` rendering an unmeasured indicator as a live C2 beside a pulsing green dot, and `return 4290` as the unconditional OpenSky error value. `/agents` fed three hardcoded Jerusalem coordinates into the focal-point detector and printed the result as convergence analysis. `/tasks` badged all five modules "Nominal / Verified" on a compliance console, including two the same page's self-test called NOT IMPLEMENTED. **`<Toaster />` was never mounted**, so all 30 `toast.*` call sites were silent — including the vault path that rejects evidence when SubtleCrypto is unavailable, which failed invisibly.

  **Phase 2 — `e564b60`, correctness in working features.** Module 1 was rating `news.google.com` for every article, because a queried corpus comes from the Google News search feed whose links are all redirects; `src/utils/rss-source.ts` recovers the real publisher from the `<source url>` attribute in the raw XML, which rss-parser discards (verified live: 104 items, 104 index-aligned slots, 10 distinct publishers). TF-IDF ran over raw HTML and reported `nbsp`, `href` and `6f6f6f` among its dominant terms. The GPS and radiation collectors ran in the BROWSER, were CORS-blocked, and swallowed the failure into a permanent "Loading…"; the GPSJam endpoint also did not exist — it publishes a daily CSV of H3 cells, now parsed with `h3-js` (3,425 real cells). Spike detection bucketed on author-declared `createdAt`, running a median 2.8 hours behind, so `SocialPost.observedAt` records arrival instead. `validateCitations` accepted "Corroborated by two sources" while citing one.

  **Phases 3-4 — `c96912f` and `f3a21e0`.** EXIF capture time was run through `new Date(...).toISOString()` over a timezone-naive camera clock, applying the analyst machine's offset and appending a Z; `readExifCaptureTime` keeps the wall clock and fills `absolute` only when the file records a real offset. `/watchlists` had zero controls while importing six unused symbols. `/alerts` rendered 178 characters with no empty state. `/graph` had eight dead controls and its sample banner nested inside a button. The notification bell had no handler and a permanent unread dot. The evidence vault carried three invented SHA-256 digests — one of them 66 hex characters, not a SHA-256 at all, and unnoticed until now.

  **567 unit tests passing**, **`tsc --noEmit` clean**, **123 core exports verified**, `bun run build` green.

- [x] **YouTube module repaired — InnerTube rewrite (2026-08-12)**: Every panel on `/youtube` was failing at once and looked like one fault; it was three. (a) `@distube/ytdl-core@4.16.12` can no longer parse YouTube's player script ("Could not parse decipher function"), so metadata degraded to oEmbed and showed Unknown duration/views/date, and downloads died on "Failed to find any playable formats". (b) Both subtitle strategies called the **unsigned** `api/timedtext` endpoint, which now returns HTTP 200 with a **zero-length body** — they could never have succeeded for any video. (c) `&fmt=vtt` is ignored by YouTube, so the one strategy that did fetch captions ran a WebVTT parser over `<timedtext format="3">` XML, found nothing, and reported "no subtitles available" for 93,199 bytes of captions. Rebuilt on the InnerTube `player` endpoint (see the architecture note above), added `parseTimedTextXml`/`parseSubtitleBody`, and removed two fabrications: a hardcoded `available_subtitles` English entry and an invented "age-restricted or region-locked" cause printed on every download failure. Also removed the unused `export default` from `timeline.tsx` and `tasks.tsx`, which was the source of the router's code-split warnings on every page load. **567 unit tests passing** (+56), **`tsc --noEmit` clean**, **123 core exports verified**, `bun run build` green.
- [x] **Credentials Vault wired to the collectors (2026-08-12)**: The Settings page was a write-only box — it saved `data/credentials.json` and nothing in the system ever read it, so an operator could add a key, watch it save, see it badged "Active", and collect exactly as much as before. New `src/utils/credential-vault.ts` is now the store the collectors resolve from, with a nine-provider registry, env-first resolution, real per-provider verification probes, masked listing plus a separate reveal call, and a computed capability matrix. Two fabrications removed: `status: "Active"` written at save time for an untested secret (now `unverified` until a live call says otherwise, and legacy `"Active"` rows are read back as `unverified`), and `lastUsed: "Never"` stored as data (now `null`). **New collection unlocked:** authenticated Bluesky `searchPosts` (`fetchBlueskySearch`) — historical keyword search, previously listed as a stated limitation because it 403s unauthenticated — and authenticated Mastodon `api/v2/search` (`fetchMastodonSearch`), reaching posts that carry no hashtag. Reddit, UCDP and GitHub now resolve from the vault as well as the environment. **511 unit tests passing** (+41), **`tsc --noEmit` clean**, **113 core exports verified**, `bun run build` green. Two pre-existing defects fixed in passing — see the note below.
- [x] **YouTube Ingestion for OSINT & Social Intelligence (2026-08-11)**: Implemented full YouTube video ingestion with a Python FastAPI backend (`yt-dlp` Python API) and a TanStack Start frontend route (`/youtube`). Includes URL host validation (`youtube.com`/`youtu.be`), metadata extraction without video download, subtitle timestamp segment parsing (`[{start, end, text}]`), analyst-initiated single-video MP4 artifact download into `YT_DOWNLOAD_DIR` with audit logging, privacy-mode embed player (`youtube-nocookie.com`), clickable timestamp seeking, handoff to video analysis pipeline (`/videos`), 5 Python unit tests passing (`pytest tests_py/`), 470 JS/TS unit tests passing (`bun test`), 90 core exports verified, and `tsc --noEmit` clean.
- [x] **v1 Feature & Intelligence Merge (2026-08-11)**: Merged missing working functions, APIs, resources, OSINT tools, agents, crawlers, and GIS data sources from `social-media-ai-agents-` (v1) into `v2`. Added real-time GPS jamming feed (`gps-interference.ts`), environmental radiation monitoring network (`radiation.ts`), CISA KEV cyber intelligence (`cyber-intel.ts`), social velocity & posting volume spike calculator (`social-velocity.ts`), Telegram topic classifier (`telegram-intel.ts`), multi-domain threat classifier (`threat-classifier.ts`), spatio-temporal focal point convergence engine (`focal-point.ts`), OSINT route updates, and collector health probes. **470 unit tests passing**, **85 core exported symbols verified**, **`tsc --noEmit` clean**.
- [x] **Fabrication sweep + gate repair (2026-08-11)**: Removed the last three fabricated outputs — `osint.tsx`'s invented RSS fallbacks (real outlet names, current timestamps), its six hardcoded Overview cards ("3 repos leak internal endpoints"), and `tasks.tsx`'s synthetic Module 1 probe with its `50` / `0.5` defaults. Also fixed a live `ReferenceError` on the Tasks page, and an entity-key regex covering only U+0900–U+0DFF that silently merged every Urdu entity name into one. `tsc --noEmit` clean for the first time; lint 10,051 → 224 problems (all `no-explicit-any`). New `src/utils/osint-summary.ts` + 21 tests; `fromSocialPost` now refuses a platform the frozen contract does not define rather than remapping it.
- [x] **Container & Azure Deployment Drift Resolution (2026-08-10)**: Replaced broken v10 auth drift with demo session (`src/utils/demo-session.ts`). Live version has since moved on — see the deployed-state note above.
- [x] **Data Contract Freeze (2026-08-06)**: Six inter-developer boundary types frozen in `src/types/core.ts` (Article, Post, Entity, Finding, MediaAsset, VideoAsset) + adapters in `src/types/core-adapters.ts`.
- [x] **Module 1 (Source Credibility)**: Synchronous scoring in `credibility.ts`, 7 PS-18 factors, language marker assessment in `credibility-llm.ts`.
- [x] **Module 2 (Open-Source Content Analysis)**: Topic clustering, stance detection, entity extraction in `analysis.ts` & `analysis-llm.ts`.
- [x] **Module 3 (Social Media Analysis)**: Bluesky Jetstream WS, public AppView, Reddit OAuth, Mastodon hashtag timelines, Telegram preview, CIB signal detection in `cib.ts` & `social.ts`.
- [x] **Module 3 Collection Integrity (2026-08-10)**: Reddit migrated to OAuth after unauthenticated access started returning 403 everywhere; Mastodon added as a keyless second open feed; the fabricated Instagram/Facebook cache and both fake scraper scripts removed; `/crawlers` replaced its invented throughput figures with a live reachability probe.
- [x] **Module 4 (Media Analysis & Provenance)**: C2PA manifest verification, EXIF parsing, Tesseract OCR, DCT pHash in `imaging.ts` & `imaging-client.ts`.
- [x] **Module 5 (Reports & GIS)**: Provider-agnostic LLM client (`llm.ts`), citation validator, PDF export, UCDP/USGS/GDELT integration in `reports.ts`, `geo.ts`, `geo-sources.ts`.
- [x] **Project Memory & Preservation System (2026-08-10)**: Created `PROJECT_MEMORY.md`, `.claude/rules/memory-and-preservation.md`, updated `CLAUDE.md`, and added `scripts/check-exports.ts`.

### Two pre-existing defects fixed on 2026-08-12

Both were found while wiring the vault and both silently disabled a safety net.

1. **`.gitignore` was ignoring the entire repository.** Commit `d935aef` ("ignore root
   `*.js`") appended the pattern as **UTF-16LE**, so the bytes on disk were
   `* \0 . \0 j \0 s \0`. Git parses the line up to the NUL and reads the pattern as bare
   **`*`** — every untracked file in the tree, matched. Since that commit no new file could
   be `git add`ed without `-f`, including the two added here. Replaced with a correct
   `/*.js`. **If a new source file ever seems to vanish from `git status`, check this line
   first.**
2. **`scripts/check-exports.ts` had failed on every run since the YouTube feature landed.**
   It required `fetchYoutubeMetadata`, `fetchYoutubeSubtitles` and `downloadYoutubeVideo`
   from `youtube-collector.ts`, which has never exported those names — the public symbols
   are the `serverFetch*` / `serverDownload*` `createServerFn` wrappers over private
   `_getMetadata` / `_getSubtitles` / `_getDownloadUrl`. A permanently red gate is worse
   than no gate: a real deletion would have hidden among those three. Registry corrected;
   the audit now passes at 113 symbols.

### Collection policy — the ingestion-legality matrix (2026-08-12) — READ BEFORE ADDING A COLLECTOR

`src/utils/collection-policy.ts` is the single source for **what may be collected, on what basis,
and by what route**. It supplements `PLATFORM_NOTES` (which answers "does this deployment collect
it"); the two answer different questions and both are needed.

| Source | Mode | Basis | How content gets in |
| --- | --- | --- | --- |
| Instagram, Facebook | `manual-only` | Platform ToS + DPDP Act 2023 | Analyst captures a public post and attests to it |
| YouTube | `partial` | Official API + ToS | API for metadata/comments/captions; **frames only via analyst-initiated download** |
| News (RSS), GDELT, HN | `automated` | Syndication by design | Already automated |
| Reddit, Telegram, Bluesky, Mastodon | `automated` | Official/public APIs | Text and **media URLs** |
| X / Twitter | `none` | No free tier | Nothing automatic — a commercial limit, not a legal one |

- **`allowsAutomatedCollection()` returns `false` for an unknown source.** Absence of a policy is a
  gap to close, never a licence. Do not invert this.
- **Media is collected as URLs, never bytes.** Re-hosting is redistribution, and under the DPDP Act
  these are images of identifiable people. Bytes are fetched only when an analyst sends one asset to
  Module 4.
- **`SocialPost.media` — `undefined` means NOT COLLECTED, `[]` means collected-and-none.** Load
  bearing. A contract-sourced post leaves it `undefined`; see `CONTRACT_MEDIA_LIMITATION` in
  `core-adapters.ts` for why the frozen `Post` is not gaining a media field.
- **Manual captures are `AttestedCapture`, never `SocialPost`.** Fixed marker
  `provenance: "analyst-attested-capture"`, mandatory source URL / capturer / capture time, and they
  must never be counted in volume, rate or coordination signals. This is the exact distinction v1's
  `agent_scraper.py` collapsed when it wrote fabricated Instagram posts into the real collectors'
  cache.

**Payload shapes verified live 2026-08-12 — two do not match the documentation:**

- **Bluesky ships `app.bsky.embed.gallery`** (up to 10 images) whose fields are `items[].thumbnail`,
  *not* the `images[].thumb` of the older `images` embed. An extractor written from the documented
  shape silently drops every carousel post. Also note the raw Jetstream record stores blobs as CIDs
  (compose `cdn.bsky.app/img/feed_fullsize/plain/{did}/{cid}` — verified HTTP 200 image/webp) while
  the AppView returns resolved URLs; both paths exist and must not be merged.
- **A t.me page carries ~85 `background-image` declarations of which ~4 are post photos** — the rest
  are avatars and emoji. The selectors are class-scoped to `tgme_widget_message_photo_wrap` and
  `tgme_widget_message_video_thumb`; an unscoped regex reports a channel avatar as evidence.
- **Reddit's shape is the one NOT verified live** (no OAuth credential in this environment). Its
  fixture is hand-built. Confirm against a real response once a script app is registered.

### Two more pre-existing defects fixed on 2026-08-12

1. **Telegram posts were misattributed.** `fetchTelegramChannel` scanned the page three times into
   parallel `ids` / `texts` / `times` arrays and zipped them by index. Any message without a text
   block — i.e. every photo-only post — shifted all later text up one slot, so a channel posting
   `[text A][photo][text B]` rendered post 2 carrying **text B** under the photo's id, URL and
   timestamp. Confirmed live on `t.me/s/durov`, where `durov/522` is media-only. Replaced with
   `splitTelegramMessages` (slices on `data-post="` boundaries) + `telegramBlockToPost`, so every
   field of a message stays together — which is also the only way media can attach to the right post.
2. **`/images` had no `validateSearch`.** The new Analyse hand-off would have navigated and done
   nothing. Added, and it rejects anything that is not an absolute `http(s)` URL, so a crafted
   `javascript:` or `data:` param cannot reach the fetch.

### Pending Backlog / Roadmap

0. **Obtain the Reddit credential** — free script app at reddit.com/prefs/apps. Reddit
   collection is dead without it. **No longer needs a redeploy**: enter the client ID and
   secret on `/settings` and press Verify. `REDDIT_CLIENT_ID` / `REDDIT_CLIENT_SECRET` stay
   the durable path, and still take precedence.
1. **Obtain a Bluesky app password** — the largest single collection gain available, and
   free. It turns Bluesky from forward-only firehose monitoring into historical keyword
   search. Same route: `/settings` → Bluesky → Verify.
2. **Module 1 Enhancement**: Persist custom weight profiles to a backend API (currently localStorage `sentinel_credibility_profiles`).
3. **Module 5 GIS Enhancement**: ~~Add UCDP API Token configuration UI~~ — **done 2026-08-12**, the token is a vault provider. Still needs the token itself from ucdp.uu.se.
4. **Vault durability**: `data/` is not a mounted volume and is excluded from the build
   context, so a vault credential dies with the replica on Container Apps. Fine for a demo;
   a real deployment needs Key Vault (which the env path already uses) or a mounted secret.
5. **vLLM Self-Hosted Migration**: Prepare config switch once Azure NC8as-T4 GPU quota is approved.

---

## 2. Non-Destructive Update Protocol (Anti-Deletion Rules)

> [!CAUTION]
> **NEVER DELETE EXISTING EXPORTED FUNCTIONS OR TYPES.**
> Partial updates using snippet views frequently lead AI models to replace entire files with small code snippets, wiping out existing functions. Follow these rules on EVERY edit.

### Rule 1: Full Context Verification Before Editing

- Before editing any file in `src/utils/`, `src/types/`, `src/routes/`, or `tests/`, view the entire file or target function block.
- Identify all existing `export function`, `export const`, `export type`, and `export interface` statements.

### Rule 2: Additive-Only Extensions

- **Never rename or remove an exported symbol** without explicit user instruction and project-wide refactoring.
- When expanding functionality:
  - Add optional parameters to existing signatures (e.g. `opts?: NewOptions`).
  - Add new helper functions alongside existing functions.
  - Wrap logic rather than replacing existing function signatures.

### Rule 3: Export Registry Audit

- Run `bun scripts/check-exports.ts` before committing code updates to ensure no public exports were dropped.

### Rule 4: Mandatory Post-Edit Verification

- Run `bun test` after EVERY modification. If any of the 415+ tests fail, fix the breakage immediately before reporting completion.

---

## 3. Codebase Inventory & Export Registry

This registry lists key files and their exported symbols. When adding features, verify that these exports remain intact.

### Core Types & Seams (`src/types/`)

- **[core.ts](file:///d:/social_media_research/src/types/core.ts)**: Frozen boundary contracts (`ArticleSchema`, `PostSchema`, `EntitySchema`, `FindingSchema`, `MediaAssetSchema`, `VideoAssetSchema`, `ContractViolationError`, `parseMany`).
- **[core-adapters.ts](file:///d:/social_media_research/src/types/core-adapters.ts)**: Seam adapters (`toAnalysisArticle`, `fromAnalysisArticle`, `toSocialPost`, `fromSocialPost`, `toGeoPoint`, `PostDegradation`, `CONTRACT_MEDIA_LIMITATION`).
  - `degraded[]` means "the producer could have supplied this and did not", which is why a fully populated fixture must report **zero** entries. Media is deliberately NOT in that list — the frozen `Post` has no media field, so no producer can ever supply it; flagging it per post would make the list describe the seam rather than the record. It is stated once as `CONTRACT_MEDIA_LIMITATION`.

### Intelligence & LLM Layer (`src/utils/`)

- **[llm.ts](file:///d:/social_media_research/src/utils/llm.ts)**: Open-source LLM client speaking OpenAI format.
  - Exports: `chat`, `chatJson`, `summariseText`, `extractEntitiesFrom`, `assessLanguageOf`, `getLlmStats`, `llmStatsSnapshot`, `LlmUnavailableError`.
- **[osint-summary.ts](file:///d:/social_media_research/src/utils/osint-summary.ts)**: OSINT Overview collection summary, pure half. Lives here rather than in `routes/osint.tsx` because a route file calls `createFileRoute` at module load and so cannot be imported by `bun test` — which is why the hardcoded cards it replaces went uncaught for so long.
  - Exports: `buildOverviewModules`, `rssEmptyReason`, `formatFeedDate`, `OverviewModule`, `OverviewTone`, `OverviewInput`, `RssCollection`.
- **[credibility.ts](file:///d:/social_media_research/src/utils/credibility.ts)**: Module 1 deterministic scoring.
  - Exports: `scoreArticle`, `scoreCorpus`, `defaultFactors`, `TIER_SCORES`, `DOMAIN_REPUTATION`.
- **[credibility-llm.ts](file:///d:/social_media_research/src/utils/credibility-llm.ts)**: Module 1 linguistic factor via LLM.
  - Exports: `assessArticleLanguage`, `assessLanguageFor`, `assessmentSummary`.
- **[credential-vault.ts](file:///d:/social_media_research/src/utils/credential-vault.ts)**: The credential store the collectors resolve from, and the backend of `/settings`. Resolution order is **environment first, vault second** — Key Vault `secretref:` env vars are the audited path and must not be shadowed by a file on an ephemeral replica. `status` is measured by a live call, never asserted at save time; a non-collectable provider (Instagram, Facebook) is permanently `unusable` and can never be verified.
  - Exports: `CREDENTIAL_PROVIDERS`, `CredentialVaultError`, `STATUS_LABELS`, `providerById`, `normaliseStatus`, `normaliseEntry`, `normaliseVault`, `maskSecret`, `secretTail`, `redactEntry`, `redactVault`, `normaliseHost`, `readVault`, `writeVault`, `resolveCredential`, `recordCredentialUse`, `verifyProviderCredential`, `buildCapabilityMatrix`, `githubHeaders`, plus the server functions `listCredentialProviders`, `listCredentials`, `addCredential`, `deleteCredential`, `revealCredential`, `verifyCredential`, `capabilityMatrix`.
  - `writeVault` replaces the **whole** file, so a stale client (an old tab, a page loaded before another was edited) can submit a vault that drops entries it never knew about. It therefore copies the previous generation to `data/credentials.prev.json` before each overwrite — ignored by `.gitignore` and covered by `.dockerignore`'s wholesale `data/` rule. **If a credential disappears from `/settings`, look there first.**
  - **Do not re-add an Instagram or Facebook collector behind these entries.** The bottom of the file records exactly what v1's `agent_scraper.py` / `agent-scraper.js` did with these same rows — instaloader login, then a Google-News-RSS fallback relabelled as Meta posts, then hardcoded posts with 842 and 420 likes. That decision is settled.
- **[collection-policy.ts](file:///d:/social_media_research/src/utils/collection-policy.ts)**: The ingestion-legality matrix — see the dedicated section above. Answers "may this be collected, on what basis, by what route"; `PLATFORM_NOTES` answers "does this deployment collect it". **`allowsAutomatedCollection()` returns `false` for an unknown source — absence of a policy is a gap, not a licence.**
  - Exports: `COLLECTION_POLICIES`, `MODE_LABELS`, `BASIS_LABELS`, `BASIS_DETAIL`, `policyFor`, `policyById`, `allowsAutomatedCollection`, `policySummary`, `CollectionMode`, `LegalBasis`, `CollectionPolicy`.
- **[manual-evidence.ts](file:///d:/social_media_research/src/utils/manual-evidence.ts)**: Analyst-attested capture — the ONLY route by which Instagram/Facebook content enters. Deliberately **not** a `SocialPost` and **not** a contract `Post`: a collected post is an observation, a screenshot is an observation of a screen whose source is the analyst. Crosses to Dev 1 as a `MediaAsset` with `source: "analyst upload — <url>"`, so `PlatformSchema` never has to widen.
  - Exports: `AttestationError`, `CAPTURE_PLATFORM_LABELS`, `CAPTURE_CAVEATS`, `ATTRIBUTION_LIMITATION`, `isPublicPostUrl`, `buildAttestedCapture`, `attestedCaptureToMediaAsset`, `AttestedCapture`, `CapturePlatform`.
  - `attestedCaptureToMediaAsset` **throws** when `phash` is null rather than inventing one — a synthesised perceptual hash matches nothing, which renders as "no near-duplicates found", a finding from a value never measured.
- **[evidence.ts](file:///d:/social_media_research/src/utils/evidence.ts)**: `sha256OfFile` extracted from `routes/vault.tsx` so the vault and the capture panel hash identically. Refuses rather than falling back when SubtleCrypto is unavailable — this digest was once 64 random hex characters.
  - Exports: `EvidenceIntegrityError`, `sha256OfFile`, `bytesToHex`, `isSha256`, `HASH_MEANING`.
- **[evidence-store.ts](file:///d:/social_media_research/src/utils/evidence-store.ts)**: The single owner of `sentinel_evidence`. That key had TWO writers with two independently-declared shapes — `routes/vault.tsx` (inlining the literal three times) and `components/manual-capture-panel.tsx`, whose own comment admitted it: *"Local mirror of vault.tsx's stored shape."* Same duplication `evidence.ts` was extracted to prevent for hashing.
  - Exports: `EVIDENCE_KEY`, `EvidenceRecord`, `withoutSeeded`, `getEvidence`, `saveEvidence`, `appendEvidence`, `deleteEvidence`, `setEvidenceCase`, `nextEvidenceId`.
  - **`withoutSeeded` drops only `seeded: true` rows.** The three that shipped carried `caseId: "INV-2041"` / `"INV-2038"` — ids `createInvestigation` can never mint, since it numbers from INV-1001 up — so every one rendered a bold blue case reference resolving to nothing. It **migrates rather than wipes**: the same key holds real analyst uploads and attested manual captures.
  - **`nextEvidenceId` never reuses an id.** The route computed `EVID-0${400 + list.length + 1}`, so deleting one record and adding another produced two exhibits under one identifier — in the one store whose purpose is identifying exhibits.
- **[bookmark-store.ts](file:///d:/social_media_research/src/utils/bookmark-store.ts)**: The `/live` shortlist. v1 was a bare `string[]` of URLs that **no file in the repo ever read back**, discarding publisher, date, headline and body — so a bookmark could never be reconstituted into `PinnedEvidence`.
  - Exports: `BOOKMARK_KEY`, `Bookmark`, `BookmarkInput`, `migrateBookmarks`, `getBookmarks`, `saveBookmarks`, `isBookmarked`, `toggleBookmark`, `removeBookmark`, `setBookmarkCase`, `shortlisted`, `pinnedBookmarks`.
  - **Migration back-fills NOTHING.** A v1 record keeps its URL and reports every other field as `null`, because v1 genuinely did not store them. Inventing a headline or date at migration time would manufacture provenance.
- **[live-filters.ts](file:///d:/social_media_research/src/utils/live-filters.ts)**: The `/live` date window, extracted so the predicate is testable — a route module calls `createFileRoute` at load and cannot be imported by `bun test`.
  - Exports: `DATE_WINDOWS`, `DEFAULT_WINDOW_ID`, `DateWindow`, `windowHours`, `withinWindow`, `WINDOW_REACH_NOTE`.
  - **An undated item passes every window**, deliberately — dropping it would delete real reporting over a field the publisher never supplied. **An unknown window id fails open.** "Any time" is `hours: null`, a real window rather than a missing key the predicate happened to no-op on.
- **[graph-build.ts](file:///d:/social_media_research/src/utils/graph-build.ts)**: Module 2's entity co-occurrence graph. Replaces a fixed ten-node topology written into `routes/graph.tsx` with literal x/y coordinates.
  - Exports: `ENTITY_TYPES`, `normaliseEntityType`, `entityKey`, `buildEntityGraph`, `degreeCentrality`, `shortestPath`, `layoutGraph`, `nodeRadius`, `COOCCURRENCE_CAVEAT`, plus the `GraphNode` / `GraphEdge` / `EntityGraph` / `PositionedNode` types.
  - **`layoutGraph` is deterministic** — Fruchterman–Reingold seeded **on a circle by index**, never randomly. A graph that reshuffles between renders cannot be cited, and `Math.random()` is banned in this layer. Hand-written rather than adding d3-force or cytoscape, the same call as the DCT hash in `imaging.ts`.
  - **`entityKey` moved here from `entities.tsx` unchanged.** Read its comment before touching it: the old class covered U+0900–U+0DFF only, so every Urdu name (Arabic script) was stripped to an empty key and merged into one node.
  - **No betweenness, no modularity, no "avg. degree".** Degree over a graph we built is computable and is shown; modularity needs a walked follow graph this system does not have. `network.tsx:372-377` records what happened last time one was printed anyway.
  - `COOCCURRENCE_CAVEAT` must stay on screen: an edge means two entities were named in the same article, and **nothing more**. A node-edge diagram is very good at making a weak claim look strong.
- **[social.ts](file:///d:/social_media_research/src/utils/social.ts)**: Module 3 collection & monitors.
  - Exports: `eventToPost`, `monitorMatches`, `assessSpike`, `bucketise`, `readMonitor`, `fetchProfile`, `fetchProfiles`, `fetchAuthorFeed`, `redditCredentials`, `resolveRedditCredentials`, `resetRedditToken`, `fetchRedditSearch`, `fetchTelegramChannel`, `fetchBlueskySearch`, `resetBlueskySession`, `fetchMastodonTag`, `mastodonStatusToPost`, `fetchMastodonSearch`, `stripMastodonHtml`, `mastodonLinks`, `MASTODON_INSTANCES`, `MASTODON_DEFAULT_INSTANCE`, `socialMastodon`, `socialBlueskySearch`, `socialMastodonSearch`, `socialCredentials`, `PLATFORM_NOTES`, `SocialUnavailableError`, `blueskyMediaFromRecord`, `blueskyMediaFromView`, `redditMediaFrom`, `telegramMediaFrom`, `mastodonMediaFrom`, `splitTelegramMessages`, `telegramBlockToPost`, `SocialMedia`.
  - **`SocialPost.media`: `undefined` = NOT COLLECTED, `[]` = collected and none.** Load bearing; do not collapse. Media is collected as **URLs only, never bytes** — re-hosting is redistribution and these are images of identifiable people under the DPDP Act.
  - `redditCredentials()` stays **synchronous and env-only** — call sites depend on that and its tests pin it. `resolveRedditCredentials()` is the async superset the collector uses, which also reads the vault.
  - **Removed 2026-08-10 (deliberate, not a regression):** `socialCache`. It read `data/social_cache.json`, whose only writers were `scripts/agent-scraper.js` and `scripts/agent_scraper.py` — both fabricated Instagram/Facebook posts with `Math.random()` engagement counts. All 128 records were invented. Both scripts and the reader are deleted; do not restore them (see §4 rule 2).
- **[collector-health.ts](file:///d:/social_media_research/src/utils/collector-health.ts)**: Live reachability probe for every collector endpoint (replaces the invented `/crawlers` telemetry).
  - Exports: `probeCollectors`, `collectorHealth`, `CollectorProbe`, `ProbeStatus`.
- **[cib.ts](file:///d:/social_media_research/src/utils/cib.ts)**: Module 3 Coordinated Inauthentic Behavior detection.
  - Exports: `analyseCib`, `assessCluster`, `temporalSynchrony`, `contentDuplication`, `accountMaturity`, `handlePatterns`, `amplification`.
- **[imaging.ts](file:///d:/social_media_research/src/utils/imaging.ts)** & **[imaging-client.ts](file:///d:/social_media_research/src/utils/imaging-client.ts)**: Module 4 media & provenance.
  - Exports: `pHash`, `hammingDistance`, `interpretExif`, `interpretC2pa`, `interpretOcr`, `assessProvenance`.
- **[reports.ts](file:///d:/social_media_research/src/utils/reports.ts)** & **[report-pdf.ts](file:///d:/social_media_research/src/utils/report-pdf.ts)**: Module 5 intelligence product generation.
  - Exports: `sourcesFromArticles`, `sourcesFromSocial`, `sourcesFromImages`, `sourcesFromGeo`, `renumber`, `buildSourceContext`, `validateCitations`, `citedSourceNumbers`, `generateProduct`, `toMarkdown`, `renderProductPdf`.
- **[geo.ts](file:///d:/social_media_research/src/utils/geo.ts)** & **[geo-sources.ts](file:///d:/social_media_research/src/utils/geo-sources.ts)**: Module 5 GIS & spatial analysis.
  - Exports: `isRealCoordinate`, `fromUsgsFeature`, `fromUcdpEvent`, `GEO_LAYERS`, `collectSeismic`, `collectConflict`, `collectNewsGeo`, `collectGeoLayers`.
- **[dorks.ts](file:///d:/social_media_research/src/utils/dorks.ts)**: OSINT query dork generators.
  - Exports: `buildDork`, `toDomain`, `DORK_TEMPLATES`.
- **[recon-sources.ts](file:///d:/social_media_research/src/utils/recon-sources.ts)**: Infrastructure recon utilities.
  - Exports: `parseIssuerOrg`, `isWithinDomain`, `collectCrtShSubdomains`.

### Python backends (separate from the TS/JS export registry above)

- **`app/`** (repo root): FastAPI YouTube ingestion/collector backend (`yt-dlp`). Tests in `tests_py/`. Own `requirements.txt` at repo root.
- **`ai-service/`** (merged 2026-08-14): FastAPI vision/forensics backend — `app/forensics.py` (EXIF/GPS/pHash/C2PA), `app/ocr.py` (Tesseract), `app/detect.py` (Grounding DINO). Own `requirements.txt`/`.env.example`/`Dockerfile`/`docker-compose.yml` inside `ai-service/`, deploys independently of both the frontend and `app/`. Tests in `ai-service/tests/`. See `CLAUDE.md`'s `ai-service/` section for the full status table and the milestone entry above for what was and wasn't verified at merge time.

---

## 4. Hard Constraints & System Policies

1. **Open-Source LLMs Only**: Sarvam (`sarvam-105b`), Groq (`openai/gpt-oss-120b`). **Meta Llama models are explicitly BANNED** due to Meta's Acceptable Use Policy prohibiting military and espionage use (disqualifying for IAF PS-18).
2. **Data Honesty**: Never fabricate data or placeholder confidence scores. Unmeasured values must strictly be `null`, never `0` or default fallbacks.
3. **Zero Budget / Free Tier**: All external tools must run in-browser (WASM/WSS) or use keyless/free open endpoints.
4. **Coordinate Honesty**: No record without a real coordinate is plotted on GIS. `0,0` is treated as missing.
