fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
# torch/torchvision: on a CUDA machine, install from the matching wheel index
# BEFORE this file, and pin both together in that same command — letting pip's
# resolver pull torchvision from default PyPI here can silently swap in an
# unpinned, wrong-index torch build and try to uninstall the working one
# (hit live during this project's build; mid-uninstall failure on a locked
# DLL left torch partially removed). Verified working:
#   pip install "torch==2.5.1" "torchvision==0.20.1" \
#     --index-url https://download.pytorch.org/whl/cu121 \
#     --extra-index-url https://pypi.org/simple
# CPU-only: plain `pip install torch==2.5.1 torchvision==0.20.1` is fine.
torch==2.5.1
torchvision==0.20.1
python-multipart==0.0.20
numpy==2.2.1
pillow==11.1.0
piexif==1.1.3
pytesseract==0.3.13
scipy==1.14.1
# 5.15.0 (this file's previous pin) does not exist on PyPI and was never
# actually installed or tested — 4.47.1 is the version live-verified this
# session against both grounding-dino-tiny and Florence-2-large.
transformers==4.47.1
timm==1.0.28
accelerate==1.14.0
einops==0.8.2
insightface==1.0.1
scikit-image==0.26.0
# onnxruntime-gpu: pin 1.20.2, not latest (1.29.0 at time of writing). 1.29.0's
# CUDA execution provider needs CUDA-13-era runtime DLLs (cublasLt64_13.dll)
# not present on this project's dev machine; 1.20.2 targets CUDA 12.x/cuDNN 9,
# which torch's own bundled cu121 runtime already satisfies once torch is
# imported first in-process (verified live — see app/loaders.py's
# _load_insightface comment). Only install ONE of onnxruntime /
# onnxruntime-gpu at a time: both ship a `cv2`-style same-namespace package
# (`onnxruntime`) and having both installed corrupts the import at the
# file level, not just a version conflict — same failure mode as mixing
# opencv-python with opencv-python-headless below.
onnxruntime-gpu==1.20.2
# opencv-python-headless, not opencv-python: this is a server backend with no
# display. Never install both — they share files at the filesystem level, so
# installing one after the other corrupts both (verified live this session).
opencv-python-headless==5.0.0.93
# c2pa-python is intentionally NOT pinned here: it's a compiled wheel installed
# best-effort in the Dockerfile so a missing platform build never fails this
# file's install. For local dev, install it manually if you want a live C2PA
# reader instead of the "reader_unavailable" fallback: pip install c2pa-python
